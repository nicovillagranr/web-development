// 🧮 1. Analizador de cálculos
// Enunciado:
// Crea una función calcularOperaciones() que realice una serie de operaciones matemáticas (suma, resta, potencia, raíz).
// Usa console.log() para mostrar cada resultado.
// Usa console.group() para agrupar operaciones por tipo.
// Si una operación produce NaN, muéstrala con console.error().

function calcularOperaciones(operador1 , operacion , operador2){
    console.group(`Operacion: ${operacion}`)

    let resultado

    switch 
}