# TypeScript + React — Entrenamiento

Proyecto único de entrenamiento progresivo en TypeScript puro y React + TypeScript.
Cada carpeta en `src/exercises/` agrupa ejercicios por tema en orden de dificultad creciente.

> ⚠️ **Este archivo es la BITÁCORA, no la especificación.** Cuenta qué pasó en cada
> sesión y con cada archivo. Para **montar o revisar un ejercicio**, la fuente de
> verdad del formato es **[`docs/AUTORIA-DRILLS.md`](docs/AUTORIA-DRILLS.md)** (spec
> v3, vigente desde el 11 ago 2026). Las reglas de trabajo del día a día están en
> [`CLAUDE.md`](CLAUDE.md).

## Stack

- Vite + React 19 + TypeScript (strict + `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes`)
- Vitest + Testing Library (jsdom)
- pnpm

## Scripts

```bash
pnpm dev          # arranca la app (FASE 2 en adelante)
pnpm test         # vitest en modo watch
pnpm test:run     # vitest una sola vez
pnpm test:ui      # UI de vitest
pnpm typecheck    # tsc -b  ⚠️ `tsc --noEmit` a secas NO comprueba nada y devuelve 0
pnpm lint         # eslint
pnpm build        # build de producción
```

## Estructura

```
src/exercises/
├── 01-tipos-basicos/
├── 02-funciones/
├── 03-arrays/
├── 04-objetos/
├── 05-unions-narrowing/
├── 06-generics-basicos/
├── 07-utility-types/
├── 08-discriminated-unions/
├── 09-react-props/          .tsx en adelante
├── 10-eventos-formularios/
├── 11-useState-useReducer/
├── 12-custom-hooks/
├── 13-fetch-tipado/
├── 14-context-refs/
├── 15-componentes-genericos/
└── 16-escenarios-reales/
```

Cada ejercicio: `exercise-NN.ts(x)` + `exercise-NN.test.ts(x)`.

## Contrato del modo coach (vigente)

Después de probar un formato más estricto los 3 primeros ejercicios y ver que no facilitaba el aprendizaje, el contrato vigente es:

### Metodología — Concepto → Ejemplo → Mini-ejercicio

Cada concepto se trabaja en un ciclo de 3 pasos:

1. **Explicación** (Claude habla, 1-3 párrafos): qué es, por qué existe, ejemplo mínimo de código.
2. **Ejemplo guiado** (Claude muestra, 1-2 ejemplos comentados): uso correcto, y cuando aplique uso incorrecto. El usuario puede preguntar antes de pasar al ejercicio.
3. **Mini-ejercicio** (usuario practica): un solo concepto aislado, 1-3 huecos pequeños. El test verifica solo eso.

### Reglas del coach

- **Explicar primero, evaluar después.** No pedir descubrir conceptos sin haberlos enseñado.
- **Un concepto nuevo por ejercicio.** No mezclar 3 cosas a la vez.
- **Pistas conceptuales desde el principio**, no solo cuando se bloquee.
- **Mantener exigencia técnica**: nada de `any` sin razón, justificar decisiones, entender los tipos antes de marcar como aprobado.
- **Formato del archivo**: cada `.ts`/`.tsx` empieza con una sección "EXPLICACIÓN" (concepto enseñado por escrito) seguida del "EJERCICIO" (los huecos a rellenar).

### Progresión actual

> **Nota de reorganización (jun 2026):** los ejercicios vivían todos en `01-tipos-basicos/`
> como una escalera plana 01→12. Se reubicaron por tema a su carpeta correspondiente y se
> renumeraron dentro de cada carpeta (empezando en `exercise-01`). Abajo se indica entre
> paréntesis el nº plano antiguo para rastrear referencias didácticas internas que aún lo citan.

- `01-tipos-basicos/` — **CARPETA CERRADA (jun 2026): 01–04 APROBADOS; 05–10 COMPLETOS.** 01 inferencia vs anotación, 02 arrays/null/uniones simples, 03 tuplas/`readonly`/`as const`, 04 `readonly` vs `as const` (formato nuevo). 01–03 en formato antiguo denso; 04 en adelante, formato nuevo.
  - **05 (type aliases) — COMPLETO (5/5 + typecheck limpio, jun 2026).** Es el archivo con el que ABRIÓ la sesión (tenía un `=> )` con cuerpo de arrow vacío → error de sintaxis que rompía el parseo y mataba el autocompletado de VSCode; ya estaba arreglado). Drills 1–4 ya pasaban; quedaba el 5 `nombresActivos` (filter + map encadenados sobre type alias `Usuario`). Lo cerró en DOS capas leyendo los fallos: (a) cuerpo — primero devolvía los Usuarios enteros con un `.map(u => u)` no-op; lo corrigió a `filter(activo).map(u => u.nombre)` en el orden correcto (filter necesita `estado`, así que va antes); (b) tipo — el runtime pasó pero `tsc` cazó `TS2322` (`Usuario[]` declarado vs `string[]` devuelto); cambió la firma a `string[]`. Refuerzo perfecto del tema de la sesión: **test verde ≠ tipos correctos**, el listón es runtime + typecheck.
  - **06 (`unknown` vs `any`) — COMPLETO (5/5, jun 2026).** Estrechar `unknown` con `typeof` + `Array.isArray` como portero dedicado (cazó que `typeof [] === "object"`, no `"array"`; simplificó él el `typeof === "object" &&` redundante). Sintió el peligro de `any` en carne propia: el crash de `.toUpperCase()` sobre un number, y la **mentira silenciosa** `"2" + "3" === "23"` (concatena en vez de avisar). Portero doble con `&&` para los dos valores. Conectó SOLO con caso real: `data: any` → `.toUpperCase()` sobre number → **pantallazo blanco en React** (error en render desmonta el árbol) → Error Boundary como cinturón (no frenos) → validar en el borde con Zod (`13-fetch-tipado`). Capstone `formatear` leyendo el spec al detalle (no sobre-mayúsculas). Vocabulario reincidente vigilado: **comentarios con resultado falso** (`longitud(5)` → puso `4`, es `-1`) y **retorna ≠ imprime** (llamadas sueltas que no muestran nada).
  - **07 (`typeof` como operador de tipo) — COMPLETO (5/5, jun 2026). EL MÁS DURO hasta ahora.** Derivar un TIPO de un VALOR (`type X = typeof OBJ`) como única fuente de la verdad. **Drills 1 y 3 los resolvió el coach** (se trabó y pidió "resuélvelo tú"); **2, 4 y 5 SOLO** (2 y 5 a la primera; el **4 lo autocorrigió leyendo el test que falló**, sin que se le diera la respuesta). Confusiones clave destrabadas: (a) **qué lado del `=` lleva el `typeof`** — escribió `typeof OBJ = {...}` (lo puso a la izquierda) → se enseñó la anatomía `type [nombre] = [lo que vale]`, paralela a `const x = 5`, el `typeof OBJ` va a la DERECHA. (b) **construir objeto literal `{ clave: valor }`** — escribía `{ data.host }` / `{ data.host, ... }` suelto (member expression dentro de `{}`) → **error de sintaxis que rompía TODO el archivo** (eco directo del exercise-05 roto con el que ABRIÓ la sesión: una línea inválida tira el parseo entero y mata tests/tooling). Además metió un parámetro `data` inexistente en una función sin args. (c) **valor vs tipo**: `CONFIG_BASE` tiene doble función → fuente del tipo (`typeof`) Y del valor (drill 3 `{ ...CONFIG_BASE }`, que de paso apaga el lint `no-unused-vars` "only used as a type"); `PERFIL_BASE` es SOLO type-source (drill 4 pide valores distintos invitado/0, no spread) → su lint `no-unused-vars` QUEDA, y se le explicó que es arruga benigna del diseño, no bug. Capstone `conPuerto` con patrón inmutable `{ ...c, port }` (shorthand + el de después pisa + no mutar el original) ya dominado de 04-objetos. Buenas preguntas conceptuales suyas: "¿qué retorna la llamada?" (retorna ≠ se guarda/imprime), "¿por qué ese rojo del linter?".
  - **08 (`as const` + `(typeof LISTA)[number]`) — COMPLETO (5/5 + typecheck limpio, jun 2026).** De una lista a la UNIÓN de sus literales (`const ROLES = [...] as const; type Rol = (typeof ROLES)[number]`). Verificado en DOS capas: el test usa `// @ts-expect-error` para exigir que la unión rechace valores inválidos ("jefe"/talla mala) → la directiva solo se satisface si el tipo es estricto (sin `as const`, `Rol` colapsa a `string` y el `@ts-expect-error` queda "unused" = `TS2578`). Drills 1–4 los hizo solo: 1 `esAdmin` (añadir `as const` + `r === "admin"`), 2 `recargoTalla` (cazó él "la cagada" = cuerpo bien pero parámetro seguía `string`; lo cambió a `Talla`), 3 `etiquetaRol` (switch EXHAUSTIVO sin `default` ni return final — TS lo acepta porque la unión cerrada agota los casos, `r` queda `never`), 4 `esTallaGrande` (`t: Talla` + `||`). **Drill 5 `nombresPorRol` (capstone) — costó 3 pasadas, GEMELO de `nombresActivos` (05): reincidió en `.map(u => u)` no-op (olvidó extraer `.nombre`) y en dejar el tipo de retorno viejo (`{nombre,rol}[]` en vez de `string[]`).** Sabía el patrón (lo hizo bien en 05) pero se le escapó el último paso; lo cerró leyendo test + typecheck. Ver [[feedback_slip_map_noop_y_tipo_retorno]].
  - **09 (`interface` + `extends`) — COMPLETO (5/5 + typecheck limpio, jun 2026).** `interface` como otra forma de nombrar tipos de objeto (sin `=`, se le pre-avisó por el lío del `=` del 07); `extends` para componer (Empleado extends Persona; Coche extends Vehiculo). Drills 1–4 SOLO y rápidos: 1 `presentar` (param `{nombre}`→`Persona`), 2 `ficha` (param→`Empleado`, doble `$` del template), 3 `contratar` (**cambió el tipo de retorno a `Empleado` PREVENTIVAMENTE** tras el pre-aviso — el slip empieza a corregirse; cuerpo `{...p, salario}`), 4 `describirCoche` (tuvo `c.nombre`→corrigió a `c.marca`). **Drill 5 `bienPagados` (capstone) — TERCER gemelo del patrón filter+map+nombre y TERCERA vez el mismo resbalón** (filtró bien `>= minimo` pero devolvió los objetos sin `.map(e => e.nombre)` y dejó el retorno `Empleado[]`), ESTA vez incluso tras pre-avisarle los dos slips explícitamente. Es hábito del dedo, no comprensión. Se le ofreció montar 3-4 drills cortos de "filtra y quédate con un campo" para automatizar el gesto. Ver [[feedback_slip_map_noop_y_tipo_retorno]].
  - **10 (CAPSTONE — mini-modelo de dominio) — COMPLETO (5/5 + typecheck limpio, jun 2026). CIERRA LA CARPETA.** Sin concepto nuevo: tejer 05–09 en un mini-catálogo de tienda (`as const`→unión `Categoria`, `interface` + `extends` `Producto`/`ProductoConId`, alias + filter/map). Drills 1–4 ya estaban. **Drill 5 `ofertasDe` — CUARTO gemelo del patrón filter+map+nombre, esta vez con DOBLE condición en el filter (`categoria === cat && precio <= precioMax`).** **AVANCE REAL en el slip reincidente:** por primera vez NO cayeron los dos resbalones a la vez. Lo cerró en 3 ediciones, cada una con UNA pieza buena: (1) cuerpo correcto (map con `.nombre` + doble filtro a la primera) pero firma vieja `ProductoConId[]`; (2) arregló la firma a `string[]` pero reintrodujo el no-op `.map(producto => producto)`; (3) las DOS piezas buenas a la vez. Tenía cada pieza dominada por separado desde 05; lo que faltaba era sostener ambas sin que se cayera la otra — el músculo del slip por fin cediendo. **Misconception conceptual corregida al cierre:** creyó que reusar el nombre `producto` en filter y map daba error ("choque por repetición de nombre"). NO: filter y map encadenados son ámbitos HERMANOS (no anidados), cada flecha tiene su propio scope de parámetro → mismo nombre OK (analogía dos oficinas distintas). El `TS2322` real lo causaba el no-op `=> producto`, no el nombre; confundió DOS cambios simultáneos (rename `producto`→`p` + añadir `.nombre`) y atribuyó el arreglo al equivocado. Lección transversal: cuando cambias 2 cosas y el error se va, aísla cuál fue. Ver [[feedback_slip_map_noop_y_tipo_retorno]].
- `02-funciones/` — **01–10 APROBADOS — CARPETA COMPLETA (28 jun 2026). Refuerzo 06→10 RESUELTO ENTERO por el usuario: 43/43 tests verdes.**
  - **🔁 REFUERZO 06→10 (28 jun 2026): EXPANSIÓN EN SITIO.** El usuario chocó un muro ("llegó un punto en los drills en que ya no entiendo qué hago") y diagnosticó SOLO la raíz: `06/mapConIndice` — el primer drill donde ENTREGA una función y OTRO (`.map`) la llama por él ("¿quién corre la fn? ¿cuándo?"). Pidió reforzar TODO 06→10. Decisión acordada: **expandir EN SITIO (NO archivos nuevos, NO renumerar globalmente → preserva orden y evita tocar imports/refs/README)**, con un **BLOQUE 0 de calentamiento al frente de cada archivo**, cuerpos en blanco (el usuario escribe la lógica), formato coach con sus propias analogías. **Protocolo verificado en cada archivo: solución → `pnpm test:run` verde + typecheck del folder limpio → vaciar cuerpos nuevos → confirmar rojo.**
    - **06** (callbacks): BLOQUE 0 "una función es un VALOR" (`correr`/`aplicar`/`aplicarDosVeces`/`elegirYaplicar`: TÚ aprietas el gatillo `fn(x)`) + puente `aplicarACadaUno` (ahora `.map` llama a tu fn). Drills 1–5 NUEVOS (rojos), 6–10 suyos (verdes, renumerados desde 1–5).
    - **07** (closures): BLOQUE 0 "la CAFETERA" (`multiplicador`/`sumador`/`saludador`: devolver una fn que captura un valor FIJO, antes del estado mutable). 1–3 nuevos, 4–8 suyos.
    - **08** (composición): BLOQUE 0 "componer y aplicar YA" (`g(f(x))` desnudo, devuelve el número) antes de las versiones que devuelven función — **implementa su propia intuición** (él dijo que el 5 desnudo debía ir primero). 1–3 nuevos, 4–8 suyos.
    - **09** (pipe): BLOQUE 0 "el CARRY, del bucle al reduce" (`aplicarPasosConFor` → `aplicarPasosConReduce`) + `aplicarYregistrar` (**= el drill 4 del 10 que "no entiende para nada", reconstruido con el bucle**). Se quitó un `import { aplicar } from "./exercise-01"` MUERTO. 1–3 nuevos, 4–8 suyos.
    - **10** (capstone): CALENTAMIENTO insertado ANTES del capstone (drills 5–6 `crearCaja`/`crearContadorObj` = objeto cuyas DOS funciones comparten un `let` → scaffold de `crearProcesador`). **NO se renumeraron los drills 1–4 del usuario** (su numeración y el feedback del bug del drill 3 siguen válidos); solo `crearProcesador` pasó de 5 a 7. Drills 1, 2, 4 suyos verdes; **3 sigue con su bug de firma** (`crearPipeline(pasos: Paso[])` debe ser `...pasos`; el test llama con args sueltos → `TypeError: pasos.reduce is not a function` + comentario-demo falso en la línea 75 — SIN TOCAR, es suyo para arreglar); 5–7 por resolver.
  - **✅ RESUELTO POR EL USUARIO (28 jun 2026), en una sola sesión larga, coach Concepto→Ejemplo→drill, todo runtime + typecheck:** cerró los BLOQUE 0 de 06→09 en orden + el calentamiento y capstone del 10 + su bug del drill 3. Hitos de la sesión (registrar para FASE 2):
    - **06 (función-como-valor):** los 5 a la primera. VOCAB win: nombró las piezas en voz alta y autocorrigió su modelo ("`fn` no tiene parámetro" ✔ vs "no tiene tipo" ✗ — su tipo es `() => string`). Cazó/corrigió un comentario falso (`elegirYaplicar(10,true,...)` → puso `//10`, es **11**; copió la entrada en vez del retorno). Buena pregunta suya: por qué `.map(fn)` y no `.map(fn())` → se aterrizó en `onClick={handleClick}` de React.
    - **07 (cafetera):** drills 1–2 a la primera, usando él solo la forma de 2 pasos (construir ≠ usar). Pregunta conceptual fina: "¿la función retorna la ejecución del retorno?" → se separó RETORNAR la función (momento ①) de EJECUTARLA después (momento ②). Drill 3 costó 3 pasadas pero **solo por mecánica de strings** (orden + separador `", "`), NO por el closure (que estuvo bien desde el 1er intento) — se le reencuadró así explícitamente.
    - **08 (composición):** 1 y 3 a la primera; en el 2 invirtió la anidación (`funcionUno` afuera en vez de adentro) pese a tenerlo bien en el 1 → se corrigió con el contraste lado a lado de sus propios drills.
    - **09 (pipe/carry):** drill 1 primero como `return ( for ... )` → **error de sintaxis** (no se puede `return` un `for`: statement ≠ valor); se enseñó statement vs expresión + la "puerta giratoria" (return fuera del bucle). Tras eso, los 3 limpios con trazas de comentario CORRECTAS. Pregunta de mecanismo muy buena sobre `actual = paso(actual)` (lee viejo→escribe nuevo; `=` es asignar, no "es igual"). **`aplicarYregistrar` (el efecto/`registro`) lo escribió limpio** — la función que dijo no entender "para nada" en el 10.
    - **10 (capstone):** crearCaja/crearContadorObj (objeto de closures) tras pedir "no lo entiendo" → se ancló en **`useState` es este patrón** + carrito encapsulado (su e-commerce). Capstone `crearProcesador` a la primera (fusión contador+pipe); cazó/corrigió su propio *shadowing* `(n,paso)`→`(acum,paso)`. El bug del drill 3 (`...pasos`) le costó 3 pasadas reincidiendo en hacer **código y demo coherentes con array** mientras el test pedía rest — patrón a recordar: *"el tipo se elige por lo que el llamador pasa; si el test pasa sueltas, la firma lleva `...`"*. Reapareció el slip de comentario falso (líneas-demo) y el de **no guardar el archivo** (varios "revisa de nuevo" sin cambios).
  - **▶ PRÓXIMO HILO:** 02-funciones cerrada. Quedan por CREAR los 10-ejercicios de `03-arrays`/`04-objetos`/`05-unions`/`06-generics`/`08-discriminated` (ver proyecto abajo), y la FASE 2 (React+TS), donde se reusan: cafetera/ENTREGAR≠EJECUTAR (props-función, `onClick`), objeto-de-closures (`useState`/custom hooks), pipe/carry. Ver [[feedback_drills_en_escalera]], [[feedback_aprende_con_analogias]], [[feedback_animo_evidencia_progreso]], [[feedback_slip_map_noop_y_tipo_retorno]].
  - 01 (antes 09) — funciones como valores; 10 drills (bloques A/B/C/D) porque la sintaxis `(...) => ...` no estaba sólida. Tipo a la izquierda, tipo de retorno, callbacks (`fn(valor)`, `fn(fn(n))`) y factories que devuelven funciones (closures). 10/10.
  - **02 — APROBADO (5/5 + typecheck limpio).** Opcionales `?` vs por defecto `=`. Clave: con `=` el parámetro nunca es `undefined` dentro (no hace falta `??`); con `?` sí hay que contemplarlo. Resbalón cazado: resolvió el Bloque A (`=`) con la técnica `?`+`??` del Bloque B → se le hizo rehacer con `=`. Reincidente *verde ≠ tipos*: en `rango` borró el `?` al reescribir → runtime verde pero `tsc` cazó `TS2554` (`rango(10)` con `max` obligatorio); lo devolvió.
  - **03 — APROBADO (5/5 + typecheck limpio).** Rest params `...args` (recoge argumentos sueltos en un array; espejo del spread). Tras un "no entiendo" inicial, cerró los 5 solo con decisiones propias: `.join(" ")` en vez de reduce ingenuo, `Math.max(...nums)` (spread DENTRO de una función rest), guard divide-por-cero en `promedio`.
  - **04 — APROBADO (5/5 + typecheck limpio).** Alias de tipo función (`type Op = (...) => ...`): mismo alias como tipo de parámetro y de constante; alias distintos por forma (`Operacion` 2num→num / `Predicado` num→bool para `.filter` / `Transformador` str→str para `.map`); capstone `reduce(op, inicial)` (Operacion = forma del combinador). **Pregunta conceptual clave del usuario:** *"¿en qué momento `Operacion` me deja usar `+ - *`?"* → desmontado: un tipo describe la FORMA (qué entra/sale), no qué operador usas dentro; lo razonó él hasta corregir su propio modelo en vivo. *Verde pero concepto esquivado* saltó 2 veces (drill 2 sin alias, inline; drill 4 con `.toUpperCase()` hardcodeado + shadowing de `t`), corregidas ambas. Patrón de la sesión: "no entiendo" → ejemplo paralelo en OTRA función → cierra los drills solo.
  - **05 — APROBADO (5/5 + typecheck limpio, 23 jun 2026).** `void` = retorno que no se recoge; callbacks de efecto (`=> void`). Drills 1–3 ya cerrados (patrón forEach + efecto sobre lista). **Drill 4 `repetirAccion`:** lo resolvió con `Array.from({length}, (_,i)=>i).forEach(accion)` (verde) → se le hizo ver que FABRICA un array fantasma `[0,1,2]` solo para recorrerlo; reescrito (a petición suya, "resuélvelo tú") al `for` directo. Regla que quedó: `.forEach` cuando te DAN una colección; `for`/`for...of` cuando tú decides cuántas vueltas. **Drill 5 `porCadaPositivo` — REINCIDIÓ el clásico "verde ≠ tipos":** cuerpo correcto (`filter(>0).forEach`) pero dejó el tipo del callback en `=> number` (no lo cambió a `=> void`) → runtime 5/5 verde, typecheck ROJO (TS2345, el error sale en el TEST línea 50 pero la causa está en SU firma). Es el REVERSO de la regla void enseñada en el archivo (`=> number` exige retorno, no acepta un callback de efecto). Lo arregló solo tras señalárselo. **Sesión muy fuerte en preguntas de mecanismo (registrar — es su modo natural y le rinde):** (a) pidió desglose de la sintaxis del `for` (3 partes con timing distinto: init 1 vez / condición antes de cada vuelta / `i++` al final) — traza a mano con `veces=3`, y por qué `<` (no `<=`) da N vueltas desde 0. (b) Confusión MUY buena destrabada: creía que el `i` del tipo `accion: (i: number) => void` "recibía" el `i` del `for` → se desenredó que hay TRES `i` que solo comparten letra (① contador del `for` = el que se ENVÍA / ② parámetro del callback = el que RECIBE / ③ nombre en el tipo = etiqueta decorativa); se renombraron a `contador`/`valor`/`indice` para probar que son independientes. Distinción enviar (`accion(contador)`) vs recibir (`(valor) => …`). (c) "¿renombrar `valor` a `texto` rompe algo?" → no (nombre local y libre), PERO sería un nombre que MIENTE (el contenido es number; el tipo manda, `.toUpperCase()` daría error) → regla: el nombre es libre pero debe describir lo que contiene. (d) **`.map` en vez de `.forEach`:** correcto que funcionaría, pero map es para COSECHAR el retorno; con callback `=> void` cosecha `[undefined,…]` que tiras = trabajo desperdiciado + miente la intención (code smell, linters lo marcan). Añadida la 3ª pata a su analogía: map=FÁBRICA, filter=CONTROL DE CALIDAD, **forEach=operario que hace algo y no entrega nada**. (e) "¿ese array basura se elimina o queda en memoria?" → se enseñó GC: basura transitoria (inalcanzable tras la línea → el GC la barre sola) ≠ fuga de memoria (algo referenciado por accidente que nunca se suelta); el motivo para evitar `.map` aquí NO es miedo a fuga (no la hay) sino desperdicio + claridad; el coste real solo pesa en hot paths (allocation churn). **VOCAB WIN:** usó "retorna"/"no retorna nada" correcto por su cuenta (la palabra que arrastraba mal era "imprime").
  - **06 (callbacks tipados a fondo) — APROBADO (5/5 + typecheck limpio, 25 jun 2026). CIERRA EL BLOQUE de callbacks de funciones.** Sesión DE DESBLOQUEO conceptual, no de velocidad. **Drill 1 `mapConIndice`:** firma `fn:(item,i)` + `items.map(fn)`. Cerró bien, pero EN MEDIO lo "arregló" rompiéndolo: **invirtió el orden a `(i:number, item:string)`** → se enseñó que `.map` tiene contrato FIJO `(elemento, índice)` y TS empareja **por posición, no por nombre**, así que el orden invertido es una **mentira de tipos que NINGUNA capa caza** (runtime verde por casualidad + typecheck verde porque `i+1`/templates son tolerantes); el peligro real: con esa firma TS te dejaría hacer `item.toUpperCase()` sobre el índice number → crash. Lo devolvió al orden correcto. **Drill 2 `transformarA` SOLO y perfecto** (retorno `string[]` + `nums.map(fn)`, tipos verdaderos). **Drill 3 `aplicarSegun` — lo resolvió el coach a petición suya** ("resuélvelo para entender el cuerpo"), como ejemplo guiado comentado: `cond ? siVerdad(n) : siFalso(n)`. **AQUÍ VINO EL CLICK GRANDE de toda la sesión.** Antes del 3 chocó con un muro real ("este tipo de funciones no me dejan avanzar, NO LAS ENTIENDO") → con AskUserQuestion señaló que la niebla era **"una función recibe OTRA función"** (el concepto callback de raíz). Se reconstruyó desde el suelo con la analogía **una función es un VALOR como un número** (pasas `echarSal` igual que pasas `25`) + **ayudante de cocina** (le entregas datos + receta; él la corre por ti). El click lo selló ÉL con una pregunta suya buenísima en el drill 3: *"si `cond` es true, ¿por qué está el `(n)=>n-1` al final?"* → se desmontó **ENTREGAR ≠ EJECUTAR**: le pasas las DOS recetas siempre (la firma exige los 4 args), pero el cuerpo solo LLAMA a una según el boolean; la otra se entrega y no se cocina. Lo reformuló él solo: *"aaaah, es como echarSal/noEcharSal, la función elige según el boolean"* → de "no entiendo" a explicarlo con su propia analogía. **REGLA DE ORO que quedó: una función recibida como parámetro se usa LLAMÁNDOLA `nombre(dato)`; los `()` son el gatillo.** Reusar la analogía echarSal + ayudante de cocina en FASE 2 (props que son funciones, `onClick`). **Drill 4 `primerQueCumple` — A MEDIAS, REINCIDIÓ el slip del tipo de retorno (pre-avisado en el mismo turno):** cuerpo PERFECTO `nums.find(cumple)` (aplicó solo el concepto recién desbloqueado: pasar el callback a `.find`), pero dejó el retorno en `: number` → `.find` da `number | undefined` → **TS2322 con test VERDE** (el "verde ≠ tipos" del día, en estado puro). **Lo cerró SOLO entre sesiones (25 jun):** el archivo volvió con `: number | undefined` bien puesto SIN que se le señalara — el slip que perseguíamos desde el 05 corregido por iniciativa propia. **Drill 5 `mapYfiltra` (capstone map+filter encadenados) — CERRADO LIMPIO DE UNA PASADA (25 jun): QUINTO gemelo del patrón slip y el PRIMERO escrito sin NINGÚN resbalón** (`nums.map(transformar).filter(mantener)`: cero `.map(n=>n)` no-op, orden correcto map→filter, retorno `number[]` intacto), pese a que en el 09 los dos slips cayeron incluso pre-avisados → el músculo del slip por fin sosteniéndose. Pregunta de control de cierre (invertir a `filter().map()`) respondida BIEN: razonó que el filter juzgaría los valores CRUDOS `[1,2,3]` (ninguno > 15, no llegan a la multiplicación) en vez de los transformados → `[]`; aterrizó la lección de que el umbral está calibrado para la SALIDA del map. **VOCAB:** reincidió "imprime" por "retorna/devuelve" (lo reconoció riéndose); se le dio el autocheck *"¿hay un console.log? no → no se imprime"*. Ver [[feedback_slip_map_noop_y_tipo_retorno]], [[feedback_aprende_con_analogias]], [[feedback_animo_evidencia_progreso]].
  - **07 (CLOSURES con estado) — APROBADO (6/6 + typecheck limpio, 25 jun 2026).** Concepto único: `let` mutable declarado EN MEDIO (en la factory, fuera de la función devuelta) que persiste entre llamadas; analogía máquina dispensadora de tickets (cada `factory()` = comprar una máquina NUEVA con su papelito independiente). Resolvió los 5 solo, pero cada drill destapó un matiz fino: **D1 `crearContador` — cuerpo bien a la primera, pero añadió comentarios-demo FALSOS** (`crearContador() // 1 // 2 // 3`) que delataban el modelo cruzado factory-vs-inner: llamar a la FACTORY 3 veces crea 3 contadores frescos (da 1,1,1), no continúa la cuenta; además la factory devuelve una FUNCIÓN, no un number, así que el `// 1` ni puede ser cierto. Es el reincidente "comentario con resultado falso" + eco del ENTREGAR≠EJECUTAR del 06. Pregunta de control (`a();a();b();` → ¿b() da 1 o 3?) respondida PERFECTA: *"1 porque es su propia máquina con n en 0"*; corrigió el demo a `const c=...; c();c();c() // 3`. **D2 `crearAcumulador` — slip LEER≠GUARDAR:** escribió `(n) => n + total` (lee `total` pero nunca le ESCRIBE de vuelta) → `total` clavado en 0, la 1ª llamada coló de chiripa (5+0=5) y de ahí se rompió; lo arregló a `total = total + n; return total`. Regla que quedó: un closure solo recuerda lo que le ESCRIBES de vuelta (puente con `useState`). **D3 `crearAlternador` — trampa del ORDEN:** voltear-antes-de-devolver (`comienzo = !comienzo; return comienzo`) arrancando en `true` da `false,true,false`. Eligió la opción ① (arrancar en `false`) tras trazar la tabla; quedó nota de que el nombre `comienzo=false` miente un poco. Hubo una pausa "revisa de nuevo" sin cambios = no había guardado el archivo (Ctrl+S). **D4 `crearLimitado(max)` — limpio:** combinó contador mutable + valor fijo capturado (`max`), eligió bien `<=` (incrementa-luego-compara). **D5 capstone `crearContadorDesde` — MISMO slip del orden que D3, en versión number:** sumó-y-devolvió (`base=base+paso; return base`) → `15,20,25`, saltándose el `inicio`. Contraste pedagógico clave: líneas IDÉNTICAS a las de D2 pero ahí querías el valor NUEVO y aquí el VIEJO ("coja turno": entrega el actual, luego avanza) → patrón guardar-en-temp-antes-de-avanzar (`const actual = base; base += paso; return actual`). Cerró tras la traza. **Vocab/patrón a vigilar:** el "comentario con resultado falso" volvió a aparecer (D1) y la confusión factory-call vs inner-call es el filo a reforzar en FASE 2 (`useState`, handlers). Día de buen impulso: pidió cerrar los 10 archivos de la carpeta hoy. Ver [[feedback_slip_map_noop_y_tipo_retorno]], [[feedback_aprende_con_analogias]].
  - **08 (composición — `g(f(x))`) — APROBADO (5/5 + typecheck limpio, 26 jun 2026). PRIMER ejercicio del bloque sin patrón map/filter, así que el slip gemelo no tenía dónde aparecer.** Resolvió los 5 SOLO y limpios A LA PRIMERA en las dos capas: encadenar dos/tres con anidado `h(g(f(n)))` en el orden correcto, `procesar` con cambio de tipo string→number→string (incluido el tipo de retorno), `repetir2` `f(f(n))`, capstone `encadenarYaplicar` que devuelve el number (no la función). **WIN del slip "comentario con resultado falso":** sus 4 líneas-traza (`...(4) // n=4 -> 5 -> 10 -> 7`) están TODAS bien calculadas — el resbalón que arrastraba desde `03-arrays` y los closures del 07 (el `// 1 // 2 // 3` mentiroso) NO apareció. **CLICK metacognitivo grande (lo trajo él):** notó que el drill 5 le hizo entender los 4 anteriores y que "debería haber ido primero". Se le validó y se le dio el nombre: el 5 es `g(f(n))` DESNUDO (aplica ya, devuelve number) y 1–4 son ESE MISMO núcleo envuelto en `(n) =>` (fabrican una función reutilizable). Analogía **taza vs cafetera** (aplicar-ahora vs entregar-la-máquina-para-disparar-después) = eco directo del ENTREGAR≠EJECUTAR (06) y la máquina dispensadora (07): una función que devuelve otra es una fábrica. Matiz honesto sobre el orden: su instinto concreto→abstracto es didáctica legítima, pero el 5 va de último a propósito porque el capítulo está enganchado a closures (07) y la destreza entrenada es FABRICAR la función compuesta, no calcular `g(f(3))`. **Pregunta de control (¿`procesar` al revés `f(g(s))`?) respondida BIEN:** "chocan los tipos de lo que esperan ambas funciones" → se afiló a la versión precisa (meter el string en `g` que espera number rompe en la llamada interna; el encadenado va porque el TIPO DEL MEDIO, number, conecta la salida de f con la entrada de g). Ver [[feedback_animo_evidencia_progreso]], [[feedback_aprende_con_analogias]].
  - **09 (PIPE — aplicar una LISTA de funciones con `reduce`) — APROBADO (5/5 + typecheck limpio, 27 jun 2026).** Concepto: en vez de anidar `e(d(c(b(a(x)))))`, guardas los pasos en una lista `((n: number) => number)[]` y los aplicas con un `reduce` cuyo acumulador es "el dato según sale de cada paso" y cuyo inicial es la entrada. **Drill 1 `aplicarPasos` ya estaba** (el motor reduce). **Drill 2 `aplicarPasosTexto` (gemelo strings):** tradujo el tipo `((s: string) => string)[]` leyendo su propia frase en español — PERO antes hubo un alto grande en la SINTAXIS del tipo (`((n: number) => number)[]` "se me hace chino"): se desarmó por capas con escalera de sustitución (`number` → `number[]` → `(n)=>number` es un TIPO, la FORMA de una función, no una función → `(función)[]` = array de funciones), y el papel de los `()` para que el `[]` se enganche a TODA la función (contraste `(n)=>number[]` = función que devuelve array, vs `((n)=>number)[]` = array de funciones; analogía "caja de (manzanas rojas)"). Cerró con su lectura correcta: "un array de funciones que reciben string y retornan string"; matiz vocab: "lista"="array" (no "lista a modo de array"). **Drill 3 `pipe` — EL MURO de la sesión, reapareció el concepto función-que-devuelve-función ("no entiendo el drill 3").** Primero escribió la versión "taza" (reduce SUELTO ejecutándose ya, con un `0` INVENTADO como inicial, retorno `number`) en vez de la "cafetera" (DEVOLVER una función). Se destrabó EXACTO como el patrón que le funciona: UN ejemplo paralelo simple SIN reduce (`multiplicador(factor) => (n) => n*factor`, `triple = multiplicador(3)`) + tabla recibe/devuelve/cuándo-llega-el-dato + analogía cafetera (entregar la MÁQUINA apagada; el grano `n` lo pone quien la enciende, después). Lo cerró solo: `return (n) => pasos.reduce(..., n)`, tipo de retorno `(n: number) => number`. Eco directo de 06 (ENTREGAR≠EJECUTAR), 07 (máquina dispensadora) y 08 (taza vs cafetera). **Naming cazado:** nombró la variable-resultado `pasos` (MIENTE: guarda la máquina/tubería, no los pasos, que son lo que PASÓ a `pipe`; además choca con el parámetro `pasos`) → renombró a `tuberia` + `let`→`const` (nunca reasigna). **Drill 4 `pipeTexto` (gemelo strings de 3) — SOLO, sin ayuda.** **Drill 5 `procesarCada` (capstone) — `.map` + reduce inline A LA PRIMERA** (`nums.map((n) => pasos.reduce(..., n))`). **REINCIDIÓ "comentario con resultado falso" DOS veces, lo cazó/corrigió ambas:** (a) drill 1 dejó `aplicarPasos(5, ...) // = 7` (copió el 7 del ejemplo con n=3) → recalculó SOLO `((5+1)*2)-1 = 11` y reescribió la traza paso a paso completa (buen hábito); (b) drill 4 `// "HOLA,BUENOS Días"` con DOS errores — además del slip, una MISCONCEPTION de `.trim()`: creía que quita espacios INTERNOS → se enseñó que trim solo limpia los EXTREMOS (el espacio de la coma queda) y que `.toUpperCase()` va ENTERO ("DÍAS", no "Días"); corrigió a `"HOLA, BUENOS DÍAS"`. **WIN grande: construyó su PROPIA analogía — el TÚNEL DE LAVADO automático** (coche=dato, estaciones=pasos en orden, salida de una=entrada de la siguiente; `pipe` = MONTAR el túnel vacío sin coche todavía → por eso devuelve una función; `procesarCada` = una FILA de coches por el mismo túnel = el `.map`). Internalización real, no recitado; se reusará en FASE 2. Ver [[feedback_slip_map_noop_y_tipo_retorno]], [[feedback_aprende_con_analogias]], [[feedback_animo_evidencia_progreso]].
- `03-arrays/` — **01 APROBADO** (antes 10). Arrays + callbacks (map/filter/reduce), 9 drills en escalera (A=map / B=filter / C=reduce / D=higher-order). Refuerza arrays tipados (`number[]`), transformar/seleccionar/combinar, recibir callbacks (`nums.map(fn)`) y encadenar (`.filter(...).length`). 9/9 tests.
  - **03 (refuerzo `reduce`, jun 2026) — COMPLETO. Drills 1–7 APROBADOS; 8–10 RESUELTOS por el usuario (8 `todosPositivos`, 9 `sumaDeCuadrados`, 10 `contarParesImpares` capstone acum objeto) — falta solo revisión de coach del razonamiento de 8–10, el código está correcto.** 10 drills aislando `.reduce` (alcancía): número → cambio de tipo → booleano → objeto. 1–3 ya estaban. 4 `maximo`: cazó que faltaba el inicial `-Infinity` (no `0`, que cuela un fantasma; no `Infinity`, que gana siempre) — entendió que `Infinity` es un "tope garantizado", no un número a memorizar; sin inicial `reduce` toma el 1er elemento y revienta en `[]`. 5 `concatenar` (neutro `""`) y 6 `longitudTotal` (`string[]`→`number`, cambio de tipo con `valor.length`): leyó bien acum/valor/inicial. 7 `contarVerdaderos` (`acum + (valor ? 1 : 0)`): dedujo SOLO que contar falsos sería `!valor`; se le corrigió vocabulario (el ternario evalúa el VALOR/veracidad del booleano, no "si existe"; `if(valor)` = truthiness, ≠ `=== true`). **Retomar por el drill 8 `todosPositivos`** (acum `boolean`, inicial `true` como neutro de `&&`, `acum && valor > 0`); luego 9 `sumaDeCuadrados` (cuadrado dentro del reduce) y 10 `contarParesImpares` (capstone: acum OBJETO, `...acum`).
- `04-objetos/` — **CARPETA CERRADA (01–10, 19 jul 2026).** Con el capstone 10 queda COMPLETADO el entrenamiento de TypeScript puro → siguiente hito: FASE 2 (React + TS).
  - 01 (antes 06) — objetos: shapes, opcionales y `readonly` por propiedad.
  - **02 (refuerzo, jun 2026) — APROBADO (7/7).** Actualización INMUTABLE con spread `{ ...obj, prop: ... }`. Surgió porque no entendía la línea `{ ...acum, pares: acum.pares + 1 }` del capstone de `03-arrays/exercise-03` (drill 10). 7 drills en escalera (A=copiar/pisar, B=cambio booleano y calcular-desde-lo-que-había, C=capstone reduce con acum objeto tocando 2 props). Aprendizajes que se consolidaron: (a) el spread copia y la propiedad escrita DESPUÉS pisa (orden manda); (b) `obj.prop + 1` LEE, no muta; (c) drill 6 `aplicarIva`: error conceptual `* iva` (solo el impuesto, 190) → `* (1 + iva)` (precio+impuesto, 1190); puente con el neutro `1` de multiplicar; (d) el `...acum` es NECESARIO solo si dejas propiedades sin tocar — en `resumirCompra` reescribe las 2 props, así que sobra (decorativo), a diferencia de `contarParesImpares` donde sí hacía falta; (e) las dos formas de retornar objeto en arrow: `=> ({...})` (paréntesis = "esto es un valor objeto", evita que `{` se lea como cuerpo/bloque) vs `=> { return {...} }`. Vocabulario afinado: propiedades (de objeto) ≠ parámetros (de función); "retornar" lo hace la función, "tener propiedades" el objeto.
  - **03 (refuerzo, jun 2026) — APROBADO (8/8 tests, tipos limpios).** Actualización inmutable ANIDADA: objeto dentro de array (`.map`+ternario) y dentro de diccionario (`{ ...reg, [id]: { ...u, ... } }`, spread shallow + llave calculada `[id]` + guard del `noUncheckedIndexedAccess` que vuelve `reg[id]` = `Usuario | undefined`). 7 drills: D1 (1–3 arrays), D2 (4–5 diccionario con guard), D3 (6–7 capstone carrito, el 7 combina `.map` para bajar cantidad + `.filter` para eliminar el que llega a 0). Aprendizajes que costaron: (a) **D2 disparó la trampa esperada** — aplicó `.map` + `r.id` sobre un diccionario (que no es array y donde el id es la LLAVE, no una propiedad del usuario); se corrigió con la analogía casillero-con-etiquetas vs fila-numerada. (b) **El `[id]` (llave calculada) fue el muro real**: dijo "sigo sin entender" varias veces; se destrabó con escalera de sustitución concreta (`{ [id]: true }` con `id="carrito9"` → `{ carrito9: true }`; luego nombre repetido → "gana el último"; luego con `...reg`). Frase que quedó: "los corchetes usan el VALOR de la variable como NOMBRE de la propiedad, no el texto literal" (ojo: primero lo dijo al revés, "cambia valor no nombre" → corregido). (c) confundió los dos trabajos del `:` (tipo `edad: number` vs valor `edad: 5`) → aclarado por "¿qué hay a la derecha, un tipo o un valor?". (d) D3·7: intentó meter el `.filter` DENTRO del `.map` ("sobre el return del spread") → se enseñó que son DOS estaciones encadenadas `arr.map(...).filter(...)`, no anidadas (`.map` transforma, no elimina; `.filter` decide quién sobrevive). Nota: 4 errores TS2532 estaban en el `.test.ts` de andamiaje (acceso `arr[0].x` sin `?.`), no en su solución — corregidos a `arr[0]?.x`.
  - **04 (refuerzo `.map`+`.filter` encadenados, jun 2026) — APROBADO COMPLETO (10/10 drills, 10/10 tests, typecheck limpio).** Era el "Bloque E" dentro de exercise-03; a petición del usuario se MOVIÓ a archivo propio (`exercise-04.ts` + test) y se amplió de 6 a 10 drills — le resultaba engorroso subir a leer los tipos al inicio del archivo, así que ahora CADA TIPO está definido justo encima del bloque que lo usa (mantener ese formato en futuros refuerzos largos). Escalera: A (1–3) aísla `.filter`, B (4–5) aísla `.map`, C (6–10) encadenan.
    - **Hitos de la sesión (12 jun 2026), drills 1–8:** (a) 1–3 limpios y rápidos (1 booleano tal cual sin `=== true`; 3 con `!` evitando `=== false` por decisión propia — se le enseñó el matiz: con `boolean` estricto son equivalentes, con `boolean | undefined` no). (b) Pregunta suya valiosa en el 2: "¿stock 0 o null quedan fuera?" → se separó: el 0 sí por `> 0`; el null NI LLEGA (contrato `stock: number`); y preguntó si entonces "había que" añadir `| null` → NO: **el tipo se elige por evidencia, no por miedo** (`| null` solo si el dato real puede traerlo; si no, es trabajo falso defendiéndose de fantasmas — conectado con su regla de F5 "pon `?.` solo donde puede ser null"). (c) En el 4 preguntó por qué el `return` va entre `{}` → se enseñó a fondo los DOS MODOS de la arrow: **VENTANILLA** (expresión: entrega directa, sin return) vs **OFICINA** (bloque `{`: varios trámites, nada sale sin `return` explícito), y los DOS TRABAJOS del `{` (crear objeto / abrir cuerpo) — tras `=>` JS asume SIEMPRE cuerpo, el `(` lo desambigua ("encapsula/protege un valor", frase suya). En el quiz de 3 etiquetas falló justo la `c)` `({...})` → se destrabó con "¿dónde está el return si fuera cuerpo?" + regla mecánica "mira lo PRIMERO tras la flecha". Reescribió el 4 de oficina a ventanilla por iniciativa propia. (d) VOCABULARIO corregido (vigilar, reincide): "map recorre cada PROPIEDAD del array" → los arrays tienen ELEMENTOS, los objetos PROPIEDADES, map recibe el elemento ENTERO; y "se imprime" por "se devuelve/transforma" (conectado con su `imprimir: void` de D3). También "se filtra solo EL que cumpla" (singular) → TODOS los que cumplan. (e) **Analogía que ÉL construyó y quedó como canon del bloque: map = FÁBRICA (salen todos, algunos defectuosos), filter = CONTROL DE CALIDAD (no fabrica ni repara, decide qué llega al cliente; se para a la SALIDA porque los defectos no existen antes de fabricar)** — nació de corregirle el "colador 2 veces" (map NO es un filtro ni atrapa nada). (f) Pregunta suya de las dos capas en el 6: ¿rebajar 50 a un precio de 40 da error? → NINGUNA capa protesta: -10 es `number` válido (TS vigila FORMAS) y JS resta feliz; el -10 vive ENTRE estaciones y muere en el filter — la regla de negocio la vigila el código, no el compilador. (g) 7 perfecto a la primera con narración previa del flujo. (h) El 8 "lo pilló" → pista = su propio `quitarUnaUnidad` + esqueleto con huecos; lo llenó bien. Cambió `Enemigo.id` a `number` por su cuenta (coherente: tipo+firma+ejemplos) → se ACEPTÓ y se sincronizó el test (lección F3 repetida: cambiar un tipo obliga a sincronizar el test; los tests runtime seguían verdes porque JS no mira tipos). (i) **PATRÓN A VIGILAR (costó 3 revisiones):** en sus comentarios de ejemplos escribe el resultado INCOMPLETO — omite los elementos que el ternario no toca (`// → [{...vida:7}]` con 3 enemigos entrando) y los números a ojo (10-11 le salió "0", luego "-10", al fin "-1"; se descartó el modelo videojuego de vida clavada en 0: la resta no tiene piso, el clamp sería `Math.max(0,...)` explícito). Se le dijo: un comentario con resultado falso es peor que no tener comentario. Sus 3 ejemplos finales del 8 cubren golpe normal / id inexistente / golpe letal. **PENDIENTE al retomar: verificar que corrigió el comentario de la línea ~161 (ejemplo 1 del drill 8, debe listar los 3 supervivientes).**
    - **Cierre del bloque (12 jun 2026), drills 9–10:** (a) El 9 (`asequiblesConEnvio`) salió BIEN A LA PRIMERA en código (map suma envío, filter `<=`, ventanilla), y razonó bien de palabra el caso límite. (b) **PATRÓN COMENTARIOS REINCIDIÓ FUERTE — vigilar en próximos bloques:** el comentario pendiente de la línea ~161 (resultado incompleto del drill 8) necesitó TRES recordatorios en la sesión y al final NO lo corrigió él — pidió "haz los cambios tú" y los hizo el coach (aceptado por ser comentarios, no código de drill). Además: al cambiar los números de su ejemplo dejó un `$810000` fantasma desincronizado, y reincidió 2 veces en "el presupuesto es MAYOR" tras corregirle que `<=` incluye IGUAL (frase correcta: "el precio final NO SUPERA el presupuesto"). El coach dejó escrita la pareja de ejemplos del caso límite (presupuesto 810 → sobrevive con `<=`; 809 → se cae) — releerla si vuelve a confundir `<` con `<=`. (c) El 10 (capstone `depurarCarrito`) PERFECTO a la primera: map sin ternario + spread + filter, sin ayuda. (d) Pregunta de control de cierre (orden invertido filter→map en el 10) respondida BIEN: vio que saldría el item zombi con `cantidad: 0` porque el portero miraría la cantidad VIEJA — la regla del bloque (el filter juzga lo que la fábrica acaba de crear → va después) quedó consolidada.
  - **05 (`Object.keys` / `values` / `entries`) — COMPLETO (12/12 tests + typecheck + lint limpios, 12 jul 2026).** Drills 1–5 ya estaban. **Se AUTORÓ escalera R (R1–R7) a petición suya: el nudo era `.map(([clave, valor]) => ...)` — "paréntesis → corchete → paréntesis me enreda".** Se desmontó en 3 CAPAS (paréntesis de la LLAMADA a map / lista de PARÁMETROS de la flecha / PATRÓN de desestructuración) con la clave: **la flecha recibe UN solo parámetro; el corchete no añade otro, ABRE el que ya hay** (analogía: un paquete de Correos con dos cosas dentro). R1–R3 tupla por índices sin flechas; R4 EL SALTO (mismo cuerpo, patrón en posición de parámetro); R5–R6 dentro de map; R7 capstone entries+sort+map+join. Aprendizajes: (a) **tupla ≠ array** — `entrada[0]` NO da `| undefined` pese a `noUncheckedIndexedAccess` (longitud fija y conocida), a diferencia de `xs[0]`; por eso `Object.entries` devuelve `[string, number][]`. (b) El **hueco** `([, valor])` para saltarse una pieza (la coma sola que abre el corchete) — en R6 lo ESQUIVÓ volviendo a índices (correcto y limpio, pero rodeó justo lo que el drill enseñaba; se le señaló sin forzar). (c) **REINCIDIÓ el error de sintaxis que mata el parseo**: dejó un `return Object.` a medias → vitest dijo "no tests" (no "R6 falla") y murió el archivo entero con sus 12 tests. Tercera vez (eco del `=> )` de 01/05 y el `{ data.host }` de 01/07). (d) En R7 se fue a **point-free** (`.map(formatearConIndices)`) por iniciativa propia — se le enseñó el footgun de `["1","2","3"].map(parseInt)` → `[1, NaN, NaN]` (map pasa 3 args) y por qué el `sort` sin copia es seguro aquí (`Object.entries` ya fabrica array nuevo). **VOCABULARIO (pregunta suya): clave + valor JUNTOS = PROPIEDAD; el valor no es "de la clave" sino de la propiedad; ENTRADA (entry) = el par como tupla.** Ver [[project_05_diccionario_vocabulario_ts]].
  - **06 (diccionarios `Record<string, number>`) — EN CURSO (12 jul 2026). Drill 1 `contar` hecho; 2–5 pendientes. Escalera R (R1–R11) MONTADA y sin resolver.** Dijo "el drill 1 me cuesta entenderlo" señalando `({ ...acumulador, [palabra]: (acumulador[palabra] ?? 0) + 1 }), {})`. **Se AUTORÓ escalera R aislando los CINCO mecanismos apilados en esa línea:** spread / llave calculada / leer con `?? 0` / los paréntesis de la flecha / el acumulador inicial. R1–R3 copiar-añadir-pisar; **R4–R5 la LLAVE CALCULADA** (`{ [clave]: v }` vs `{ clave: v }` — ojo: es el corchete OPUESTO al del ejercicio 05, aquel ABRE una tupla, este CALCULA un nombre); R6–R7 leer con respaldo y **R7 `sumarUno` = UNA vuelta del reduce escrita a mano** (el peldaño clave); **R8 la trampa del paréntesis** (`=> { valor: n }` es un BLOQUE y devuelve `undefined`; `=> ({ valor: n })` devuelve el objeto) — se le pide escribir la versión MALA a propósito y mirar el `[undefined, undefined]`; R9–R10 ya con reduce (R9 reconstruye el drill 1); R11 reduce-campeón, prepara el drill 5 `masFrecuente`. Protocolo verificado: soluciones typecheck (strict + `noUncheckedIndexedAccess`) + runtime OK → cuerpos vaciados → 15 rojos por diseño (11 nuevos + los 4 drills originales). Nota: la llave calculada YA fue muro en `04-objetos/exercise-03` ("sigo sin entender") — es su segunda pasada por el mismo concepto. Ver [[feedback_drills_en_escalera]].
  - **07 (transformar VALORES — `entries → map → fromEntries`) — COMPLETO (19/19 tests + typecheck limpio, 14 jul 2026). CERRÓ el bloque que abrió diciendo "todo lo que queda es lo que más me ha costado y aún me cuesta".** Abrió respondiendo "TODO" al AskUserQuestion de dónde se le hacía niebla el viaje. **Se AUTORÓ escalera E (E1–E14) que desarma el viaje en mecanismos de un paso:** E-A mirar los pares (`entries` devuelve array de arrays), E-B abrir UN par (índice `par[0]`/`par[1]` → E7 ⭐ el SALTO al desempaquetado en el parámetro `([clave, valor]) =>`, con test que compara E6==E7 para probar que son la MISMA función con distinta puerta), E-C recorrer con `.map` (listas planas), E-D montar con `fromEntries` desnudo, E-E el viaje completo (E12 clonar = prueba de que entries/fromEntries son inversas; E13 en 3 líneas con **la trampa del `any` silencioso** si no anotas `const pintados: [string, number][]` — `fromEntries` traga el `(string|number)[][]` por una sobrecarga-escotilla y devuelve `any` SIN gritar: aquí TS CALLA, no grita; E14 el drill 1 reconstruido). Protocolo verificado: solución 19/19 + typecheck limpio → cuerpos vaciados → rojos por diseño. **Resolvió E1–E14 + drills 1–5 SOLO.** Hitos: (a) **el escalón difícil (E7, desempaquetar en el parámetro) — cerrado**; escribió `${clave}=${valor}` usando los nombres directos, el `([clave, valor]) =>` dejó de ser jeroglífico. (b) **Transferencia real, sin pedírsela:** la **casilla vacía** `([, valor])` aprendida en E9 la reusó sola en el drill 3 (`filter`); y `.map(describirParBis)` **point-free** (ENTREGAR≠EJECUTAR, la cafetera del 02 / `onClick={handleClick}`) en E10. (c) **drill 3 `soloPositivos` — eligió bien `.filter` (descartar) vs `.map` (transformar)**; drill 4 `valoresATexto` — **el cambio de tipo del valor number→string (su slip histórico) NO cayó**, firma `Record<string,string>` respetada; drill 5 capstone `aplicarDescuento` limpio. **REINCIDENTE cazado — "comentario con resultado falso":** en E3 la traza decía `["b", 10]` (copió el 2º par) cuando `primerPar` da el 1º `["a", 5]`; corregido. **PATRÓN NUEVO de gran valor — la DIRECCIÓN de `fromEntries` en los comentarios, invertida 3 VECES (drills 3/4/5):** describió `fromEntries` como "recibe objeto, devuelve lista" (que es lo que hace `entries`, su INVERSA). Diagnóstico confirmado con su propia mano: en **E13** (código escrito en orden de EJECUCIÓN, entries arriba/fromEntries abajo) escribió la traza de `fromEntries` CORRECTA (lista→objeto); en drills 3/4/5 (`fromEntries` ENVOLVIENDO por fuera, escrito arriba) la invirtió. **No confunde qué hace la función — sigue el orden en que LEE en vez del orden en que se EJECUTA. Es su "leer ≠ ejecutar".** Regla que quedó para 08/09: cuando `fromEntries` envuelve por fuera, anotar de DENTRO afuera (empezar por el `entries` del centro y subir). Ver [[feedback_slip_map_noop_y_tipo_retorno]], [[feedback_trace_lines_comentadas]], [[feedback_drills_en_escalera]], [[feedback_animo_evidencia_progreso]].
  - **08 (AGRUPAR una lista — `groupBy`) — CERRADO (17/17 tests + typecheck limpio, 18 jul 2026). REHECHO DESDE CERO a petición del usuario.** El archivo venía enmarañado de idas y vueltas (const-drills que él rechazó: quería FUNCIONES en escala, no constantes). Se reconstruyó como **escalera de 17 drills granular**, conservando los mismos ejercicios pero partiendo cada salto en peldaños de 1–3 líneas, con protocolo verificado (solución 17/17 + typecheck → cuerpos vaciados → 17 rojos). BLOQUE 4 baja EL MURO en rampa: **meter UN elemento (5/6) → `meterDosAMano` arrastre a mano (7) → `for` (8) → `reduce` (9)**; luego clave calculada (`paridad`→`agruparPorParidad` 10/11), **groupBy con objetos genérico (12 ⭐)**, contar (13), Set (14/15), reduce-campeón (16/17). **ARCO EMOCIONAL de la sesión (18 jul):** abrió con el `for` roto en el editor y diciendo *"R1 es re complicado"* y *"esto es una locura"* (por la composición función-sobre-función); cerró **resolviendo SOLO** reduce, groupBy genérico, count, Set y campeón. Se le reencuadró la composición como lo contrario del caos (cajitas de 1 línea, nunca todas en la cabeza a la vez; analogía cocinar-con-ingredientes-ya-hechos) y con evidencia dura de su propio avance. Hitos técnicos: (a) **drill 6 `meterEn` genérico — vivió "test verde ≠ tipos" otra vez**: reusó `agregarEnCasillero` (fijada a `number`) en un genérico `<T>` → runtime verde, typecheck rojo (`TS2322`/`TS2345`); lo cerró inline-ando la expresión con `T`. (b) **cuándo el `reduce<...>` sobra vs hace falta**: en 11 la anotación era redundante porque el **tipo de retorno de la función** da contexto; en 16 SÍ hace falta porque la semilla es `undefined` y el `?.[0]` corta el contexto → `reduce<[string, number] | undefined>`. Entendió el *porqué*, no lo puso a ciegas. (c) **preguntas suyas de mecanismo, señal de que desarma en vez de copiar:** de dónde sale el `:` de `{ Zapatos: 2 }` (está en su `[producto.categoria]:`, el `[...]` calcula la clave, el `:` no se mueve); para qué el spread de `[...new Set(xs)]` (Set no es array → el `[...]` traduce); si `{Zapatos:2}` era el return exacto. (d) **el "segundo spread" = acumular vs pisar** costó (dijo "no se aclaró") → se bajó a mecánica pura de `...` sobre arrays diminutos (`[...viejo,2]` vs `[viejo,2]` vs `[2]`). (e) resolvió 14 con `reduce`+`includes` (válido) antes de que se le enseñara `[...new Set()]` (O(n) vs su O(n²)); y usó `const` vs `let` según reasignación. **PREFERENCIA NUEVA:** dejar un recordatorio comentado del `type` al lado del drill que lo usa (no subir al inicio del archivo) → ver [[feedback_type_reminder_local]]. Ver [[project_ts_mindset_shift]], [[feedback_animo_evidencia_progreso]], [[feedback_slip_map_noop_y_tipo_retorno]], [[feedback_drills_en_escalera]].
  - **09 (INDEXAR por id — `Record<number, T>`) — CERRADO (8/8 tests + typecheck limpio, 19 jul 2026). Resuelto SOLO.** Escalera de 8 drills: `ponerItem`→`indexarDosAMano`(arrastre)→`indexarPorId`(reduce)→`buscar`/`nombrePorId`(guard `?.`/`??`)→`renombrarItem`→`actualizarNombre`(⭐ guard+componer piezas)→`nombresDe`(capstone). Drills 1–6 ya venían resueltos (con sus trazas y comentarios didácticos); 7–8 en esta sesión. **Drill 7 ⭐ `actualizarNombre` — código PERFECTO a la primera** (`const item = indice[id]` + guard `if (item===undefined) return indice` con narrowing + `ponerItem(indice, renombrarItem(item, nombre))`, sin spread anidado). **El único fallo fue el COMENTARIO de traza (reincidente "comentario con resultado falso"):** en la llamada con id inexistente (9) escribió `{9:{id:9,nombre:"z"}}` — inventó un item nuevo, lo CONTRARIO de su propio guard (que devuelve el índice IGUAL). Diagnóstico que quedó: el código demuestra que el mecanismo lo tiene; el slip es de TRAZADO (leer≠ejecutar), no de comprensión. Coach corrigió los comentarios a petición suya. **Dos preguntas de mecanismo suyas, muy buenas:** (a) qué accede `indice[id]` → se destrabó la trampa de los DOS `id` que aquí valen 1 (la CLAVE del casillero vs la PROPIEDAD `id` del item); `indice[id]` es lookup directo por clave, NO busca por campo (contraste con `find`). (b) qué hace falta para caer en `return indice` → entendió que depende de que el id NO sea clave del record (da igual el nombre). **ANALOGÍA PROPIA, canon del cierre: "el id es la CERRADURA de la chapa"** — aunque solo cambie el nombre, el id identifica/localiza el casillero y es INMUTABLE; por eso el item renombrado (conserva su id) vuelve al MISMO casillero y PISA al viejo, no crea uno huérfano. Lo conectó solo con la identidad inmutable / clave primaria de BBDD. **Drill 8 `nombresDe` (capstone) — QUINTO gemelo del patrón filter/map+extraer-campo, y los TRES slips históricos NO aparecieron ni uno** (`.map(x=>x)` no-op / tipo de retorno viejo / comentario falso): eligió `.map` (lista→lista), reusó `nombrePorId`, firma `string[]` directa, traza honesta, a la primera. El músculo del slip, que en el 08 aún cedía a medias, aquí aguantó entero. Ver [[feedback_slip_map_noop_y_tipo_retorno]], [[feedback_trace_lines_comentadas]], [[feedback_aprende_con_analogias]], [[project_ts_mindset_shift]], [[feedback_animo_evidencia_progreso]].
  - **10 (CAPSTONE analítica de ventas) — CERRADO (10/10 tests + typecheck limpio, 19 jul 2026). Resuelto SOLO, los 10 drills, sin pedir una sola pista.** Escalera: `montos`/`totalGeneral`→`sumarEnCategoria`(una vuelta)→`totalPorCategoria`(reduce)→`categorias`(Set)→`campeonDeTotales`/`categoriaTop`→reporte paso a paso (`ordenarPorTotalDesc`→`lineaDe`→`reporte`). **CIERRA la carpeta y el TS puro.** Hitos: (a) **el `?? 0` del drill 3 lo puso solo y supo explicarlo** — con `noUncheckedIndexedAccess` no es opcional, `acumulador[categoria]` es `number | undefined` y sin `??` no compila. (b) **Drill 2 `totalGeneral`: NO reusó `montos` como pedía el enunciado**, redujo directo sobre `ventas` — su versión es correcta y de UNA pasada (mejor que la pedida); preguntado si era criterio o despiste, respondió **despiste de lectura del enunciado** (patrón a vigilar: leer el enunciado ENTERO, no la primera línea). Se dejó su versión, no se empeoró el código para cumplir la consigna. (c) **REINCIDIÓ el "comentario con resultado falso"** en el drill 10: pegó en `totalPorCategoria(ventas)` la traza de `ordenarPorTotalDesc` (copy-paste del drill 8) — `totalPorCategoria` recibe `Venta[]` y devuelve `{ A: 15, B: 20 }`, nunca ve pares. Mientras editaba dejó además `{ Acc: 15, B: 20 }` con claves desincronizadas de su propia salida. **Regla que quedó: al trazar, COPIAR la clave literal, no una parecida.** (d) **CRÍTICA SUYA MUY BUENA, aceptada:** la primera corrección del coach metía comentarios de 100+ chars colgando de una llamada anidada; dijo *"el drill apenas se puede leer, se ve muy desordenado"* — tenía razón. Se le ofrecieron 3 formatos rankeados y **eligió pasos intermedios con nombre** (`totales`/`ordenados`/`lineas`), que además de legible le da NOMBRE a cada valor intermedio (refuerza el vocabulario pendiente, ver [[project_05_diccionario_vocabulario_ts]]). **Lección de fondo del cierre: el código va perfecto, el slip vivo es el TRAZADO (leer≠ejecutar) — mismo diagnóstico que en el 09.** PENDIENTE menor: partir la línea de ~180 chars de `campeonDeTotales`. Ver [[feedback_trace_lines_comentadas]], [[feedback_jerarquia_opciones_rankeada]], [[feedback_comentarios_didacticos]].
- `05-unions-narrowing/` — **CARPETA COMPLETA (10/10 archivos, 75/75 tests + `tsc -b` limpio, 4 jul 2026).** 01, 02 y 03 APROBADOS de antes (03 completo, 29/29 tests): 01 (antes 05) tipos literales y unions de literales; 02 (antes 07) narrowing con `typeof` y `undefined`; **03 (refuerzo, jun 2026)** narrowing con objetos y `null` (surgió en el parcial B3). **04–10 RESUELTOS por el usuario en una sola sesión larga (4 jul 2026), coach Concepto→Ejemplo→drill, todo runtime + `tsc -b`.**
  - **A–D (APROBADOS):** trampa `typeof null === "object"` (descartar null por valor / truthiness, NO con `typeof`) + operador `in` para distinguir objetos sin tag (escalera C/D con 1→3 variantes, campos compartidos, capstone null+`in`).
  - **BLOQUE E (APROBADO, E1–E6):** el `null` en el borde de una API REAL, enmarcado en los dos proyectos del usuario (portfolio + e-commerce junior). Enseñó por escrito `?.` (optional chaining) y `??` (nullish coalescing) por primera vez. Drills: recurso null (404), campo opcional null, descuento null, `?.`+`??` en una línea, normalizar carrito null a `[]`, capstone con formato `$`. Aprendizajes clave que costaron: `throw` vs `return` (un valor de respaldo se DEVUELVE, no se lanza — lo confundió 2 veces); `?? []` NO produce `[undefined]` sino `[]` (0 elementos); reuso DRY de `precioFinal` en el capstone (criterio sobre su nivel).
  - **BLOQUE F (APROBADO, F1–F6):** sintaxis fina de `?.`/`??`. F1 encadenar `?.` dos niveles (lee bien la posición del `?.` con la regla mecánica "¿lo de la IZQUIERDA del punto puede ser null?"). F2 trampa del `0` (`??` vs `||`, preserva el `0` legítimo). F3 forma `?.[]` (índice opcional): aquí su `if (length===0)` chocó con `noUncheckedIndexedAccess` (TS no conecta el chequeo de `length` con `[0]`, sigue `string | undefined`) → la idiomática `fotos?.[0] ?? fallback` resuelve los 3 casos y satisface a TS. Cambió `placeholder.png`→`.webp` por realismo; se actualizó el test y el enunciado a `.webp` (lección: cambiar un valor obliga a sincronizar el test que lo verifica). F4 forma `?.()` (llamada opcional): TENÍA EL MODELO MENTAL CRUZADO — creía que accedía al DOM/evento `onClick`; se corrigió a fondo: `b` es un objeto plano, `onClick` una propiedad que guarda una FUNCIÓN (un valor), `b.onClick` la lee y `()` la ejecuta; puente con `props.onClick?.()` de React. F5 encadenar `?.`+índice: error conceptual valioso — puso el `?? "vacío"` A MITAD de la cadena (`u.carrito?.items ?? "vacío"`) creando un tipo mezcla `array | string`; se enseñó que el `??` va UNA vez AL FINAL (analogía tobogán+colchón). Y CAZÓ SOLO un `?.` de más en `items?.[0]` (items no es `|null`) → dedujo la regla "pon `?.` solo donde la izquierda puede ser null/undefined". Quedó la versión mínima de 2 `?.`. F6 capstone 3 formas (`?.prop`+`?.()`+`??`): salió a la primera, usó `??` y no `||` para preservar el descuento `0%` sin recordárselo. Nota histórica: en F2 sobrescribió con un ternario de strings (mi ejemplo "del mundo real") y lo rompió; se aclaró que ese ejemplo era para otro concepto.
  - **04–10 (proyecto "10 por carpeta") — RESUELTOS por el usuario (4 jul 2026). Sesión de mucha densidad conceptual y varias preguntas de mecanismo excelentes.**
    - **07 (quitar null/undefined de unión y lista):** `?.`/`??` en Bloque A + predicado de tipo en lista. Drill 3 `sinNulos` costó 4 pasadas y fue refuerzo PERFECTO del slip `map` vs `filter`: (1) `map(?? "")` (confundió transformar con quitar → `["a","","b"]`); (2) copió del drill 5 `map(?? "").filter(!== "")` (verde pero arrastraba bug: borra `""` legítimos); (3) añadió el predicado `(x): x is string` CORRECTO pero dejó el `map` delante neutralizando el filter (map mata el null antes → filter no encuentra nada); (4) borró el map → `xs.filter((x): x is string => x !== null)` limpio. El predicado (lo nuevo/difícil) lo escribió bien SOLO en el paso 3; lo que fallaba al final era el resto mecánico, y lo cerró leyendo la traza. Ver [[feedback_slip_map_noop_y_tipo_retorno]].
    - **08 (type guards propios `x is Tipo`) — EL MÁS DURO.** Porteros con nombre (`esString`/`esNumero`) pasados a `.filter` (entregar ≠ ejecutar). **Capstone `contarTipos` (reduce con acumulador objeto) destapó 3 capas:** (a) `strings++` sobre variable inexistente → debía ser `acum.strings++` (contador DENTRO de la alcancía); (b) faltaba el valor inicial del reduce (lección de 03-arrays: sin inicial toma el 1er elemento y revienta); (c) **filo real del lenguaje: `reduce` sobre `unknown[]` NO infiere el tipo del acumulador**, lo deja en `unknown` → cascada `TS18046`/`TS2698` en los spreads. Se arregla anotando el parámetro `acum: {strings:number; numeros:number}` (sin `as`, que está prohibido). **NOTA DE INTEGRIDAD:** el coach diagnosticó mal la causa primero (dijo que era el `return acum` faltante) y lo corrigió tras reproducir en aislado. También se corrigió el comando de typecheck: es **`tsc -b --noEmit`** (build mode con referencias), NO `tsc --noEmit` pelado (que da 0 errores SIEMPRE y no chequea de verdad). **Reapareció y se vivió el "verde ≠ tipos" en estado puro:** el test pasó con datos amables (`[1,"a",2,"b","c"]`, todos string o number) mientras el typecheck estaba rojo.
    - **09 (narrowing con `in`) — FLUIDO.** `'lado' in f ? ...` para distinguir formas sin tag. Único tropiezo: drill 1 escribió `lado in f` sin comillas (variable inexistente en vez del nombre-propiedad como string). Tras eso, 2/3/4 SOLO y rápidos (el `in` ya automático en 4 formas). Capstone `areaTotal` REUSÓ `area` con `.map(area).reduce((a,b)=>a+b,0)` (DRY) a la primera.
    - **10 (CAPSTONE de la carpeta — valores de formulario) — CIERRA LA CARPETA.** Teje 04–09. Resolvió con `switch (typeof v)` (elección propia, más legible que el ternario). **Esquivó la trampa del `0`** en `esVacio` (comparaciones explícitas `v === null || v === ""`, nada de `!v`). **Pregunta de mecanismo EXCELENTE:** "si en el switch no se especifica el number, ¿por qué da false?" → se desmontó ROUTING (a qué rama) vs CÁLCULO (qué devuelve la rama): un number cae en `default` porque no matchea `case 'string'`, y ahí `v === null` da false; el `default` atrapa DOS tipos (number Y null) y el `v === null` los separa dentro. **Aprendió TS moderno real (no estaba en el guion):** sus `filter((v) => v !== null)` y `filter((v) => typeof v === 'number')` SIN predicado explícito compilan limpio porque **TS 6.0.3 tiene inferred type predicates (desde 5.5)** — TS deduce solo que la flecha es un type guard. El "OJO" del enunciado del 07 ("TS no lo conecta solo") quedó desactualizado para esta versión. Capstone `resumen`: primer intento contó mal los "válidos" (`typeof v !== 'number' && v !== null` tiraba los números, que SÍ son válidos → 2 en vez de 3); lo arregló a `v !== null`.
  - **BLOQUE G (APROBADO 7/7, 14 jun 2026):** escalera AÑADIDA a petición suya ("mi mente aún no entiende los diferentes trabajos de `??` y `?.`") — el problema raíz era que en E/F SIEMPRE los usó pegados en una línea, nunca por separado. G los DIVORCIA: G·1 (G1–G3, **solo `??`**, sin `?.`: respaldo puro — G2 trampa del `0`, G3 que `??` caza `undefined` igual que `null`), G·2 (G4–G5, **solo `?.` SIN `??`**, retorno `string | undefined` a propósito para que VEA salir el `undefined` crudo; test con `.toBeUndefined()`), G·3 (G6–G7, los GEMELOS de G4/G5 + `??` al final = "el relevo": `?.` produce el undefined, `??` lo recoge → retorno vuelve a `string` a secas). Resolvió los 7 SOLO. Hitos: (a) en G1 cazó por su cuenta el matiz de que el respaldo es indistinguible de un dato real idéntico (`saludoG("invitado")` vs `saludoG(null)` → misma salida) → se conectó con elegir respaldos que un dato real nunca sería (`-1` para stock). (b) Aplicó la regla F5 "`?.` solo donde la izquierda puede ser null" leyendo el TIPO (puso `?.` tras `u`/`p`/`envio` y NO tras `nombre`/`ciudad` porque la estructura los garantiza). (c) Vocabulario corregido REINCIDENTE: "se imprime/muestra" por "se DEVUELVE" (return ≠ console.log) — vigilar, lleva 3+ correcciones entre sesiones. Corregido también "el `?`" → "`?.`" (con punto; el `?` solo es propiedad opcional o ternario).
- `06-generics-basicos/` — **CARPETA COMPLETA (10/10 archivos, 84/84 tests + typecheck limpio, 12 jul 2026).** 01 y 02 APROBADOS (02 COMPLETO, 36/36 drills, jun 2026). 03–06 APROBADOS (jul 2026). **07 y 08 CERRADOS el 12 jul 2026; 09 y 10 ya estaban resueltos** (el usuario no era consciente de haber cerrado la carpeta).
  - **08 (opcionales y respaldos `T | undefined`) — COMPLETO (5/5 + typecheck limpio, 12 jul 2026).** `oPorDefecto`/`primeroODef`/`mapearDef`/`noNulos`/`primerValido`. **La lección de la sesión fue "test VERDE ≠ código correcto", vivida en carne propia:** el drill 3 `mapearDef` pasaba sus 2 tests con la guardia `x ? fn(x) : def`, pero esa condición pregunta por VERDAD (truthy), no por EXISTENCIA → `mapearDef(0, n => n*2, -1)` devolvía `-1` en vez de `0` (el test solo probaba `5` y `undefined`). Lo cerró con `x !== undefined` tras dársele la escalera rankeada (`!== undefined` / `!= null` / guardia invertida). **Analogía que funcionó: el portero que en vez de mirar si traes ENTRADA mira si le CAES BIEN — el `0` y el `""` traen entrada.** Drills 4 y 5 los resolvió mejor que el coach: (a) en `noNulos` escribió `filter(x => x !== null)` SIN predicado explícito y compiló — **el coach había dado por buena la regla vieja ("filter no estrecha"), desactualizada: TS 5.5+ tiene inferred type predicates** (se verificó que la inferencia SÍ se rompe con `(x): boolean` y con `filter(Boolean)`); (b) en `primerValido` eligió `find(...) ?? def` en vez de componer `noNulos`+`primeroODef` como se le había sugerido — **mejor solución** (corta al primer no-nulo, sin array intermedio). **Tres preguntas de MECANISMO excelentes, todas sobre el mismo hueco conceptual (entregar una función ≠ ejecutarla):** (1) "¿por qué `undefined * undefined` daría -1?" → `fn` NUNCA se ejecutó, el ternario solo evalúa UNA rama; el `-1` es `def` devuelto entero, no calculado (si se ejecutara daría `NaN`). (2) "¿por eso el compilador dice possibly undefined?" → se corrigió el modelo: **el compilador NO ejecuta el código, lee el TIPO**; "possibly" = "tu firma ADMITE undefined", no "vale undefined" (analogía: inspector que lee los PLANOS, no visita la obra); y la guardia no cambia ningún valor, cambia **lo que TS SABE en cada rama** (narrowing). (3) "¿por qué VSCode marca roja la segunda `n` en `mapearDef(undefined, (n) => n * 2, -1)`?" → **inferencia genérica sin fuente**: el único sitio de donde TS puede sacar `T` es `x`, y le pasó `undefined` → `T = undefined` → `n` es `undefined` → `TS18048`. Por eso el TEST escribe `mapearDef<number, number>(...)` con argumentos de tipo explícitos: no era capricho. Analogía: pedir traducir "de X a español" y entregar una HOJA EN BLANCO. Ver [[project_ts_mindset_shift]], [[feedback_animo_evidencia_progreso]].
  - **07 (`plegar` — reduce genérico) — COMPLETO (jul 2026), con escalera R0 de refuerzo autorada.** El drill 1 apilaba TRES cosas nuevas (dos comodines `<T,U>` + parámetro que es una FUNCIÓN + `reduce`), así que se montó un BLOQUE R0 que las aísla en tres sub-bloques: **G** (`<T>` como caja etiquetada: `identidad`/`envolver`/`par`), **F** (el callback como parámetro: `aplicar`/`aplicarDosVeces`/`mapear`), **R** (reduce: `sumar`/`multiplicar` con su neutro/`todosPositivos` donde el acumulador es de OTRO tipo que los elementos) y el pago `plegarBis`. Drills 2–5 (`total`/`contarPor`/`algunoCumple`/`resumenNumerico`) resueltos. Nota menor sin corregir (es suya): en `contarPor` usa `(acumulador[clave(v)] || 0) + 1` — aquí no muerde (el contador nunca vale 0 en el acumulador) pero el operador correcto por INTENCIÓN es `?? 0` (existencia, no verdad); mismo patrón que le falló en el 08.
  - **06 (Set y duplicados `Set<T>`) — APROBADO (5/5 + typecheck limpio, 5 jul 2026). Resuelto SOLO entre sesiones.** `unicos`/`incluye`/`sinElemento`/`agregarUnico`/`combinarUnicos` (capstone `[...new Set([...a,...b])]`). **CRITERIO PROPIO en drill 1:** el archivo enseña `[...new Set(xs)]` pero resolvió `unicos` con `xs.filter((x,i) => xs.indexOf(x) === i)` — válido e igual de correcto; se le señaló el matiz O(n²) vs O(n) del Set (irrelevante en listas cortas) sin marcarlo como error. Drill 4 `agregarUnico` con guard `includes(x) ? xs : [...xs, x]` (devuelve misma ref si ya está; no muta → cumple inmutable; se le apuntó que en React a veces se prefiere ref nueva siempre). Ver [[feedback_animo_evidencia_progreso]].
  - **05 (constraints `<T extends ...>`) — APROBADO (5/5 + typecheck limpio, 5 jul 2026).** Reencuentro con `extends` (no concepto nuevo: ya lo trabajó en exercise-02 y en los `Record<Clave,number>` de 07-utility). `masLargo` (`extends {length:number}`), `idDe`/`idsDe`/`porId` (`extends {id:number}`, con `find`/`map`), y capstone `maxPor` (reduce campeón GENÉRICO, `T` pelado + criterio por callback `valor(x)`). **`maxPor` costó 4 pasadas pero por DOS bugs que cazó/arregló él, NO por el patrón campeón (que salió entero a la 1ª: reduce<T|undefined> + inicial undefined + portero + comparación):** (1) **SHADOWING** — nombró el elemento del reduce `valor`, igual que la función criterio → el nombre interno tapó la función (TS6133 'valor' never read + TS2349 not callable + TS2304 'x'); lo desenredó renombrando a `dato`/luego `(m, x)`. (2) **POLARIDAD DEL PORTERO** — tras arreglar nombres cambió el correcto `acum === undefined ||` por `acum &&` (nunca sembraba el 1er campeón → devolvía undefined siempre); se trazó tabla turno a turno y lo invirtió. Curiosidad didáctica: su 1ª versión YA tenía el portero bueno y la 2ª los nombres buenos → el cierre fue JUNTAR dos piezas que tuvo correctas en momentos distintos. **Reincidió el slip de NO GUARDAR (Ctrl+S): un "revisa" sobre archivo idéntico.** El techo histórico (reduce campeón de 07-utility) quedó demostrado superado: lo montó de cero, con constraints, sin plantilla. Ver [[feedback_slip_map_noop_y_tipo_retorno]], [[feedback_animo_evidencia_progreso]], [[project_ts_mindset_shift]].
  - **03 (utilidades de array genéricas `<T>`, `T[]`, `T | undefined`) — APROBADO (5/5, jul 2026).** `primero`/`ultimo` (`xs[0]` / `xs[len-1]` con `noUncheckedIndexedAccess` → `T | undefined`), `envolver` (`[x]`), `repetir` (`Array(n).fill(x)`), `tomar` (`slice(0,n)`). Resuelto entero y limpio.
  - **04 (genéricos que envuelven callbacks `<T>`, `<T, U>`) — APROBADO (5/5 + typecheck limpio, 5 jul 2026). CIERRA el bloque callbacks-genéricos.** `mapear` (`<T,U>`, U lo decide el callback), `filtrar`, `encontrar` (`find` → `T | undefined`), `contarSi` (`filter().length`), y capstone `mapearYfiltrar`. **HITO anti-slip: el capstone es el 6º gemelo del patrón map+filter y lo escribió de UNA pasada sin ninguno de los dos resbalones históricos** (`.map(x=>x)` no-op + tipo de retorno viejo) — en 01-tipos-basicos 08/09 caían incluso pre-avisados; aquí el músculo se sostiene solo. Detalle: escribe los callbacks con envoltorio `(x)=>fn(x)` en vez de point-free `.map(fn)`; se le señaló como matiz de estilo (ambas correctas), no lo forcé. Ver [[feedback_slip_map_noop_y_tipo_retorno]], [[feedback_animo_evidencia_progreso]].
  - 01 (antes 11) — genéricos básicos. 7 drills en escalera (A=identidad / B=`T` en arrays / C=varias etiquetas `<A,B>` / D=`<T,U>` + callbacks). Cubre `<T>` como parámetro inferido, `T[]` y `T | undefined`, mezclar genérico con tipo fijo (`repetir(valor: T, n: number)`), y que en `<T, U>` el tipo de salida `U` lo decide lo que retorna el callback. Se explicó a fondo la ambigüedad de `undefined` en el borde de un array (vacío vs elemento undefined vs hueco/sparse) y `Array.from`. 7/7 tests.
  - 02 (antes 12) — constraints (`extends`) + literales + `keyof` + `T[K]`. Ejercicio grande con drills internos numerados 1→36 (sin letras), por bloques: A/B/refuerzo `extends` (1–13), keyof en escalera (14–21), `keyof` con un solo genérico (BLOQUE E, 22–26), y refuerzo dúo `<T,K>`+`T[K]`+pluck (BLOQUE F, 27–36).
    - **REINICIADO (jun 2026) a "versión nombres descriptivos".** El usuario tenía resueltos 1–30 con la convención `T`/`K`, pero se atascó leyendo la sintaxis ("siento que leo chino", "los tipos vacíos T y K"). A petición suya, se reescribió `exercise-02.ts` DESDE 0 (mismos 36 drills, mismos nombres de función → tests intactos) cambiando los genéricos a **nombres descriptivos** (`Objeto`, `Llave`, `Item`) en explicaciones y starters, con una NOTA arriba aclarando que la convención profesional es `T`/`K` y hay que aprender a LEER ambas. Starters "flojos" otra vez (sin portero / retorno `unknown` / clave `string`) para resolver todos de nuevo. Decisión del usuario: "Reemplazar exercise-02" + "Vacíos para resolver" (aceptó perder las soluciones previas).
    - **Lo que destrabó la lectura** (registrar para FASE 2 React+TS): (a) un genérico es un HUECO que se RELLENA al LLAMAR (T = la forma del objeto que pasas, K = el nombre exacto de la llave); (b) analogía del VESTUARIO — objeto = casilleros; la LLAVE es la ETIQUETA (nombre), el VALOR es lo guardado dentro; `keyof Objeto` = lista de etiquetas, `Objeto[Llave]` = tipo de lo guardado; (c) `keyof T` NO es `string`, es un MENÚ cerrado de literales ("patata" se rechaza) — conecta con drills 14–15; (d) puedes renombrar los genéricos como quieras (no son palabra reservada). Cuesta especialmente: distinguir LLAVE (nombre) vs VALOR (contenido).
    - **Estado tras el reinicio: drills 1–13 APROBADOS (bloque `extends` completo, jun 2026), 14–36 PENDIENTES.** El usuario resolvió 5–13 SOLO entre sesiones, incluidos los dos con truco (7 `areaRect` portero con DOS campos, 8 `presentar` portero con MÉTODO `{ saludar(): string }`), y reutilizó bien el alias `Identificable` en 3 funciones. Dos correcciones menores en la revisión: un typo (`18Va`) y un comentario que decía `false` donde la llamada devuelve `true` (`tieneContenido("Hola buenas tardes")`) — al preguntarle, razonó correcto ("length es 18 y 18 > 0") y lo corrigió él, añadiendo además el caso de contraste `tieneContenido([]) → false`. **Drills 14–36 APROBADOS — BLOQUE F CERRADO (jun 2026), exercise-02 COMPLETO.** Hitos del cierre 28–36: (a) 28 `igualA` (`Objeto[Llave]` en PARÁMETRO): primer intento confundió el tipo del ARGUMENTO (`"treinta"` = string) con el tipo del PARÁMETRO (`valor: number`), y mezcló rechazo de tipos con "devolver false" → se separaron las DOS CAPAS (rechazo en compilación vs comparación en runtime: `25` compila y da false; `"treinta"` ni corre); en el 2º intento aplicó la escalera de sustitución perfecto. (b) 29: la palabra **CONTRATO** hizo clic (la firma define cantidad+tipos; las llamadas se adaptan, no al revés — propuso añadir `c[clave]` para encajar una llamada rota de 4 argumentos y se le corrigió la dirección). La bonus (¿`Llave` se gana su sueldo?) la esquivó ~7 turnos y dijo "no entiendo" → se destrabó con las firmas del 24 vs 29 lado a lado + traza de hover (¿cambia algún tipo al cambiar la llave?); respondió bien "b, sobra porque el retorno siempre es boolean", y la inversa también (28 sin `Llave` → `valor: unknown` = el starter flojo). (c) 30–34: los 5 porteros a la primera (gesto automatizado). PATRÓN a vigilar: cuando el 👉 dice "portero + RETORNO" entrega solo el portero (pasó en 30/33/34 y de nuevo en 35) — darle el check mental "① puerta ② salida". En 34 reincidió en escribir el RESULTADO a mano (`string[]`, mismo error que 18–19) + arrastró un `| undefined` de contrabando del 33; lo corrigió leyendo el error TS2322 (que nombra el tipo correcto en su 1ª línea). Preguntó cómo dedupe el Set → analogía ÁLBUM DE LÁMINAS (lámina repetida no tiene segundo casillero) + 3 pasos map→Set→spread; cerró bien. (d) 35 `dosValores`: la TUPLA costó 3 intentos (unión `A | B[]` → `Objeto[Llave1, Llave2]` → coma faltante) → se enseñó la SIMETRÍA valor↔tipo (`["Nico", 23]` ↔ `[string, number]`) + tabla de los 3 corchetes (`X[]` array / `[X,Y]` tupla / dueño de cada par); pregunta de control bien: con UN hueco compartido TS infiere la UNIÓN y las dos posiciones pierden precisión. (e) 36 capstone `buscarPor`: firma de 3 piezas A LA PRIMERA. Su ejemplo `buscarPor({}, ...)` decía "undefined" pero en realidad NO COMPILA (las dos capas otra vez, espejo del error del 21) — lo corrigió él con un caso runtime real (`[{id:3}], "id", 2` → undefined). Pregunta de control bien: `.find` devuelve el OBJETO COMPLETO (vs `valorDe` que da el contenido del casillero) → por eso `Objeto | undefined`. Nota didáctica del 22: su primer intento trajo el DÚO completo (`<Objeto, Llave extends keyof Objeto>`) — compila, pero es un genérico que "no gana su sueldo" (nadie usa lo que `Llave` captura; se conectó con el criterio D3 del parcial). Se le dio la REGLA DE DECISIÓN del bloque: ¿el retorno depende de la llave? → dúo + `Objeto[Llave]`; ¿retorno concreto (boolean/string/number)? → un solo genérico + `clave: keyof Objeto`. Corrigió a la forma ligera bien. Hitos del cierre del bloque E (24–27, jun 2026): (a) 24 `sonIguales` lo resolvió SOLO entre sesiones aplicando la regla de decisión sin recordatorio, y probó con llaves number Y string. (b) 25–26: firmas bien, pero su modelo verbal de `filter` necesitó TERCERA corrección de LLAVE vs VALOR (insistía en "¿la clave está dentro del objeto?") — lo destrabó el argumento "si fuera existencia de la llave, los dos objetos la tienen → retornaría siempre arr.length, pero retorna 1" + traza peldaño a peldaño de `obj[clave]` (la etiqueta se USA para sacar el valor; `Boolean` evalúa su veracidad). Cerró con frase correcta propia: "saca false porque el VALOR de ok es false". Matiz enseñado: cuenta truthy, no solo `true` literal. (c) La pregunta de control (¿por qué E no necesita `Llave`?) dijo "no sé" DOS veces → se enseñó con escalera concreta (mismas 3 llamadas contra `mostrarValor` vs `valorDe`: ¿cambia el TIPO de salida al cambiar la llave?) + **analogía GUARDARROPA vs FOTOCOPIADORA** (guardarropa = te devuelven LO QUE dejaste → el ticket `Llave` debe recordar el casillero → dúo; fotocopiadora = salga lo que salga es papel/string → salida fija → sin `Llave`). Respondió bien: "fotocopiadora, siempre retorna number aunque cambie la llave". REGISTRAR: la analogía guardarropa/fotocopiadora funcionó — reusarla en bloque F y FASE 2. (d) 27 `valorDe`: el dúo completo a la primera. Además dijo que el 21 fue "el techo" de sintaxis y pidió expandir con más ejercicios tipo 21 → se le mostró que el bloque F (27–36) ES esa expansión; si tras terminar F sigue floja la sintaxis, montarle un bloque G con más plucks. En el 21 la firma salió bien a la primera, pero su llamada de ejemplo pasó UN objeto suelto donde iba `Objeto[]` (TS2353 "'id' does not exist in type 'unknown[]'") — se conectó con la mochila/argumentos y con CÓMO LEER el error ("si TS menciona `X[]` donde veías un objeto, sospecha corchetes"); lo corrigió él. Hitos del tramo 14–20: (a) en 18–19 escribió el RESULTADO a mano (`string`/`boolean`) en vez del acceso indexado — se le exigió la sintaxis `Libro["titulo"]` con el argumento de mantenimiento + "en el 20 no podrás escribir el resultado porque no lo sabes"; corrigió bien. (b) En el 20 tenía la firma correcta pero NO el modelo: preguntó "¿la función retorna Objeto[Llave]?" y creía que la llamada `propiedad({...}, "name")` tenía 3 ARGUMENTOS (contaba las propiedades del objeto inline como argumentos) — se destrabó con: comas dentro de `{}` separan PROPIEDADES vs coma del paréntesis separa ARGUMENTOS (analogía mochila: 2 cosas, no 3) + reescritura `const ana = {...}; propiedad(ana, "name")`. (c) Pidió ver la versión JS → se enseñó type erasure en concreto (la firma es 80% tipos; lo que sobrevive es `function propiedad(obj, llave) { return obj[llave] }` que ya dominaba de JS). (d) En la pregunta de control dijo "el retorno podría ser un objeto" → se destrabó con la ESCALERA DE SUSTITUCIÓN (rellenar huecos = reemplazar texto: `Objeto[Llave]` → `{id:number;tipo:string;activo:boolean}["tipo"]` → drill 18 → `string`); verificó solo la segunda (`"activo"` → boolean). Patrón didáctico que le funciona 2 veces ya: sustitución mecánica peldaño a peldaño.
    - **Terreno conceptual cubierto esta sesión (jun 2026) — registrar para no repetir:** (a) VOCABULARIO afinado: lo de `<>` es un **parámetro de tipo** (paralelo a parámetro de valor `(x)`); lo que entra al llamar es el **argumento de tipo**; TS lo deduce solo = **inferencia**. (b) Es **exclusivo de TS/lenguajes tipados**: JS no tiene tipos, por eso nunca los usó; al compilar se **borran** (*type erasure*). (c) Analogía que funcionó y se CORRIGIÓ: chapa de baño (abre con cualquier cosa) = `<Item>` PELADO; la "llave con diseño" = el `extends`/constraint, NO el parámetro de tipo (él la confundía). Superpoder que la analogía no capturaba: el parámetro de tipo **recuerda qué lo abrió** (captura el tipo para reusarlo en la salida) — por eso brilla cuando el retorno depende de la entrada (`Objeto[Llave]`), y por eso en drill 3 "no ganaba su sueldo". (d) El constraint es un **contrato de doble lado**: tiene que servir al CUERPO (`> 0` pide algo numérico) Y a los CALLERS reales (el `.length` de string/array ES number) — `{length:string}` fallaría por ambos. (e) "¿Para qué sirven en la vida real?" (pregunta suya, hilo de impostor): se aterrizó con casos de SUS proyectos — ya usó genéricos sin saberlo (`useState<Producto[]>`, `useRef<HTMLInputElement>`), `getJSON<T>` (fetch tipado), `sortBy(productos, "precio")` con `keyof` (typo de columna = error en editor), `<Table items={...}>` reutilizable. (f) Frase-resumen que quedó: **genéricos = reutilización SIN perder precisión de tipo** (vs duplicar = preciso pero no reusable; vs `any` = reusable pero tiras la seguridad); y la "seguridad" es en DESARROLLO (editor/CI), no protege el producto en runtime.
  - Notas didácticas clave: tipos literales como subtipo estrecho de `string` (analogía círculos / código de vestimenta), y que el tipo de salida en map/filter lo decide el callback.
- `07-utility-types/` — **CARPETA COMPLETA (10/10 archivos, 72/72 tests, jul 2026).** (Entrada añadida el 12 jul 2026: la carpeta estaba resuelta y verde pero NO figuraba en esta progresión. Los hitos didácticos de la "maratón de utility-types" no quedaron registrados en su momento; se cita de pasada en el 08-discriminated/06 como el origen de su "martillo" del `reduce`.)
- `08-discriminated-unions/` — **CARPETA COMPLETA (10/10 archivos, 62/62 tests, jul 2026).** (Cabecera corregida el 12 jul 2026: decía "08–10 pendientes", desactualizado — el commit `abada` ya los cerró. La escalera C de refuerzo del 07, C1–C5, sigue montada y SIN RESOLVER: es material de repaso disponible, no un hueco.) 01 (antes 08) uniones discriminadas. **02 (refuerzo, jun 2026)** — discriminante-por-PROPIEDAD (mató el reflejo de `typeof figura` que falló en el parcial) + exhaustividad con `never`. 5 drills (A=narrowing por propiedad, B=patrón `never` con 3 y 4 variantes). El usuario hizo clic con el `never`: entendió que es chequeo en COMPILACIÓN (no runtime), que TS borra los tipos, y que `never` = "ya no queda ningún caso por atender". 5/5 tests.
  - **03 (máquina de estados de carga `cargando`/`exito`/`error`) — COMPLETO (5/5 + typecheck limpio, 9 jul 2026).** El patrón estrella de toda petición a API (analogía cartero: no abres la caja hasta que llega). Variantes con CAMPOS DISTINTOS (`usuario` solo en "exito", `mensaje` solo en "error"). Bloque A `estaCargando`/`nombreSiExito`/`edadOcero` (narrowing por `e.estado` antes de tocar el campo exclusivo). Bloque B `mensajeEstado` (switch de 3 casos SIN default) y capstone `resumenEstado` (switch + `default` con guardia `never`). **Contraste enseñado al cierre (buen "por qué"): el drill 4 compila sin `default` ni return final porque la unión CERRADA agota los 3 casos; el `never` del drill 5 no es obligatorio HOY sino red de seguridad para MAÑANA (si añades una 4ª variante y olvidas su `case`, el `const _exhaustivo: never = e` lo grita en compilación con TS2322).** Resuelto entero por el usuario.
  - **04 (máquina de estados `Peticion`, discriminante string `fase`) — COMPLETO (9/9 tests + typecheck limpio, jul 2026).** Unión de 4 fases `inactivo`/`cargando`/`ok`/`fallo` = "estados imposibles, imposibles" (no puedes tener `datos` y `error` a la vez). **Decisión didáctica: la fase inicial se llamó `"inactivo"` en vez de `"idle"`, con NOTA en el archivo de que en React pro (React Query, Redux Toolkit) SIEMPRE es `status: "idle" | "loading" | "success" | "error"`.** Bloque A: `puedeReintentar`, `cantidadDatos`, y `descripcion` (switch EXHAUSTIVO + guardia `const _exhaustivo: never = p` con `return _exhaustivo` en el default — se arreglaron dos slips de sintaxis del starter: `case: "idle"` con dos puntos de más, y el `never` sin cerrar). Bloque B: `desdeBanderas` (drill 4, cadena de `if`-con-`return` por PRIORIDAD: el código gana aunque haya datos/cargando) y capstone `reintentar` (una TRANSICIÓN de estado — germen del reducer del 07). **Se AUTORÓ una escalera C de refuerzo del drill 4** (C1 `primeraActiva` prioridad pura sin objetos / C2 `desdeCodigo` una señal nullable→2 variantes / C3 `desdeCargaODatos` dos señales / C4 `ganaPrimero` EL ORDEN IMPORTA, la trampa exacta `(true,500)→"fallo"`) + sus tests. Concepto clave: `if (x)` con `null` falsy / number truthy, y "el primer `return` que dispara corta los de abajo" (analogía triage).
  - **05 (discriminante BOOLEANO — el tipo `Resultado<T>`) — COMPLETO (5/5 tests + typecheck limpio, jul 2026).** El "tag" no tiene que ser un string en un campo `tipo`: `ok: true`/`ok: false` son literales válidos → `if (r.ok)` narrowea a `{ valor }` y el `else` a `{ error }` (analogía sobre del banco con sello PAGADO/DEVUELTO). Bloque A leer: `esExito` (el discriminante ES la respuesta, sin if), `valorODefecto` y `mensajeError` (ternario sobre `r.ok`). Bloque B producir: `dividir` (errores como DATO, no excepción) y capstone `mapResultado` (transforma el valor de éxito con `f`, deja el error intacto — antesala de `andThen`/`secuenciar` del 06).
  - **06 (errores como DATOS: `andThen` + `secuenciar`) — COMPLETO (8/8 tests + typecheck limpio, 9 jul 2026).** Cerrado EN SESIÓN. Bloque A `parseNumero`/`raiz` (fábricas de Resultado), B `andThen` (encadena: si ok aplica `f`, si error lo deja pasar sin llamar a `f`) y `primerError` (recorre + corta al primer `!r.ok`). **Escalera C de refuerzo del capstone (autorada): C1 `por10` acumulador puro (cesta del súper), C2 `soloPositivos` acumular+CORTAR (short-circuit con `return null`), C3 `valoresSiTodoOk` (C2 pero cortando por `!r.ok` y acumulando `r.valor`), y capstone `secuenciar` (C3 envuelto en Resultado).** **ARCO DE LA SESIÓN (registrar — es anti-impostor puro): abrió con "el C3 no lo sé resolver, no entiendo" y cerró resolviendo el capstone `secuenciar` ÉL SOLO.** El bloqueo real del C3 NO era el concepto (tenía C1 y C2 verdes) sino la HERRAMIENTA: fue al `reduce` (su martillo de la maratón de utility-types) y se estrelló → **lección grande que quedó: `reduce` acumula pero NO sabe frenar a media lista; un `return` dentro del callback solo cambia el acumulador del siguiente turno, no sale de la función. Para short-circuit hace falta `for...of`, donde `return` sí aborta todo.** C3 se resolvió como EJEMPLO GUIADO (a petición suya, "resuélvelo tú para entender"), comentado línea a línea, anclando el "aha" del narrowing: puedes escribir `r.valor` en el push SOLO porque el `if (!r.ok) return` de arriba ya descartó la rama de error → TS sabe que lo que queda es `{ ok: true; valor }`. **Buena pregunta de comprensión suya (señal de que entró):** conectó sola `if (!r.ok)` con el ejemplo `[{ok:true},{ok:false}]→null` = "aunque muchas sean true, si UNA es false se corta todo y retorna null" (regla todo-o-nada). En el capstone `secuenciar` aplicó los DOS cambios sobre C3 sin que se cayera ninguno (`return null`→`return {ok:false,error:r.error}` y `return salida`→`return {ok:true,valor:salida}`); micro-nota dada: el corte podía ser `return r` a secas (narrowing → `r` YA es el sobre rojo), ambas válidas. Ver [[feedback_aprende_con_analogias]], [[feedback_animo_evidencia_progreso]].
  - **07 (unión de acciones — el patrón `reducer`) — DRILLS 1–5 COMPLETOS (5/5 + typecheck limpio, 9 jul 2026); escalera C de refuerzo MONTADA y PENDIENTE de resolver.** Es LITERALMENTE `useReducer` de React (FASE 2, exercise 11): `Accion` = unión de eventos (`{ tipo: ... }`), `aplicar(estado, accion)` = reducer con `switch(accion.tipo)` + guardia `never`, estado nunca mutado. Analogía cajero. Drills 1–3 (`aplicar`/`etiquetaAccion`/`esDestructiva`) ya estaban. **Drill 4 `aplicarTodas` (reduce con carry, reusa `aplicar`): reincidió el slip "calculo y tiro el resultado"** — puso el `reduce` PERFECTO (callback + valor inicial `estado`, la pieza que antes olvidaba) pero devolvía `return estado` (el original), tirando el resultado del reduce (gemelo del `secuenciar` sin `return` de ayer). **Lo cazó y corrigió él solo** en una pasada (`return acciones.reduce(...)`). Capstone `aplicarConHistorial` (estado RICO: objeto `{ contador, historial }`, actualización inmutable reusando `aplicar` + `etiquetaAccion` + `[...historial, etiqueta]`): **lo escribió LIMPIO de una pasada, sin ningún slip** — el patrón `{ ...viejo, campo: nuevo }` ya es gesto. **PERO al cerrar fue SINCERO: "el test pasa pero no entiendo la lógica del capstone" (todo el bloque foggy, respondió "todo" al AskUserQuestion de dónde drillar).** Excelente actitud (verde ≠ comprensión, frenó en vez de seguir). **Respuesta del coach: se AUTORÓ un BLOQUE C de refuerzo (5 drills) que descompone `aplicarConHistorial` pieza a pieza, analogía CUENTA BANCARIA (contador=saldo, historial=extracto):** C1 `leerContador` (sacar un campo `estado.contador`), C2 `agregarEtiqueta` (`[...vieja, nueva]` sin mutar), C3 `nuevoEstado` (construir `{ contador, historial }`, params `n`/`lista` distintos de los campos para forzar el mapeo), C4 `avanzarManual` (combinar C2+C3 con valores dados = la FORMA del capstone), C5 `aplicarConHistorialBis` (capstone reconstruido, ahora calcula con `aplicar`/`etiquetaAccion`). Verificado protocolo: soluciones 10/10 verde + typecheck limpio → cuerpos vaciados → 5 rojos por diseño. **PENDIENTE: el usuario resuelve C1→C5 en orden la próxima sesión.** Ver [[feedback_drills_en_escalera]], [[feedback_aprende_con_analogias]], [[feedback_slip_map_noop_y_tipo_retorno]].

- `09-react-props/` — **✅ CARPETA COMPLETA (10/10 archivos, 171/171 tests, `tsc -b` limpio, 25 jul 2026).** 01–04 cerrados el 20-21 jul; **05–08 resueltos por el usuario entre sesiones** (llegaron en verde, sin coaching intermedio); **09 y 10 cerrados en sesión** el 24-25 jul. Primera carpeta `.tsx`. **Tesis de la carpeta: tipar props NO es nada nuevo** — un componente es una función con UN parámetro que es un objeto, así que tipar props = tipar un parámetro-objeto (reusa 04-objetos entero). Alcance decidido: props estrictas **+ los 3 huérfanos de React básico** (render condicional, listas/keys, composición) que no tienen carpeta propia (10=eventos, 11=state, …); NO se adelantan tipos de eventos del DOM (eso es del 10). Densidad: 10 drills/archivo como en FASE 1.
  - **Convención NUEVA de FASE 2 (documentar y reusar):** la "línea de trazado" ejecutable de FASE 1 **NO funciona en `.tsx`** — un `<Componente />` suelto a nivel de módulo solo construye un descriptor que se descarta, no ejecuta nada (ni TS, ni ESLint, ni los tests lo ven). Su equivalente aquí es un **ejemplo de uso comentado con `//`**. Además el andamiaje de starters usa `{}` (props e patrón vacíos) → cada archivo lleva un bloque `eslint-disable` de `no-empty-object-type`+`no-empty-pattern` que el usuario BORRA al cerrar (su warning "unused directive" es la señal de que ya no quedan `{}` sin tipar). Ver [[feedback_trace_lines_comentadas]], [[feedback_starter_cuerpo_en_blanco]].
  - **exercise-01 (props como objeto tipado) — CERRADO EN SESIÓN (20 jul 2026).** 10 drills: B0 componente=función que retorna JSX; B1 una prop (`props: { nombre: string }`); B2 desestructurar (mismo lío del `=`/`:` del 01/exercise-07 — lo resolvió A LA PRIMERA sin pista, el error de `typeof` ya no lo comete); B3 varias props + opcional `?`+`??` + unión de literales (`variante: "info"|"alerta"`, verificada con `@ts-expect-error`→TS2578); B4 capstone `ListaUsuarios` (`Usuario[]` por prop + `.map`→JSX + `key`); B5 `children: ReactNode`; B6 prop-función `onAccion: () => void` (la CAFETERA de 02-funciones/06 aguantó el salto: `onClick={onAccion}` entrega, `onClick={onAccion()}` ejecuta — verificado con espía `vi.fn()` que NO debe llamarse antes del clic); B7 capstone `Panel` (dato+contenido+función juntos). **SLIPS de la sesión (registrar, son los de siempre en piel nueva):** (a) **"segundo punto de una corrección de 2 puntos"** — 2 veces (drill 5: le dije "el problema es DÓNDE está `deshabilitado`" y cambió su VALOR con `?? ''` dejándolo en el sitio malo; drill 8: le dije "falta `className` en el div" y lo puso en el `<p>` de la línea de trazado MUERTA). Diagnóstico: lee el feedback, identifica el símbolo correcto, y lo escribe en el primer sitio plausible en vez de en el señalado. Antídoto dado: pregunta "¿esta línea la ejecuta alguien?" antes de tocar. (b) el `.map` de FASE 1 con JSX (drill 7) salió LIMPIO — el slip histórico `.map(x=>x)`+tipo-retorno NO apareció, primera vez con JSX de por medio. **Conceptos-clave que se afianzaron:** las MISMAS llaves `{}` significan 3 cosas según posición (tipo en la firma / expresión en JSX / bloque a nivel de sentencia — este último lo tocó al comentar mal la traza); `ReactNode` es un `type` (unión) que MIRÓ en `@types/react` con F12/Ctrl-clic (hábito nuevo: los tipos de React son `.d.ts` legibles, no magia) y su miembro `boolean` explica por qué `{deshabilitado}` no pintaba nada en el drill 5; `void` = "el que recibe no mira el retorno" (analogía TIRADOR DE ESPALDAS), NO "no puede devolver" (eso sería `undefined` / pistola de fogueo — verificado con `tsc`: `()=>number` encaja en `()=>void` pero no en `()=>undefined`); render condicional (cambiar QUÉ se pinta) ≠ cambiar un atributo (el botón siempre está). **Pendiente conceptual: la pregunta "¿por qué `key` no va en las props?" se le hizo 3× y la esquivó; se respondió por el coach (React la intercepta antes de construir props, canal lateral) y se convirtió en drill entrenable del exercise-04.** Ver [[feedback_slip_map_noop_y_tipo_retorno]], [[feedback_aprende_con_analogias]], [[project_ts_mindset_shift]].
  - **exercise-02 (props opcionales + valores por defecto) — CERRADO (18/18 tests, 20 jul 2026).** B0 opcional=`T|undefined` por dentro; B1 defecto con `??` en el cuerpo; B2 defecto en la firma (`{ tamano = 'medio' }`); **B3 el CORAZÓN: `??` vs `||`** (el bug del `0`/`""` falsy-pero-válido, con `PrecioRoto`/`PrecioBien` lado a lado); B4 capstone `Tarjeta` (3 opcionales con defaults coherentes). Verificado: 18/18 con solución, 16 rojos al vaciar (2 passers de runtime que el typecheck sí caza).
  - **exercise-03 (renderizado condicional — huérfano nº1) — CERRADO (14/14 tests, 21 jul 2026).** El drill 1 se cerró en la sesión del 20 jul; los 9 restantes los resolvió el usuario por su cuenta entre sesiones (llegaron ya hechos y en verde, sin coaching intermedio — no hay narrativa de slips que registrar). Nota de estilo suya: en el capstone `Notificacion` resolvió el texto con `mensaje || "Sin novedades"` donde el enunciado pedía ternario; funciona, pero ojo al matiz `||` vs `??` que ya trabajó en el 02 (con `mensaje=""` el `||` cae al defecto y el `??` no). Corrige el error de nombrar "render condicional" a un atributo. B0 `return null`=no pintar; B1 ternario (texto y elemento entero); B2 `&&` pinta-o-nada; **B4 la TRAMPA del `&&` con números** (`{lista.length && <X/>}` pinta un `0` fantasma; conecta con `number` es ReactNode legal → ni TS ni React protestan; `CarritoRoto`/`CarritoBien`); B3 early return + estado vacío; capstone `Notificacion`. Verificado: 14/14 con solución, 12 rojos al vaciar.
  - **exercise-04 (listas y keys — huérfano nº2) — CERRADO EN SESIÓN (12/12 tests, 21 jul 2026).** B0 map de strings; B1 map de objetos key=id; **B2 por qué `key` NO va en props** (React la intercepta, canal lateral, analogía DORSAL del corredor) + id>índice; B3 lista vacía; **B4 capstone = la FAMILIA del slip histórico** (filter→map→sacar UN campo, ahora devolviendo `<li>`; `Escaparate` + `NombresEnStock` con test que caza el no-op). El usuario llegó con los drills 1–9 hechos; solo quedaba el 10.
    - **🏁 EL SLIP HISTÓRICO, MUERTO. `NombresEnStock` es el QUINTO gemelo del patrón filter→map→sacar-un-campo, y salió LIMPIO A LA PRIMERA**, sin pistas y sin las 2-3 pasadas de rigor. Comparativa del arco completo: #1 `nombresActivos` (01/05) dos capas; #2 `nombresPorRol` (01/08) tres pasadas; #3 `bienPagados` (01/09) falló incluso tras pre-aviso explícito; #4 `ofertasDe` (01/10) tres ediciones, una pieza buena por edición; **#5 aquí: cero.** Bonus: **reusó el nombre `producto` en el `filter` y en el `map` sin dudar** — la misconception del "choque por repetición de nombre" que corrigió en 01/10 (ámbitos HERMANOS, no anidados) está desmontada de verdad, no solo entendida.
    - **Duda suya que valió una mini-lección de sintaxis:** para invertir el filtro escribió `producto.!enStock`. El `.` exige un IDENTIFICADOR detrás, así que eso es error de parseo (del que tumba el archivo entero y mata el tooling, como ya vivió en 01/05 y 01/07). Va `!producto.enStock`: el `!` no forma parte del nombre de la propiedad, se aplica AL RESULTADO de la lectura (analogía: el `.` es el "de" del castellano → *"no (el enStock de producto)"*, no *"el ¡no-enStock de producto"*). Se cerró con la precedencia: `.` liga más fuerte que `!`, así que NO hacen falta paréntesis; `(!producto).enStock` sí cambiaría el significado.
    - **Apunte de CALIDAD levantado por el coach (tests verdes ≠ código limpio): drills 7 y 9 tenían el mismo reflejo — "hay dos casos → escribo dos returns" — pero con arreglos distintos.** En el 7 (`Contador`) las dos ramas del ternario producían HTML idéntico (si `length === 0`, `{items.length}` YA pinta `0`) → sobraba la condición entera. En el 9 (`Escaparate`) las ramas sí diferían, pero solo en el DATO → se decide la lista arriba y se pinta UNA vez, en vez de duplicar `<ul>`/`<li>`/`key`. Regla que quedó: **ante un ternario, mírale las dos ramas y pregunta en qué se diferencian; si es "en nada", bórralo.**
    - **⚠️ FEEDBACK DEL USUARIO — el coach se pasó de la raya.** Pidió "arréglalos tú" y en `Escaparate` le cambié, de paso, su `if` + early return por un `const` + ternario. Rechazó la edición: *"¿Por qué me cambias mi sintaxis?"*. Tenía razón: el encargo era la duplicación, no el flujo de control (y el early return es SU estilo desde el 03). Se rehízo respetando su `if` (`let visibles = productos; if (soloEnStock) visibles = productos.filter(...)`). **Regla vigente: al arreglar, se toca el defecto y nada más; los cambios de estilo se OFRECEN, no se aplican.** Ver [[feedback_no_cambiar_estilo_al_arreglar]].
  - **Escalera 05–10 — MONTADA Y VERIFICADA (22 jul 2026); RESUELTA ENTERA (25 jul 2026).** Protocolo de FASE 1 aplicado a los cuatro nuevos (07–10): solución escrita → **74/74 tests verdes + typecheck limpio** → cuerpos vaciados → **confirmado rojo (56 fallos + 116 errores de typecheck)**. Los 18 tests que siguen verdes con el starter vacío son los de tipos puros (`expect(x).toBeTruthy()` sobre `{}`), que sí caza el typecheck — el "verde ≠ tipos" de siempre.
    - **05 composición** (children vs slots nombrados; elemento `<X />` vs componente `X`) — 14 tests.
    - **06 `interface`+`extends`** en props + derivar con `Pick`/`Omit`/`Partial` (+ la trampa de `Omit` con la clave mal escrita) — 15 tests.
    - **07 props nativas del DOM**: `ComponentProps<'button'>` + rest `{...resto}` + **el orden del derrame decide quién gana** (`CajaFija`/`CajaAbierta`) + `Omit` para prohibir/estrechar props nativas — 17 tests.
    - **08 unión discriminada en props**: arranca enseñando el PROBLEMA (`BotonRoto` con opcionales, cuyo test documenta los dos agujeros: ni-una-ni-otra y las-dos-a-la-vez) → discriminante + narrowing → el operador `in` → guardia `never` → capstone `Campo` (3 formas, una con `.map`) — 19 tests.
    - **09 props-función CON argumentos**: el envoltorio `() => onSeleccionar(id)`, aislado primero SIN JSX en `hacerManejador` (fabricar ≠ ejecutar) → dentro del `.map` (cada fila recuerda su id) → callback opcional con `?.()` → **una prop-función que SÍ devuelve valor** (`formatear: (n)=>string`, contraste con `void`) — 18 tests.
    - **10 capstone árbol**: `PaginaPedidos → Panel → Contenido → TablaPedidos → FilaPedido → {EtiquetaEstado, Dinero}`. Junta las nueve herramientas; única pieza nueva, el **acceso indexado a tipos** (`Pedido['estado']`) — 20 tests.
  - **📌 Hallazgo verificado al montar el 08 (registrar, corrige una creencia común):** una unión SIN discriminante **NO impide que te manden las dos mitades a la vez**. `{ texto, segundos, onCerrar }` contra `A | B` COMPILA, porque contra una unión TS solo rechaza claves que no estén en NINGÚN miembro. Con discriminante sí se cierra: TS elige el miembro por el literal y luego revisa contra ese. Se descubrió porque un `@ts-expect-error` salió `TS2578` (no sobraba error: no había error). El test 6c quedó documentando el agujero en vez de fingir que no existe.
  - **exercise-05 a 08 — RESUELTOS por el usuario entre sesiones (22-24 jul 2026).** Llegaron ya en verde y sin narrativa de slips que registrar: 05 composición (14/14), 06 `interface`+`extends`+`Pick`/`Omit`/`Partial` (15/15), 07 `ComponentProps` + rest (17/17), 08 unión discriminada en props (19/19). Solo consta el resultado.
  - **exercise-09 (props-función CON argumentos) — CERRADO EN SESIÓN (18/18, 24 jul 2026).** Llegó con los drills 1–8 hechos; en sesión se cerraron el 9 (tipo `ListaTareasProps`) y el 10 (`ListaTareas`). **El tipo estaba en `= {}` y su test VERDE de mentira** — contra `{}` cualquier objeto encaja en runtime, y el `@ts-expect-error` que lo vigila solo lo caza el typecheck. Dos rojos en el capstone: (a) faltaba el early return del estado vacío; (b) **el botón "Borrar" se ocultaba con `onBorrar && <button>`**, cuando el enunciado pedía condicionar la LLAMADA y no la EXISTENCIA del botón → `onBorrar?.(id)`. Diagnóstico que le sirvió: la lógica no estaba mal pensada sino mal COLOCADA, y lo delataba que el test 10e (con callback) siguiera verde y solo cayera el 10d (sin callback).
  - **exercise-10 (CAPSTONE — el ÁRBOL de componentes) — CERRADO EN SESIÓN (20/20 + `tsc -b` limpio, 25 jul 2026). CIERRA LA CARPETA.** `PaginaPedidos → Panel → Contenido → TablaPedidos → FilaPedido → {EtiquetaEstado, Dinero}`, sin un `any` ni un `as`. Los datos bajan por props y los avisos suben por callbacks: `onVer` viaja PELADO cuatro niveles y vuelve como `onVer(7)` desde la fila que sabe su id. Nada se copió del dominio (`Pedido['estado']`, `Pick<Pedido,…>`, `ComponentProps<'span'>`).
    - **🔧 HALLAZGO DE TOOLING, el más importante de la sesión: `pnpm exec tsc --noEmit` NO COMPRUEBA NADA en este repo.** El `tsconfig.json` es de tipo *solution* (`"files": []` + `references`), así que **sin `-b` tsc no mira ni un archivo y sale con código 0**. El coach estuvo media sesión —y un mensaje de commit— diciendo "typecheck limpio" leyendo ese silencio. **El comando válido es el del script: `pnpm typecheck` = `tsc -b --noEmit`.** Resuelve además el misterio de la deuda técnica del 21 jul (ver sección de abajo).
    - **Drill 2 `EtiquetaEstado` — un `default` "hueco en blanco" ANULA la exhaustividad.** Resolvió primero con ternario anidado (con `: 'Entregado'` de cajón de sastre) y después con `switch`… pero `default: return <span></span>`, que es el mismo agujero con otra ropa. Verificado añadiendo una 4ª variante al dominio: **ni el `switch` sin `default` ni el `default` hueco producen error alguno**; solo la guardia `const _exhaustive: never = estado` da `TS2322` señalando la línea exacta. **Matiz NUEVO respecto a FASE 1: en un COMPONENTE la guardia no es opcional.** Allí el retorno era `string` y el hueco se notaba en el tipo; aquí un componente que se cae del `switch` devuelve `undefined`, `undefined` es un `ReactNode` legal y el tsconfig no tiene `noImplicitReturns` → silencio absoluto.
    - **Drill 3 `Dinero` — el derrame con pasajero de más.** `<span {...props}>` colaba `cantidad` al DOM como atributo. Lo arregló con `{ cantidad, ...resto }` **sosteniendo las dos piezas a la vez** (firma nueva + cuerpo ajustado a `cantidad` pelado), sin dejarse el `props.cantidad` viejo. Matiz verificado: **el spread en JSX NO comprueba claves de más** (`<C {...dato} />` con un campo extra compila), pero el atributo escrito a mano SÍ (`TS2322`) — el spread es cómodo y más permisivo.
    - **❓ SU MEJOR PREGUNTA (drill 4): "¿de qué sirve el `Pick` si en realidad trae todo? ¿no bastaría el `extends`?".** Tenía razón: `Pick<Pedido, las-4-claves>` **es** `Pedido` (verificado: asignables en ambos sentidos). Lo que compra el `Pick` no es un recorte hoy sino un **contrato congelado en lo que uso**: el día que el dominio gane un campo, `extends Pedido` obliga a TODOS los llamadores a fabricarlo (`TS2322`) y el `Pick` ni se entera. Se le dio la escalera de tres diseños (prop-objeto `pedido={p}` > `Pick` > `extends Pedido`) y se reconoció el smell: **un `Pick` que lista todas las claves parece un no-op**, y en revisión de código preguntarían lo mismo que él.
    - **⚠️ CORRECCIÓN DEL COACH (drill 8): se le dijo que desestructurar `({ estado, onVer })` rompía el narrowing. ERA FALSO.** Verificado con tres variantes: (A) sacar `estado` entero del objeto de props narrowea perfectamente — la prueba es que la guardia `never` compila; (B) lo que NO se puede es desestructurar **la unión** en sus campos (`({ fase, mensaje }: E)` → `TS2339`: sobre una unión solo existen las propiedades COMUNES), y eso es lo que avisaba el enunciado; (C) narrar por alias (`const { fase } = estado` y luego `estado.mensaje`) también compila. **El usuario leyó el aviso mejor que el coach.** Además escribió la guardia `never` sin que se la pidieran — en el drill 2 hicieron falta dos pasadas.
    - **🔁 EL PATRÓN DE LA SESIÓN — "mete la pieza nueva y deja el andamio viejo puesto": el drill 7 (`Panel`) costó TRES pasadas.** Las dos ramas del `if/else` eran idénticas salvo una línea (14 duplicadas para decidir 1). (1) Quitó el `else` → cero duplicación eliminada. (2) Borró el segundo `return` entero → **REGRESIÓN: el test 6, que estaba VERDE, se puso rojo** porque sin `acciones` el componente ya no devolvía nada… **y `tsc` no dijo ni una palabra** (la lección del drill 2 mordiéndole el mismo día). (3) Añadió el `&&` correcto pero dejó el `if (acciones)` de fuera, con la condición escrita DOS veces. Cerró a la cuarta. **La pieza nueva la escribió bien las tres veces; lo que falló fue retirar lo que sustituía.** Regla que quedó: *cuando metes la pieza nueva, la pregunta de cierre es "¿qué queda ahora de más?"*. Es hermano del slip histórico de FASE 1 (cuerpo nuevo + tipo de retorno viejo).
    - **Drill 9 `resumen` — el tipo de retorno otra vez, ahora por AUSENCIA.** Cuerpo correcto a la primera (early return + `reduce` **con valor inicial**, la pieza que antes olvidaba), pero se saltó el ⚠️ del enunciado y no anotó `: string`. Se explicó **inferir vs anotar**: sin anotación TS te informa de lo que el cuerpo produjo (espejo, siempre tiene razón); con anotación COMPRUEBA el cuerpo contra tu intención (contrato, y el error salta EN la función). Daño concreto en este archivo: `resumen` se interpola en JSX, así que si una rama pasara a devolver `number` **no se enteraría nadie** — `number` es `ReactNode` legal.
    - **Duplicación otra vez en el capstone final (drill 10):** dos `<Panel>` completos que difieren solo en el `<footer>`. Se ofreció `{estado.fase === 'listo' && <footer>…</footer>}` y **se verificó antes de proponerlo que el narrowing funciona dentro de la propia expresión `&&`**. Quedó como mejora opcional, no defecto: los tests aprueban su versión.
    - **📌 Recordatorios estilo `04-objetos` traídos a FASE 2 (petición suya).** Pidió el mismo bloque que le montó la carpeta de objetos pero con `Pick`/`Omit`. Se montó una cabecera **idéntica (verificada por hash) en los 10 archivos de la carpeta**: `Pick`/`Omit`/`Partial` + acceso indexado + `ComponentProps` y el rest, con analogía (foto de carnet: `Pick` recorta, `Omit` tapa, `Partial` no exige, el indexado saca una pieza sin el marco) y dos avisos — claves entre comillas, y **`Omit` NO protesta si escribes mal la clave** (`Omit<Usuario,'emial'>` compila y no quita nada). Ver [[feedback_type_reminder_local]], [[feedback_comentarios_didacticos]].
    - **Pendientes de trámite al cerrar: ✅ HECHOS (26 jul 2026).** Borrado el `eslint-disable` de andamiaje (`no-empty-object-type` + `no-empty-pattern`) de `exercise-09` y `exercise-10` — su warning *"unused directive"* era la señal prometida de que ya no quedaba ningún `{}` sin tipar, y al borrarlo desapareció. Borrada también la copia comentada de `Pedido` que había sobre el drill 4: el tipo real vive arriba en el bloque EL DOMINIO y a la copia no la vigila nadie (el día que el dominio cambiara, ese comentario mentiría).
  - **⚠️ Arruga de lint nueva en FASE 2 (07–10):** los drills "sin JSX" (`partirUsuario`, `describirBoton`, `etiquetaDe`, `hacerManejador`, `resumen`) disparan `react-refresh/only-export-components` — regla que exige que un `.tsx` solo exporte componentes, y que existe por el hot-reload de Vite. En un archivo de ejercicios no aplica: lleva `// eslint-disable-next-line` con el porqué escrito al lado. El resto de errores de lint en los starters (imports `ComponentProps`/`ReactNode` "sin usar", params sin usar en los cuerpos vacíos) son andamiaje: **desaparecen al resolver**, igual que en el 05.

- `10-eventos-formularios/` — **REHECHA DESDE 0 POR SEGUNDA VEZ el 8 ago 2026, a petición suya.** Segunda carpeta de FASE 2. **Cadencia: archivo a archivo** (se monta el siguiente cuando el anterior está cerrado), decisión suya sostenida en las dos reconstrucciones.

  **📚 TEMARIO PUBLICADO POR DELANTE — los 10 archivos, 1 concepto cada uno.** Se publica aquí *antes* de escribir nada, y el archivo lo repite en su `🗺️ MAPA`: es el antídoto directo contra su queja de *"estoy avanzando a ciegas"*. Un temario visible convierte cada archivo en un escalón localizable en vez de en una sorpresa.

  | # | Concepto | Al terminar sabrá | Estado |
  |---|---|---|---|
  | 01 | Entregar ≠ ejecutar (sin React, es `.ts`) | pasar una función sin llamarla | 🟢 **14/14 en test** (11 ago) — pendiente de revisar el razonamiento |
  | 02 | El evento llega solo: tipado contextual | leer `e` sin anotarlo | ✅ **CERRADO 6/6** (8 ago) |
  | 03 | Sacar el manejador del JSX y anotarlo | `MouseEvent<HTMLButtonElement>` | ✅ **CERRADO 6/6** (9 ago) |
  | 04 | Ponerle nombre al tipo | alias `type` + los de fábrica (`MouseEventHandler<T>`) | ✅ **CERRADO 12/12** (9 ago) — 6 drills + escalera T |
  | 05 | El manejador llega por props | quién lo define vs. quién lo monta | 🟡 **6/6 en test, 1 error de tipos** (11 ago) — sin cerrar |
  | 06 | `onChange` y el texto del campo | `ChangeEvent<HTMLInputElement>`, `e.target.value` | 📍 **montado 11 ago** (primer archivo nativo v3) |
  | 07 | `target` vs `currentTarget` | por qué no son lo mismo y cuándo duele | ⬜ |
  | 08 | `onSubmit` + `preventDefault` en `<form>` | `FormEvent<HTMLFormElement>` | ⬜ |
  | 09 | El manejador que traduce | evento → dato de dominio; la API no filtra el DOM | ⬜ |
  | 10 | CAPSTONE: formulario controlado | estado tipado + los 9 anteriores juntos | ⬜ |

  **Dos cambios respecto al temario de la v1**, los dos por el mismo motivo (*un concepto por archivo*, que la v1 incumplía justo donde él chocó): (a) el **04 se parte en 04 + 05** — mezclaba *alias de tipo* y *manejador por props*, dos conceptos; (b) **`preventDefault` se va del 03 al 08**, que es donde de verdad se usa. Del 09 en adelante entra `useState`, formalmente de la carpeta 11: aquí se entrena el TIPO, no el hook.

  - **🔴 POR QUÉ SE REHIZO LA SEGUNDA VEZ — la v1 recortó lo que no tocaba.** Su queja: *"los enunciados de los ejercicios no los entiendo, necesito algo más académico, estoy avanzando a ciegas"*. La v1 (29 jul) arregló el problema real de entonces —un `exercise-01` de 475 líneas con 6 conceptos apilados— imponiendo un techo de 40 líneas de cabecera. Pero al recortar **se cayó la teoría, no el ruido**, y quedaron cuatro defectos concretos: (1) los enunciados describen un **defecto**, no un **objetivo** (*"El starter hace DOS cosas mal, una de cada archivo"* — para leerlo hay que saber ya la respuesta); (2) el vocabulario del enunciado es **metáfora pura** (sello, molde, ventanilla, "la caja"/"quien la abre") y el término técnico vive en 3 palabras de `🗣️ CÓMO SE LLAMA ESTO` sin volver a aparecer donde se usa; (3) **referencias cruzadas obligatorias** (*"es el `entregarPelado` del 01 otra vez"*) que obligan a sostener 4 archivos en la cabeza; (4) **faltan datos necesarios para resolver** — la propia v1 lo admitía del drill 4 del 03: *"saber que el de React hay que importarlo — el enunciado no lo dice en ninguna parte"*. **Referencias de lo que SÍ le funciona, y que fijan la v2:** `02-funciones/exercise-07.ts` (la cafetera) y `09-react-props/exercise-05.tsx`, los dos con **teoría intercalada de verdad** (sintaxis escrita + término técnico + ejemplo ejecutable) antes de pedir **construir**.

  - **📏 ESPECIFICACIÓN DE ESTILO v2 — ⛔ SUPERSEDIDA el 11 ago 2026 por [`docs/AUTORIA-DRILLS.md`](docs/AUTORIA-DRILLS.md) (v3).** Se conserva aquí como histórico: explica de dónde vienen los campos del enunciado que la v3 retira. Lo que sigue vigente de ella —cabecera de 40 líneas, bloques de teoría intercalados, un concepto por archivo, starters rotos, forma del objeto dibujada— está recogido en la v3; lo que muere son los campos del **enunciado** (`QUÉ CONSTRUIR`, `🔧` con el error literal, `📎`, `→ resultado`). Se conserva todo el "FUERA" de la v1 —cajas ASCII de dibujo, inventarios de propiedades, códigos de error en la cabecera, diagramas de más de una rama, enunciados escritos como firma de tipos, bloques de vocabulario largos— y cambia el "DENTRO":
    - **Cabecera: sigue con techo de 40 líneas.** La teoría extra NO engorda la cabecera, baja a los bloques. Orden: `📌 RECORDATORIO` (≤16 líneas, solo lo que ese archivo usa; si manipula un objeto, su FORMA dibujada) + **`🎯 AL TERMINAR SABRÁS` (NUEVO** — 2-4 viñetas comprobables, en infinitivo) + `🟢 ¿POR QUÉ ESTE ARCHIVO?` (2-4 frases, segunda persona) + **`🗺️ MAPA DEL ARCHIVO` (NUEVO** — `TEORÍA 1 → drills 1-2 · TEORÍA 2 → drills 3-4 …`) + `▸ EJERCICIO` (prohibido `any`/`as` + comando del test).
    - **Bloques de teoría INTERCALADOS: 2 o 3 por archivo, ~20-25 líneas, cada uno pegado a los drills que lo usan.** Estructura fija: `DEFINICIÓN` (una frase, en términos técnicos) · `SINTAXIS` (la forma escrita, con las piezas señaladas) · `EJEMPLO` (código mínimo ejecutable + su resultado) · `🧠 ANALOGÍA` (una, **de APOYO — ya no sustituye a la definición**) · `🗣️ LAS PIEZAS` (pieza → nombre técnico) · `⚠️ TRAMPA` (el error concreto que va a cometer, y por qué).
    - **Enunciados: orden invertido respecto a la v1.** Primero **QUÉ CONSTRUIR**, en términos técnicos y con las piezas por su nombre; después, y **anunciado con `🔧`**, qué hace mal el starter y qué error verá. Nunca al revés.
    - **Reglas duras que la v1 incumplía:** cero referencias cruzadas obligatorias (si un drill repite un patrón de otro archivo va como nota al margen, jamás como el enunciado) · **si un drill obliga a un import nuevo, el enunciado lo dice** (regla pendiente desde el 03, ahora normativa) · los códigos de error van en su drill, nunca en la cabecera · trace lines comentadas bajo cada función.
    - **Techo de archivo: sube de 160 a ~230 líneas**, que es lo que cuesta la teoría intercalada. El techo que importa no era el del archivo sino el de **densidad por concepto**, y ese se respeta mejor que nunca: 1 concepto por archivo, partido en 2-3 bloques con sus drills al lado.

  - **🧊 CONVENCIÓN CONSERVADA DE LA v1 (pedida por él el 2 ago 2026): la FORMA del objeto se dibuja en el `📌 RECORDATORIO` de cada archivo que lo manipule.** Salió de un fallo suyo en el 02: a *"¿de qué tipo es `e`?"* contestó *"string, porque el valor era `click`"* — respondió por `e.type` (el campo) creyendo responder por `e` (la caja). El material tenía la culpa: listaba propiedades sueltas sin enseñar nunca **la caja que las contiene**. Formato, ~9 líneas: `e = { … }` con una flecha `←` por campo y la primera marcando *"la caja entera es `e`"*. **Reglas para no reincidir en el inventario de 30 propiedades (que sigue PROHIBIDO):** solo los campos que ese archivo usa, valores de ejemplo reales, y un `(recortada, trae ~25 campos)` que diga la verdad. **Distinción preservada a propósito:** el dibujo enseña la **forma en runtime** (valores), NO el **nombre del tipo** — ese sale del hover, que es la pregunta de razonamiento.

  - **🎓 STARTERS ROTOS A PROPÓSITO — se mantienen, pero ANUNCIADOS (decisión suya, 8 ago 2026).** El patrón de la v1 era bueno y el compilador es el mejor profesor de la casa: el starter del drill 2 del 01 devuelve la receta donde pedían el plato (`TS2322 "Type '() => string' is not assignable to type 'string'"`) y el del 3 hace justo lo contrario. **El mensaje de TS ES la lección.** Lo que cambia en la v2 no es el starter, es **quién lo anuncia**: en la v1 el defecto iba escondido tras un `👉` que se lee como pista opcional y que además *hacía de enunciado*; en la v2 el enunciado dice primero qué construir y el `🔧 STARTER ROTO A PROPÓSITO` avisa después. Ventaja de diseño que se conserva: los starters USAN sus parámetros, así que no hay `no-unused-vars` que silenciar.

  - **⚠️ HALLAZGO DE DISEÑO QUE SE ARRASTRA A LA v2: del 03 en adelante los tests dejan de bastar.** En el 01 y el 02 los 6 starters salen rojos; a partir del 03 no puede ser, porque **un tipo mal puesto no rompe nada al ejecutar**. Solución adoptada, no ocultarlo: cada archivo afectado lleva en la cabecera qué drills salen **verdes de mentira** y cuántos errores levanta `pnpm typecheck`, con la orden de correr **los dos** comandos. **Al montar cada archivo, comprobar drill a drill que su starter tiene señal en ALGÚN sitio** — en la v1 el drill 5 del 03 salió con la solución escrita, y el 4 del 04 no tenía ni test rojo ni error de tipos (mirror de tipo de retorno: **`(e) => string` SÍ es asignable a un manejador `=> void`**; se cambió a un parámetro de más, que sí da `TS2322 "Target signature provides too few arguments"`).

  - **📖 Protocolo de verificación de cada archivo (sin cambios desde la v1, funciona):** solución completa escrita → `pnpm test:run <archivo>` **6/6 verde** + `pnpm typecheck` exit 0 + `pnpm lint` 0 → cuerpos vaciados a los starters rotos → **rojos**, con mensajes autoexplicativos → comprobar que los errores de typecheck del starter son **exactamente** los que anuncia cada `🔧`, ni uno más. ⚠️ `pnpm typecheck` es `tsc -b`; `tsc --noEmit` a secas no comprueba nada y miente con código 0 (ver [[project_05_typecheck_comando]]).

  - **exercise-01 v2 (entregar una función no es ejecutarla) — MONTADO 8 ago 2026, 6 drills, 250 líneas, starter listo, pendiente de resolver.** Sigue siendo `.ts`, sin React, a propósito: la extensión cuenta la verdad y así el archivo no necesita **ni una línea de `eslint-disable`** de andamiaje. Estructura v2: `📌 RECORDATORIO` (el gesto en dos líneas + la forma de `{ type: "click" }` dibujada, que es la caja del drill 6) · `🎯 AL TERMINAR SABRÁS` (3 viñetas) · `🗺️ MAPA` · **TEORÍA 1** la función es un VALOR (`f` vs `f()`, el tipo de cada uno) → drills 1-3 · **TEORÍA 2** el CALLBACK: quién llama y quién pone el argumento, encajar pelado vs. envolver → drills 4-5 · **TEORÍA 3** cuando lo que llega es un OBJETO → drill 6, que cierra con un `🔭 LO QUE VIENE` apuntando al 02. Los seis drills de la v1 se conservan (la escalera era buena); lo que cambia es el enunciado y el reparto de los starters rotos.
    - **✅ Protocolo verificado (8 ago 2026), con los números exactos.** Solución escrita → `pnpm test:run` **6/6 verde** + `pnpm typecheck` **exit 0** + `pnpm lint` **0** → cuerpos rotos a los starters → **6/6 rojos** con mensajes autoexplicativos (`expected "vi.fn()" to not be called at all, but actually been called 1 times` · `expected [Function] to be 'café'` · `TypeError: fn is not a function` · `expected "vi.fn()" to be called with arguments: [ 5 ]`), **7 errores de typecheck** y **lint sigue en 0 sin ningún `eslint-disable`**.
    - **Los 6 starters rotos, cada uno con su señal** (todos rojos por test **y** por tipos, que en este archivo aún se puede): 1 `[receta()]` guarda el retorno → `TS2322 string` no es `() => string` · 2 `return receta` devuelve la función donde piden el retorno → `TS2322` · 3 `return receta()` **el espejo exacto del 2**, con los dos lados cambiados · 4 `llamaConTexto(avisar())` → **el único con DOS errores en la misma línea** (`TS2554 Expected 1 arguments, but got 0` + `TS2345 'void'` donde se pedía la función), y es literalmente el `onClick={avisar()}` del 02 · 5 `llamaConTexto(avisarLargo)` pelada sin encajar → `TS2345` con el subrayado que importa, `Types of parameters 'n' and 't' are incompatible` · 6 `avisar(suceso)` **la caja entera donde piden el campo** → `TS2345 '{ type: string; }'` no es `string`, que es **su confusión objeto-vs-propiedad puesta a propósito en el último drill**.
    - **✅ CERRADO 6/6 el mismo día que se montó (8 ago 2026), typecheck 0, lint 0, sin una sola pista pedida.** Resolvió los 6 de corrido. **El formato v2 se validó sobre la marcha por la vía que importaba: el archivo creció 35 líneas durante la sesión porque él preguntó tres cosas que el material no contestaba**, y las tres eran defectos reales, no dudas suyas: (1) la `DEFINICIÓN` de orden superior decía solo *"recibe otra función"*, con lo que `entregar` —que la devuelve— no encajaba; (2) las firmas de los ayudantes no estaban al lado de los drills que las necesitan (**se dio cuenta él solo: se copió `llamaConTexto` comentada junto al drill 4** — es [[feedback_type_reminder_local]] aplicado por su cuenta, y ahora las tres firmas van en su `📎` dentro del enunciado); (3) faltaba **por qué el callback tiene que ser una función y no el dato ya calculado** — el bloque `⏳` nuevo: cuando escribes `llamaConTexto(…)` el `"click"` todavía no existe, nace dentro después, así que no hay nada que calcular y lo único entregable es una instrucción con un hueco. Archivo final: **285 líneas** (el plan decía ~230; se le avisó del desvío y de que se recorta si al releerlo le pesa).
    - **➕ BLOQUE R DE REFUERZO (8 drills más, 7→14), pedido por él tras cerrar los 6.** Petición literal: *"expandir este archivo con ejercicios de `llamado((t) => llamadoDos(t.loQueSea))`"*, y **nombró él mismo sus dos techos: `void` en un proyecto real y "entender una función de arriba a abajo"**. Se comprobó qué había ya: `02-funciones/exercise-05` cubre `void` —**y su `🔧 POR DENTRO` ya trae la regla exacta de asignabilidad, en UNA línea y sin un solo drill que la practique**, por eso no cuajó— y `02-funciones/exercise-06` cubre callbacks, pero ahí **el gatillo lo aprieta él (`fn(x)`) o el callback transforma-y-devuelve (`.map`)**. La forma que pedía —el envoltorio que **enruta una pieza hacia otra función**— no está en ninguna carpeta. **Los dos techos son el mismo concepto:** `void` es lo que permite que el envoltorio trague cualquier retorno, y por eso el compilador no avisa cuando te equivocas dentro. Por eso el refuerzo va en el 01 y no en archivo aparte (ver [[feedback_drills_en_escalera]]).
      - **Escalera del bloque R:** 7 transformar (`.toUpperCase()`) · 8 sacar campo de un objeto · 9 **el mismo objeto, otra pieza — la firma de destino decide cuál** · 10 campo anidado (dos puntos; el drill 6 un nivel más abajo) · 11 combinar dos campos · 12 usar los dos argumentos que pasa el llamador · 13 **declarar MENOS parámetros de los que te pasan es legal** · 14 **`void` acepta cualquier retorno y por eso no te protege**. Añade `type Usuario` y dos ayudantes: `llamaConUsuario` y `llamaConDosDatos`.
      - **Novedad de diseño: `▸ TEORÍA R` enseña el PROCEDIMIENTO de leer una firma en 4 pasos**, con la regla que responde a su techo — *cuando un parámetro es una función, no sigas leyendo en horizontal: baja un nivel y vuelve a empezar. Una firma no es una línea, son capas.*
      - **⚠️ Y aquí el typecheck deja de bastar por primera vez en el archivo:** de los 8 starters nuevos solo 3 dan error de tipos (8, 9, 10); los otros 5 fallan por LÓGICA y los caza solo el test. Va avisado en la cabecera, y es a propósito porque es el tema del drill 14.
      - **🔁 El paso 4 del protocolo volvió a cazar mentiras, y esta vez tres.** Los starters de los drills 12, 13 y 14 dejaban parámetros sin usar → `TS6133` + 2 errores de `lint` de ruido, y **dos `🔧` anunciaban "typecheck calla" cuando no callaba**. Se rediseñaron los tres (se intercambiaron 12 y 13 para que la escalera fuera *usar los dos → poder declarar menos*, que además es mejor orden didáctico). **Verificación de la versión resuelta hecha en un par de archivos temporales `_verify-refuerzo.*` en vez de mutando su archivo**, porque estaba editándolo en vivo: 8/8 verde, typecheck limpio, y borrados después.
      - **🗑️ SE TIRÓ `llamaConDosDatos` Y ENTRÓ `.forEach` DE VERDAD (12 y 13), y el detonante fue él enfadándose.** Dijo *"función culia, puro relleno"* al llegar al 13 — **y tenía media razón: el ayudante era un juguete inventado, y cuando el que llama es de mentira el drill parece de mentira.** El concepto NO era relleno (es literalmente `.map(parseInt)`), pero el envase sí. Cambio: `avisarRepetido`/`avisarSoloElTexto` → **`listarConPosicion`/`avisarCadaColor` sobre `COLORES.forEach`**, que pasa `(valor, índice, arrayCompleto)`. Ahora el 12 usa dos de los tres (y el starter falla por el off-by-one real: `.forEach` cuenta desde 0, la posición que lee una persona empieza en 1) y el 13 declara **uno solo**, con un `💡` que conecta explícito: *esto es lo que arregla tu bug de julio*. **Regla que deja el episodio: si un drill necesita un "llamador", usar uno REAL del lenguaje antes que inventarse un ayudante.** Verificado igual que antes, en temporales: 12 y 13 resueltos 2/2 verde y tipando limpio.
      - **🔴 Su comentario de estudio en el drill 7 estaba conceptualmente mal, y era LA misconception.** Escribió: *"Primero se ejecuta llamaConTexto, que **retorna** "click", y se pasa a avisar que **retorna** "CLICK""*. Ninguna de las dos retorna: **las dos son `void`**; lo que hay son tres LLAMADAS y cero retornos. Es el *"retorna"* usado para lo que en realidad **llega**, de [[project_05_diccionario_vocabulario_ts]] — y esta vez **escrito en sus propios apuntes**, donde lo releería dentro de un mes creyéndoselo. Se le corrigió a petición suya con las tres llamadas numeradas y la frase *"aquí nadie retorna, todos llaman"*. **Conexión que se le hizo explícita: si todo "retorna", `void` no puede significar nada — o sea que su techo del `void` y su vocabulario son el mismo problema.**
      - **✅ Los `📎` de firmas están ya en LOS 14 DRILLS (pedido suyo, 8 ago).** Nacieron porque **se copió `llamaConTexto` comentada junto al drill 4**, y volvió a hacerlo dos veces más en el bloque R: tres veces el mismo gesto. **Formato: `← lo que se PIDE` / `← lo que TIENES`, las dos firmas enfrentadas.** En los drills 1-3, donde no hay ayudante externo, el `📎` enfrenta el **parámetro contra el tipo de retorno** — que es exactamente la comparación de papel-vs-plato. En los que usan `Usuario` se repite la forma del tipo entera, y en el **14 el `📎` lleva la lección dentro**: *"el parámetro casa, el retorno NO casa — y da igual, porque `void` no mira"*. **Regla nueva para los archivos 02→10: si un drill necesita comparar dos firmas, las dos van en el enunciado; nunca obligar a subir a buscarlas.**
      - **📏 El archivo queda en 444 líneas** — a 31 del `exercise-01` de 475 que provocó la primera reconstrucción. **La diferencia es real y hay que sostenerla o partir el archivo:** aquel tenía **6 conceptos apilados** y 99 líneas de referencia antes de la primera línea de código; este tiene **1 concepto**, 4 bloques de teoría intercalados y 14 drills. Aun así se le avisó del número y se le ofreció partirlo — **decisión suya pendiente**.
    - **🎓 Preguntas suyas de esta sesión, las tres para reusar.** (a) *"`entregar(() => "café")()` ¿qué nombre tiene esto?"* — **pidió la ETIQUETA, que es justo lo que se le estaba señalando que le faltaba**: llamadas encadenadas, dos operadores de llamada, y `entregar` es de orden superior por devolver función. Puente que funcionó: es la forma de `multiplicador(3)(5)` de `02-funciones/exercise-07`, que ya había escrito. (b) *"¿qué diferencia hay entre `llamaConTexto(avisar)` y `llamaConTexto((t) => avisar(t))`?"* — aquí ninguna funcional; la diferencia es **control sobre lo que llega**, y la regla no es "pelada mejor": se cerró con **su propio `["10","10","10"].map(parseInt)` → `[10, NaN, 2]`**, el caso donde pelar traiciona porque `.map` pasa tres argumentos. (c) *"¿por qué no me deja `llamaConTexto(dato.length)`?"* — **la mejor de las tres**, y es su pregunta de julio (*"¿de dónde sale ese 5?"*) por el otro lado: no es que el tipo esté mal, es que **el dato aún no ha nacido** cuando escribes esa línea.
    - **📌 Cierre de la confusión objeto-vs-propiedad, con evidencia.** En el 02 de la v1 contestó *"string, porque el valor era `click`"* al preguntarle el tipo de `e`. En el drill 6 de la v2 escribió `avisar(suceso.type)` a la primera, con el starter dándole la caja entera delante. **Mismo error, mismo sitio, resuelto.**
    - **⚠️ Los `🔧` se corrigieron contra la salida real del compilador, no al revés.** Dos no coincidían con lo que escupe `tsc`: el del drill 4 anunciaba un solo error y son dos, y el del 5 tenía los parámetros en el orden equivocado (`'t' and 'n'` cuando `tsc` dice `'n' and 't'`). **Es el paso 4 del protocolo y hay que repetirlo en los 9 archivos restantes:** si el `🔧` no cita el error literal, el archivo vuelve a mentirle.

  - **exercise-02 v2 (el evento llega solo — TIPADO CONTEXTUAL) — MONTADO 8 ago 2026, 6 drills, 223 líneas.** Primer `.tsx`. `📌 RECORDATORIO` con **los dos `e` dibujados** (el de `onClick` y el de `onKeyDown`, para que se vea que `key` solo lo trae uno). **TEORÍA 1** el tipo lo pone el HUECO —definición de tipado contextual, y el dato que quita la magia: *está declarado en `@types/react`*— → drills 1-3 · **TEORÍA 2** cada hueco trae SU evento, y `e` es un ENVOLTORIO (`e.nativeEvent`) → drills 4-6. Escalera: pelado · `e.type` · `onDoubleClick` · `onKeyDown`→`e.key` · `e.nativeEvent` (**los dos homónimos cara a cara**) · envolver para combinar props + evento. **Verificado:** resuelto 6/6 verde + typecheck 0 + lint 0 → starters 6/6 rojos con **4 errores de tipos** (los drills 3 y 6 fallan solo por lógica, y la cabecera lo dice).
    - **✅ CERRADO 6/6 el mismo día que se montó (8 ago), typecheck 0, lint 0, sin pistas hasta el drill 6.** En el 6 se atascó y la pista que lo desbloqueó fue **señalarle los paréntesis vacíos del envoltorio** (`onClick={() => …}`) y conectarlos con el drill 13 del 01 al revés: *allí aprendiste que puedes declarar menos parámetros de los que te pasan; aquí has declarado cero, así que se descarta el evento entero*. Lo resolvió nombrando el parámetro **`evento`** en vez de `e` — y eso mismo **demuestra la `⚠️ TRAMPA` del archivo: el tipo no viene del nombre, viene del sitio.**
    - **🎓 Se le contestó por fin QUIÉN tipa `e`** (pregunta abierta desde el 02 de la v1, donde dijo "no lo sé", y que se saltó dos veces más en esta sesión). **No se le dio como definición sino como sitio abrible:** ctrl+clic en `onClick` → `onClick?: MouseEventHandler<T>` en `DOMAttributes<T>`, y `type MouseEventHandler<T> = (event: MouseEvent<T>) => void`. **Regla de material que confirma: cuando la respuesta es "está declarado en algún sitio", dar el sitio, no la frase.**
    - **⚠️ AVISO DE MATERIAL PARA TODOS LOS `.tsx` DEL BLOQUE: las trace lines en `.tsx` NO son como las del `.ts`.** Descomentó tres (`<BotonAvisaTipo avisar={…} />`) esperando salida, y **no imprimen nada**: escribir la etiqueta solo fabrica un objeto que describe el elemento, el componente no se llama, y ni `lint` ni `typecheck` protestan porque no está mal escrito — simplemente no hace nada. **Es `avisar` vs `avisar()` un piso más arriba**, y esa equivalencia (`<Boton />` es la receta; renderizar es cocinar) hay que **escribirla en la cabecera de los `.tsx`**, no dejarla en el `📝` de una línea que solo dice "coméntalas".

  - **exercise-03 v2 (el manejador FUERA del hueco) — MONTADO 8 ago 2026, 6 drills, 236 líneas.** Analogía conservada: **la ventanilla**. **TEORÍA 1** fuera no hay contexto → anotas tú, **y el `import type { MouseEvent, KeyboardEvent } from 'react'` va en la sección SINTAXIS, no como nota al pie** (deuda saldada de la v1: el drill que costó cuatro pasadas por no decirlo). **TEORÍA 2** el argumento de tipo dice sobre QUÉ elemento, con la trampa del `<T = Element>` **planteada como experimento suyo, no como afirmación**. Escalera: sacar y anotar (`TS7006`) · `<a>`→`HTMLAnchorElement` · teclado (**el enunciado avisa de que exige tocar el import**) · el de fuera pide 2 cosas → envolver EN el hueco · `e.currentTarget.value` **sin argumento de tipo** (la trampa, como drill) · dos elementos con dos carteles. **Detalle real que salió montándolo:** el drill 5 se cambió de `onKeyDown` a `onKeyUp` porque **en el `keyDown` la tecla aún no ha entrado en el campo** y `currentTarget.value` va una letra por detrás — el test de la solución lo cazó. **Verificado:** resuelto 6/6 verde → starters con **7 errores de tipos y solo 2 tests rojos**; los otros 4 salen VERDES con el cartel mal puesto, avisado en cabecera con la orden de correr los dos comandos.
    - **✅ CERRADO 6/6 el mismo día que se montó (8 ago), typecheck 0, lint 0, sin pistas. Y con el episodio que justifica el archivo entero.** Al llegar al drill 6 tenía **`test: 5 passed` y `typecheck: 2 errores`** — o sea, **uno de sus cinco verdes era mentira**: el drill 5 pasaba el test con `KeyboardEvent` sin argumento de tipo. Se le mostró la discrepancia entre los dos comandos como dato, no como regaño (*"si hoy solo hubieras corrido el test, habrías dado por bueno un drill que está mal"*), y al arreglarlo **los dos comandos volvieron a señalar el mismo drill** — esa convergencia se le dio como señal de que lo anterior estaba realmente curado. **Además lo arregló POR LA CAUSA** (rellenar `<HTMLInputElement>`) y no silenciando (quitar el `.value`), que era la otra salida que también dejaba el error en cero.
    - **🎓 Tres preguntas suyas de este archivo, las tres de aplicación real y ninguna en el guion.** (a) *"`<button>` es para movimientos internos de la App y `<a>` para redirigir fuera"* → **medio correcto y el fallo importa:** el eje no es dentro/fuera sino **acción vs. navegación**; el contraejemplo lo tenía en su propio drill 2 (`<a href="/inicio">`, ruta interna) y en el `<Link>` de React Router, que renderiza un `<a>`. Se cerró con lo que se rompe al usar un botón para navegar (ctrl+clic, abrir en pestaña nueva, copiar dirección, el anuncio del lector de pantalla) y la regla *¿cambia la URL? → `<a>`*. (b) *"`onKeyUp` salta cuando el usuario sale del input"* → **eso es `onBlur`**; son las dos mitades de la MISMA tecla, y entre ellas el navegador inserta el carácter — de ahí que el valor vaya una letra por detrás en `keyDown`. (c) **La mejor:** *"para una app de traductor se usaría `onKeyDown`, para tener la respuesta lo más rápido posible"* → se le dio la respuesta larga porque es material de entrevista: con `keyDown` traduces **siempre el estado anterior**, y **te pierdes el pegado, el dictado y el autocorrector**; lo que se usa es **`onChange` + debounce**, y sin debounce aparece la **race condition** (la respuesta de "hol" pisando la de "hola"). Regla que se le dio: *el evento decide cuándo te enteras; el debounce decide cuándo actúas*.
  - **exercise-04 v2 (ponerle NOMBRE al tipo) — MONTADO 8 ago 2026, 6 drills, 255 líneas.** Analogía conservada: **el sello de goma**. **TEORÍA 1** el alias de tipo, con el hallazgo que lo hace valer la pena: **al poner el alias en el `const` vuelve a haber CONTEXTO, así que la `e` se tipa sola otra vez** — el tipado contextual del 02, pero con un contexto que escribió él. **TEORÍA 2** los alias de fábrica, con **la regla del nombre como la pieza que se lleva: `XEvent<T>` es el evento, `XEventHandler<T>` el manejador; la terminación `Handler` es toda la diferencia**, y vale para la familia entera. Escalera: tallar el alias · uno por elemento (`<a>`) · el del teclado · el de React · la regla del nombre aplicada él solo · capstone mezclando el suyo y el de React para ver que son intercambiables. **Detalle bueno que salió de la verificación: al usar alias, los mensajes de error pasan a decir `Type 'ManejadorDeClic' is not assignable to…` — o sea que ponerle nombre a un tipo también se lo pone a los errores.** Se le señala en el drill 1, y en el 2 la ironía de que el alias se *llame* `ManejadorDeEnlace` y por dentro diga botón. **El drill 5 exige añadir un import él mismo** (primera vez en el bloque), avisado en el enunciado. **Verificado:** resuelto 6/6 verde → starters con **11 errores de tipos y solo 2 tests rojos** (1, 2, 4 y 5 salen verdes).
  - **exercise-05 v2 (el manejador llega por PROPS) — MONTADO 8 ago 2026, 6 drills, 238 líneas.** Analogía: **el enchufe y el aparato** — el hijo pone la toma y no sabe qué le conectas; el padre trae el aparato. **TEORÍA 1** una prop cuyo valor es una función (y el alias del 04 es lo que se escribe en su tipo) · **TEORÍA 2** cuando el hijo tiene algo que aportar (envolver para añadir su `id`). Escalera: recibir y entregar pelada · otro elemento, otro alias · el hueco correcto · envolver para añadir lo suyo · dos manejadores por props · capstone. **Verificado:** 6/6 verde → starters con **6 errores de tipos y 5 tests rojos**; el único verde de mentira es el drill 2.
    - **🪜 PRIMER PELDAÑO DE LA ESCALERA DEL `useState`, y funciona como se diseñó.** El drill 6 trae un padre **ya escrito y marcado como intocable** (`PanelDeConteo`) que guarda un número con `useState` y le pasa al hijo una función que lo sube; él solo escribe el hijo. El test renderiza **el padre** y comprueba que tras tres clics pone "Total: 3". Así el `useState` **se ve pasar sin estudiarse**, que era exactamente lo que él pidió.
    - **🎯 El starter del drill 6 es el mejor del bloque hasta ahora:** `onClick={() => alPulsar}` — entrega la función en vez de llamarla, así que al pulsar no pasa nada. **`typecheck` se queda CALLADO**, porque el hueco es `=> void` y ahí cualquier retorno vale, incluido devolver una función. Es **el drill 14 del `exercise-01` reapareciendo dentro de un componente real**, y el enunciado lo dice: *la permisividad de `void` es la que te deja sin red*.
    - **🔁 El paso 4 volvió a cazar mentiras en los dos archivos.** En el 02: el `TS2322` real dice `'MouseEventHandler<HTMLButtonElement> | undefined'` (el `| undefined` porque `onClick` es opcional) y yo había escrito el tipo sin él. En el 03: el drill 3 da **dos** errores, no uno. **Y en los dos archivos un starter dejaba una prop sin usar** (`id`), metiendo un `TS6133` + error de lint que no pintaba nada — se arregló dándole al elemento su propio `id={id}`, que además es lo natural. **Patrón ya confirmado tres veces: cuidado con los starters que dejan props huérfanas.**

  ### Sesión del 9 ago 2026 — se cierran el 03 y el 04

  **Resultado: bloque de 26/38 tests y 14 errores de tipos → 35/44 y 6.** Lint 0 todo el rato. Los 6 errores que quedan son **todos del `exercise-05`**, que sigue intacto. Falta también el bloque R del `01` (drills 11-14).

  - **`exercise-03` — CERRADO 6/6, typecheck 0.** Solo quedaba el drill 6, y era de tecleo, no de concepto: escribió `` `Boton:${e.type}` `` con mayúscula donde el test pedía `boton:`. Los dos manejadores ya estaban bien tipados y cada uno colgado de su elemento. **Cerró el archivo entero sin una sola pista de código.** Las 6 líneas de trazado se comentaron al cerrar (queda una sin su resultado, la del `BarraFuera`, a propósito para que la rellene él).

  - **⚠️ EL TABLERO DE TESTS MINTIÓ Y YO ME LO CREÍ.** Abrí la sesión diciéndole que tenía cerrado el drill 2 del `04` porque `vitest` lo daba verde — pero el primer `pnpm typecheck` que corrí venía truncado con `tail -30` y me perdí los errores de cabecera. Con la lista completa, el `04` estaba en **1/6 real contra 4/6 aparente: tres de los cuatro verdes eran falsos**. Corregido en caliente. **Lección de método para el coach, no para él: nunca truncar la salida de `typecheck` con `tail` antes de contar; usar `grep -E "^src/.*error TS"`.**

  - **`exercise-04` — CERRADO 12/12, typecheck 0.** Los drills 2, 3 y 4 en sesión; **el 5 y el 6 los cerró él solo mientras yo explicaba teoría de más** (incluido añadir el import de `KeyboardEventHandler`, que era la primera vez del bloque que le tocaba a él). Conceptos que articuló con sus palabras, sin que se los diera: **type erasure** (*"a la hora de compilar, la etiqueta desaparece"*, que era el porqué de los verdes de mentira), la **regla de aridad** (declarar menos parámetros de los que te pasan es legal; más, no) y la **regla del nombre** (`XEvent<T>` el evento, `XEventHandler<T>` el manejador).

  - **➕ ESCALERA T (6 peldaños, `T1`–`T6`), montada en sesión dentro del propio `exercise-04`.** Detonante: dijo **"está complicado"** después de que yo le soltara la anatomía de `const manejar: MouseEventHandler<HTMLButtonElement> = (e) => avisar(e.type)` con tabla, analogía y dos variantes equivalentes de golpe. **El diagnóstico no era el tema, era mi densidad — otra vez.** La escalera aísla una sola frase: *la etiqueta del `const` es la que le dice al parámetro qué es*. **T1–T4 sin React, sin eventos y sin JSX** (`doblar`/`triplicar`/`gritar`/`repetir`: números y textos) para ver la maquinaria desnuda; **T5–T6 devuelven el evento** para comprobar que no había cambiado nada. **T2 es el contraste que hace la escalera:** mismo trabajo que T1 pero con la información en el parámetro en vez de en la etiqueta — una de las dos, nunca las dos. **T6 se sembró como verde de mentira** (`const` desnudo → `TS7006`, test verde) y es el único peldaño que solo caza `typecheck`. **Los resolvió los 6**, el T6 con pista estrecha (se le acotaron los 4 nombres del import a uno con dos preguntas, sin darle la línea).

  - **🗣️ AGUJERO DE VOCABULARIO CONFIRMADO Y TRABAJADO (era el pendiente de [[project_05_diccionario_vocabulario_ts]]).** Salió de dos comentarios que escribió él en el T4: *"El type Repetidor es una función"* y *"Repetir es una variable que llama a la Función Repetidor"*. **El código estaba perfecto; los nombres, no** — describían una máquina que no existe (un alias no se puede llamar: desaparece al compilar, cosa que él mismo había explicado media hora antes). Se le enseñó la tríada con las tres líneas al lado: `type X = …` **alias de tipo** · `const f: X = …` **constante** · `f(arg)` **llamada**, con la señal de que **una llamada lleva paréntesis con algo dentro** — que es literalmente el `exercise-01` de este bloque, entregar ≠ ejecutar, en vocabulario. **Drill de nombrar piezas con banco de palabras: acertó 4/5.** El fallo fue el par de siempre: llamó *parámetro* al `"Ana"` de `saludar("Ana")`. Regla dada, puramente de sitio: **parámetro = el hueco, en la declaración; argumento = lo que metes, en la llamada** (la casilla en blanco contra la tinta).
    - **Pregunta suya excelente, y generaliza:** *"¿una prop para un componente es un argumento?"* Respuesta con el desazucarado delante: `<Saludo nombre="Ana" edad={30} />` → `Saludo({ nombre: "Ana", edad: 30 })`. **React llama al componente con UN solo argumento, que es un objeto**; las props son **propiedades** de ese objeto y el hueco donde aterriza es el **parámetro** (por eso `({ avisar })` es un parámetro desestructurado). Cierra el trío parámetro/argumento/propiedad y enlaza con la trampa objeto-vs-propiedad (`e` vs `e.type`) que apareció **tres veces el mismo día en tres sitios distintos**.

  - **🔧 CAMBIO DE CONFIG: `eslint.config.js` gana un bloque de override para `src/exercises/**`** que apaga `react-refresh/only-export-components`. Lo pidió la escalera T (exporta funciones normales —`doblar`, `gritar`…— desde un `.tsx` que también exporta componentes), pero **el proyecto ya venía apagando esa misma regla a mano en 7 sitios** (`09-react-props/exercise-07/08/09/10` y `00-evaluacion/parcial-02/03`), con exactamente el mismo razonamiento escrito en el comentario. El override centraliza lo que ya era la práctica y **se borraron las 7 directivas, que quedaban muertas**. Distinción que se le explicó, porque roza lo que se le predica todo el rato: **silenciar un error que señala un defecto real es trampa; apagar una regla en los archivos donde su premisa no se cumple es configurar** — estos ejercicios no se montan nunca en la app, solo los importa `vitest`, así que no hay hot reload que proteger.

  - **📏 DEUDA: `exercise-04.tsx` se fue a ~380 líneas** con la escalera dentro, muy por encima del techo de ~230. Es el mismo caso que el `exercise-01` con su bloque R (492 líneas), y sigue pendiente la misma decisión: **partir en `04` + `04b`**. Anotado, no hecho.

  - **📌 PATRÓN DE LA SESIÓN, para el coach: arregla y no explica.** Cuatro drills seguidos entregó el código corregido sin contestar la pregunta conceptual; **cuando se le repetía, la contestaba bien y sola**. O sea que el concepto estaba: lo que no hace es sacarlo si no se le pide dos veces. Se le nombró una vez, con el encuadre útil (*un arreglo de una palabra se acierta por eliminación y desde fuera no se distingue de haberlo entendido; en una entrevista el que se lleva el puesto es el que dice por qué estaba mal*) y con las preguntas reformuladas en versión barata (huecos de una palabra) para que contestar saliera casi gratis. **Funcionó mejor que insistir.** Ver [[feedback_responde_porque_salta_campo]].

  ### Sesión del 11 ago 2026 — se arregla el GENERADOR, no lo generado (spec v3)

  **Queja suya, y es la tercera vez que el material se rompe por el mismo sitio:** *"los enunciados se han vuelto demasiado robóticos, técnicos y explícitos; no estoy resolviendo el problema, estoy intentando descifrar qué quiere el autor"*. Pidió explícitamente **no arreglar los ejercicios sino el sistema que los genera**.

  - **🔍 DIAGNÓSTICO — el trinquete.** Cada campo del enunciado nació como parche a un fallo real y **ninguno se retiró jamás**: `→ resultado` (de *"avanzando a ciegas"*), `QUÉ CONSTRUIR:` (de *"describe un defecto, no un objetivo"*), `🔧 STARTER ROTO` + error literal de `tsc` (de *"el defecto iba escondido tras un 👉"*), `📎` (de *"faltan datos para resolver"*), `⚠️ verde de mentira` nombrando el drill. Cinco parches razonables uno a uno = **un enunciado de 14-18 líneas con tres fuentes redundantes de la misma respuesta**.
  - **🔍 SEGUNDA CAUSA, estructural: la spec vivía dentro de este README.** 536 líneas / 221 KB de bitácora, con la spec v2 en 6 viñetas enterradas. Releerla es caro, así que el método real al montar un archivo acababa siendo **imitar el último escrito** — y el último siempre acumula más campos que el anterior. El trinquete se retroalimentaba solo.
  - **💡 Los dos spoilers que hacían casi todo el daño.** (1) El `🔧` con el error literal: el paso 4 del protocolo *obligaba* a citar la salida de `tsc`, pero ese mensaje nombra lo que tienes **y lo que se esperaba** (`Type 'ManejadorDeClic' is not assignable to type 'MouseEventHandler<HTMLAnchorElement>'`) — o sea, es la solución escrita, **y encima `pnpm typecheck` la iba a imprimir igual**. Spoiler a precio completo por cero información nueva. (2) El `📎` (`<button> → HTMLButtonElement` / `<a> → HTMLAnchorElement`) es literalmente la sustitución que pide el drill.
  - **💡 Y la causa de fondo: no había canal de pistas.** El contrato dice "pistas en 4 niveles, solo a petición", pero eso vive en el chat y **el archivo es lo único que él lee mientras resuelve**. Cada miedo del autor a que se atascara se descargaba en el único sitio disponible: el enunciado.
  - **📐 La prueba de que el formato bueno ya existía está en su propio repo.** `02-funciones/exercise-07.ts` (drills 4-8) y `09-react-props/exercise-05.tsx` —los dos que él marcó como *"esto sí me funciona"*— resuelven el enunciado en **3 líneas** (nombre · qué hace · resultado de ejemplo) y **no tienen ninguno de esos cinco campos**. El formato no había que inventarlo: se había perdido al añadir parches.
  - **📄 ENTREGA: la spec sale del README.** Se crean **[`docs/AUTORIA-DRILLS.md`](docs/AUTORIA-DRILLS.md) (v3, fuente de verdad del formato)**, **[`CLAUDE.md`](CLAUDE.md)** en la raíz (se autocarga cada sesión y apunta a la spec — es lo que rompe el ciclo de imitar el último archivo) y **[`docs/PLANTILLA-pistas.md`](docs/PLANTILLA-pistas.md)**. Este README queda **explícitamente degradado a bitácora** con un aviso en la cabecera.
  - **🧩 QUÉ CAMBIA EN EL ENUNCIADO.** Plantilla de **3-7 líneas sin etiquetas**, en prosa, con cuatro cosas en orden: **situación · tarea · resultado esperado · restricciones**. Y el **test de la frontera** como regla de gobierno: información *necesaria* (la que no puede obtener de ningún sitio: textos, props, valores arbitrarios del test, forma de una API externa, aviso de import nuevo) va al enunciado; información *reveladora* (la que puede deducir: expresión final, hueco concreto, operador, envoltura, tablas, errores de `tsc`) va a las pistas. Regla de bolsillo: **si al leerlo puedes teclear la solución sin pensar, es reveladora.**
  - **🧩 QUÉ NO CAMBIA.** Starters rotos a propósito, teoría intercalada por bloques, `📌 RECORDATORIO` con la forma del objeto dibujada, `🗺️ MAPA`, un concepto por archivo, techos de 40/230 líneas, analogías de apoyo, trace lines comentadas. **El problema estaba en el enunciado, no en la teoría** — y esa distinción es justo la que la v1 falló (recortó la teoría y dejó el ruido).
  - **🔧 El `🔧` hacía TRES trabajos y se reparten:** *"hay algo roto"* → una frase en la cabecera, una vez, para todo el archivo; *"este drill pasa el test con el fallo dentro"* → cabecera **en número, sin nombrar drills** (decisión suya: saber que no puede fiarse del verde sin que le señalen dónde); *el error literal de `tsc`* → **Pista 3**. Ganancia: ~5 líneas menos por drill y **cero información perdida, solo movida**.
  - **🪜 Canal de pistas nuevo: `exercise-NN.pistas.md`**, archivo hermano, con cada pista dentro de un `<details>` para que el preview de Markdown las muestre **plegadas** — abrirlas tiene que ser una decisión, no un accidente de scroll. Cuatro niveles con trabajos distintos: **1** conceptual (no nombra la pieza) · **2** concreta (nombra el mecanismo, sin escribirlo) · **3** el error literal del compilador · **Solución**.
  - **✅ El paso 4 del protocolo NO se elimina, se muda.** *"Cita el error literal que escupe `tsc`, ni uno más"* ahora se verifica sobre la **Pista 3**. El chequeo que impide que el material mienta sigue en pie; cambia dónde se guarda esa verdad. **Y se añade un paso 6, auditoría de spoiler:** leer cada enunciado tapando starter y test y preguntarse si basta para saber *qué* construir, y si dice algo que el compilador o el test ya dicen.
  - **📌 REGLA QUE GOBIERNA A TODAS LAS DEMÁS, para que no haya v4:** cuando un enunciado falle, **arréglalo, no le añadas un campo**. Si de verdad hace falta uno nuevo, hay que retirar otro. Los dos síntomas y su lectura: *"no sé qué quiere el autor"* → falta información necesaria; *"esto me lo estaba diciendo"* → falla la frontera.
  - **📊 Medición base antes de tocar `src/`: 44/44 tests verdes y 1 error de typecheck** (`exercise-05.tsx(225,36)`, suyo, sin cerrar). El retrofit de enunciados debe dejar ese recuento **idéntico**, y su diff no puede contener una sola línea de código.

  - **✅ RETROFIT DE LOS 5 ARCHIVOS MONTADOS — HECHO (decisión suya: los 5, no solo los pendientes).** Resultado verificado: **44/44 y 1 error de typecheck, idéntico a la base**, lint 0, y el diff de comentarios no tocó una sola línea de su código. **−182 líneas** en total: `01` 492→433 · `02` 244→204 · `03` 260→226 · `04` 380→357 · `05` 255→229. Los cinco llevan ya su `exercise-NN.pistas.md` con 4 niveles por drill. **Cero `TS####` en los enunciados** (queda 1 en todo el bloque, el `TS2315` de la `⚠️ TRAMPA` del `03`, que cumple la excepción de la §3 de la spec: el mensaje ES la lección y no es respuesta de ningún drill).
    - **La spec se corrigió a sí misma dos veces mientras se aplicaba, que es la prueba de que sirve.** (1) Se le añadió la **excepción de teoría** para los mensajes de compilador, después de ver que borrar el `TS2315` del `03` habría destripado la lección de "silenciar ≠ arreglar". (2) Al medir, el `TS2322` de la `⚠️ TRAMPA` del `01` **incumplía esa misma excepción recién escrita** —era literalmente el error del drill 2, tres líneas más abajo— y se reescribió sin el literal. La regla se aplica también a quien la escribe.
    - **🔎 Defecto de material encontrado de paso en el `02`: las seis trace lines estaban SIN comentar** mientras la cabecera del propio archivo explicaba, en ocho líneas, por qué van comentadas. El archivo se contradecía a sí mismo desde que se montó (8 ago) y estaba así en el commit. Comentadas. Es el ritual de cierre de [[feedback_trace_lines_comentadas]], que se saltó al cerrarlo.
    - **La escalera T del `04` casi no se tocó**, y eso también es dato: sus enunciados ya eran del formato bueno (nombre · qué hace · `👀` pregunta socrática · resultado de ejemplo). Solo se le quitó la etiqueta `QUÉ CONSTRUIR` y el `TS7006` del T6. **El formato v3 no es nuevo: es el que ya salía cuando el material se escribía sin miedo a que se atascara.**
    - **DEUDA que el retrofit NO salda:** `exercise-01` (433) y `exercise-04` (357) siguen por encima del techo de ~230. Recortar los enunciados no bastó — lo que pesa son los 14 drills y la escalera T, o sea el contenido. La decisión de partir en `01b`/`04b` sigue abierta.

  - **🆕 `exercise-06` (onChange y el texto del campo) — MONTADO 11 ago 2026, PRIMER ARCHIVO NATIVO v3, 220 líneas, pendiente de resolver.** `📌 RECORDATORIO` con la forma del `ChangeEvent` dibujada (`e.target.value`, dos pisos). **TEORÍA 1** el evento que trae el texto —`onChange` dispara con cada tecla, `value` es el campo entero y **siempre `string`**— → drills 1-3 · **TEORÍA 2** el campo CONTROLADO, con `useState` dado y el circuito en dos mitades → drills 4-6. Analogías: la portería (el aviso no es la carta) y la pizarra de la cocina. **Métrica v3 clavada: enunciados de 3 a 5 líneas, cero `TS####`, cero etiquetas.**
    - **🪜 Segundo peldaño del `useState` cumplido:** el `useState` viene dado y **él escribe la primera llamada a `setX`** (drill 4). Del 5 en adelante monta el circuito entero: `value` + `onChange`.
    - **🎯 EL MEJOR STARTER DEL BLOQUE HASTA AHORA, y salió por accidente al verificar.** Drill 2, `<input type="number">` con `avisar(e.target.value * 2)`: **el test PASA** porque JavaScript coacciona `"21" * 2` a `42` por su cuenta, y typecheck lo rechaza con `TS2362`. Es la demostración más limpia de todo el bloque de que un verde no es una aprobación — y con `+` en vez de `*` ese mismo apaño habría dado `"212"`. Recogido en su Pista 3.
    - **🔁 El paso 4 volvió a cazar una mentira, esta vez del recuento:** la cabecera anunciaba **1** verde de mentira y son **2** (drills 2 y 3). Corregido contra la salida real, no al revés.
    - **🔧 Y el paso 4 cazó también el patrón reincidente por CUARTA vez: starters que dejan variables huérfanas.** Los del 5 y el 6 levantaban `TS6133` (`texto` sin leer, `e` sin usar), lo que (a) señalaba con el dedo dónde estaba el fallo y (b) **desmentía la cabecera**, que promete que esos dos typecheck no los caza. Rediseñados: el 5 gana un `<p>{texto}</p>` —que además hace el fallo más didáctico, porque el estado SÍ sale en mayúsculas mientras el campo no— y el 6 pasa a `onChange={() => setNombre(nombre)}`, **sin declarar el parámetro**, apoyándose en la regla de aridad que él dedujo en el 01. Cero ruido y typecheck mudo de verdad.
    - **Señal comprobada drill a drill:** 4 errores de tipos (1, 2, 3, 4) y 4 tests rojos (1, 4, 5, 6) → **cada drill tiene señal en algún sitio, y en dos sitios distintos según el drill**. Es el archivo que mejor justifica la orden de correr los dos comandos.
    - **Deuda de diseño sembrada a propósito:** los 6 drills usan `e.target` sin justificar por qué no `e.currentTarget`, que es el que usó en el 03. El cierre del archivo lo señala y lo deja abierto para el 07, que es exactamente su tema.

  - **🗃️ ARCHIVO DE HALLAZGOS DE LA v1 (29 jul – 3 ago 2026) — es el material que siembra las secciones `⚠️ TRAMPA` de la v2.** La v1 se descarta como formato, pero lo que se aprendió sobre **cómo falla él** es lo más caro de este bloque y se conserva entero:
    - **Confusión objeto-vs-propiedad (la más cara, reincidente).** `e` vs `e.type`, en las dos direcciones: respondió *"string"* por el tipo de `e` (02), y al revés en el 03 preguntó si el `e` de dentro del botón era distinto del de fuera → **medio acierto**: variables distintas SÍ, objeto distinto NO, es la misma caja pasando de mano en mano y el tipo es idéntico. Matiz fino que se le dio: **los dos `e` no se tapan** (ninguna función está dentro de la otra), **los dos `id` sí**. Ver [[project_05_diccionario_vocabulario_ts]].
    - **"Silenciar ≠ arreglar" (lo más rentable del bloque).** `TS2315 "Type 'KeyboardEvent' is not generic"` admite DOS lecturas y las dos callan el mensaje: *"quita los `<…>`, que sobran"* (lo que hizo) y *"has cogido el `KeyboardEvent` equivocado"* (lo que decía). Quitó el genérico y el error **no desapareció: se movió** tres líneas abajo. Regla que se le dio y que hay que repetir: **si no entiendes por qué se fue un error, no se fue** — conectada a por qué la carpeta prohíbe `any` y `as`: **son el mismo botón, más grande**.
    - **El verde que no caza NINGÚN comando.** Escribió `e: KeyboardEvent` sin el argumento de tipo y **`typecheck` dio 0 y el test verde**, con el tipo a medias. Motivo: `interface KeyboardEvent<T = Element>` (`@types/react`) — si no rellenas el hueco se rellena solo con `Element`, y un manejador que acepta *cualquier* elemento encaja donde piden uno de `<input>` (parámetro en posición contravariante: quien acepta más, cabe). **No se le afirmó, se le demostró:** meter `e.currentTarget.value` y ver que `.value` no existe sobre `Element`. **Reusar el experimento en el 06 y el 07**, que viven de `.value`.
    - **Los DOS homónimos.** El `MouseEvent`/`KeyboardEvent` global del DOM no es el de React, no es genérico (`TS2315`) y no trae `nativeEvent`. **El global NO es el malo: él ya lo usó BIEN** tipando lo que sale de `e.nativeEvent`. Regla dada: *dentro del JSX, el sobre; fuera del JSX o dentro del sobre, la carta.*
    - **El slip del capstone, reincidente.** En el último drill pone bien la pieza nueva y se le cae una vieja que había escrito perfecta media hora antes (se le olvidó `preventDefault()`). Mismo patrón que el capstone de `06-generics-basicos/exercise-08`: **la atención se va a la pieza nueva y la vieja se cae sola.** Truco dado: si el enunciado enumera tres cosas, contarlas en el código ANTES de correr el test. Ver [[feedback_slip_map_noop_y_tipo_retorno]].
    - **Vocabulario TS: avance medible.** Abrió con *"esto de `MouseEvent<HTMLButtonElement>` me sigue costando, pero con la práctica iré mejorando"* → se le cortó el "ya vendrá con la práctica" (repetir lo que no entiendes construye la costumbre de copiarlo): no era dificultad, eran **piezas sin nombre**. Puentes que funcionaron: **`string[]` ES `Array<string>`** (o sea que ya escribe genéricos a diario) y **`()` para argumentos de valor, `<>` para argumentos de tipo — la misma operación, distinto paréntesis**. Media hora después nombró las tres piezas a la primera: **parámetro / tipo genérico / argumento de tipo**. **Detector reusable: cuando traduce ("un array de strings") lo tiene; cuando repite el nombre en inglés y rellena con lo que sí sabe ("un tipo `FocusEvent` como el de CSS…"), no lo tiene.**
    - **Su mejor pregunta del bloque, para reusar:** *"¿de dónde sale ese 5 si en ningún lado del llamado ingresé texto?"* — **el argumento lo pone quien LLAMA, no quien escribe la función**. Es literalmente `onClick={(e) => …}` con React rellenando el hueco.
    - **Analogías de la v1 que valen y se conservan como APOYO** (ya no como sustituto de la definición): la **receta y el plato** (el papel se entrega, el plato se cocina, los paréntesis son el fuego) → 01 · el **molde** (a la masa no le dices qué forma tiene, se la da el molde) → 02 · la **ventanilla** (rellenas el impreso en la ventanilla y el mostrador ya tiene el cartel; en casa lo escribes tú) → 03 · el **sello de goma** → 04 · la **fotocopia** (`onChange` no canta teclas, te pasa el campo entero) → 06.
    - **Estado de la v1 al descartarla:** 01, 02 y 03 cerrados 6/6 por él; 04 a medias; 05 montado sin resolver. **Sus soluciones quedaron commiteadas** antes de rehacer (`wip(05-typescript): guardar las soluciones del bloque 10 antes de rehacerlo`), así que puede consultar sus propias respuestas mientras rehace.

#### ✅ Deuda técnica del repo (detectada 21 jul 2026, SALDADA 26 jul 2026)

Al cerrar `09-react-props/04` se corrieron `lint` y `typecheck` a nivel de proyecto y salen en rojo — **nada en `09-`**, todo en carpetas ya dadas por cerradas. No se ha investigado el origen:

- `pnpm typecheck` → 3× `TS2345` en `03-arrays/exercise-10.test.ts` (líneas 66-68): `Producto | undefined` no asignable a `Producto`. Huele a `noUncheckedIndexedAccess` sobre un acceso por índice en el test. La carpeta figura como cerrada con typecheck limpio → o se rompió después, o se cerró con otro alcance de `tsconfig`.
  - **🔎 MISTERIO RESUELTO (25 jul 2026): era el COMANDO.** `tsc --noEmit` **sin `-b`** sobre este `tsconfig.json` de tipo *solution* (`"files": []` + `references`) no comprueba **nada** y sale con código 0. Todo cierre "verificado" con esa forma del comando no verificó nada. **Usar siempre `pnpm typecheck` (`tsc -b --noEmit`).**
  - **✅ ARREGLADO (25 jul 2026).** Los tres eran el MISMO fallo: `const pan = tienda[0]` en el bloque R devuelve `Producto | undefined` por `noUncheckedIndexedAccess`, y `elegirCampeon(mejor: Producto | undefined, actual: Producto)` pide un `Producto` pelado en su **segundo** parámetro. Arreglado con `!` en las dos constantes del fixture (índice fijo sobre un array literal de 3: existen), con el porqué escrito al lado. **`pnpm typecheck` limpio en TODO el repo por primera vez desde que se detectó; 778/778 tests verdes.**
- `pnpm lint` → 9 errores: `no-unused-vars` "only used as a type" en `01-tipos-basicos/08` y `/10` (arruga BENIGNA ya diagnosticada en su día: la constante es solo type-source) y `no-unused-expressions` en los tests de `07-utility-types/07` y `/08`.
  - **✅ ARREGLADO (26 jul 2026).** Ninguno era un bug: los 9 marcaban código correcto, así que se silenciaron **en el sitio, con el porqué escrito al lado** (mismo patrón que el `react-refresh/only-export-components` de FASE 2), sin tocar `eslint.config.js` ni la lógica de ningún ejercicio.
    - **Las 4 consts type-source** (`PERFIL_BASE` en `01/07`, `ROLES` y `TALLAS` en `01/08`, `CATEGORIAS` en `01/10`): `// eslint-disable-next-line @typescript-eslint/no-unused-vars` + nota didáctica. **El porqué que quedó escrito: ESLint no cuenta los usos en posición de TIPO como uso del VALOR** — por eso `const X` consumida solo con `typeof X` parece muerta; el valor existe a propósito, es lo que impide que lista y unión se desincronicen. Es exactamente la "arruga benigna" que ya se le explicó en el `01/07`.
    - **Las 5 pruebas de tipo** de `07-utility-types/07` y `/08` (líneas sueltas tipo `vistaPublica(u).password` que solo existen para dar de comer al `@ts-expect-error` de encima): `/* eslint-disable */` **a nivel de archivo**, en la cabecera. **Motivo verificado, vale como lección de tooling: la directiva NO cabe entre el `@ts-expect-error` y su expresión** — TS mira la línea siguiente EXACTA, y un comentario en medio deja al `@ts-expect-error` sin error que cazar (`TS2578`). Tampoco servía un `eslint-disable-next-line` multilínea: ESLint rechaza esa forma en comentarios de bloque.
  - **Estado final verificado (26 jul 2026): `pnpm lint` limpio, `pnpm typecheck` exit 0, `pnpm test:run` 778/778 en 91 archivos.** Las tres capas verdes a la vez por primera vez.

#### Parcial diagnóstico — `00-evaluacion/parcial-01` (EN CURSO, jun 2026)

Examen escrito con autocorrección, nivel criba (~12 ítems). Formato simple: un hueco por ítem (tipos → reemplazar `SIN_RESPONDER`; opción múltiple → letra; código → completar cuerpo/firma; cada uno con su "Por qué" que revisa el coach). Corrección: tipos por `pnpm typecheck`, comportamiento por `pnpm test:run`. A1 va resuelta como ejemplo.

- **A1, A2, A3 (inferencia) — APROBADAS.** widening en objeto, `as const`→literal, y `numerosA[0]` = `number | undefined` (costó: reforzar widening de array + `noUncheckedIndexedAccess`).
- **B1 (`formatear`, narrowing primitivo) — APROBADA.** early return, narrowing por flujo de control.
- **B2 (`area`, discriminated union + `never`) — APROBADA**, pero requirió todo el refuerzo de `08-discriminated-unions/exercise-02` (había salido con `typeof figura`).
- **B3 — APROBADA (jun 2026).** `"b"` + Por qué completo en el archivo (quirk `typeof null === "object"`, quién se cuela, y el chequeo correcto `typeof x === "object" && x !== null` — llegó solo a la forma profesional). Corrección de dirección en su 1ª frase: "object puede ser null" → "null se DISFRAZA de object ante typeof".
- **C1 ✅, C2 ✅, D1 ✅, D2 ✅.** OJO: en D2 añadió una llamada con comentario falso (`columna([...{animal:"perro"}], "numero") // [1,2], perro no cuenta`) — se le corrigió: `.map` NUNCA salta elementos, devuelve `[1, 2, undefined]` (largo 3); "decidir quién cuenta" es de `.filter` → conecta con el bloque E pendiente de `04-objetos`. PENDIENTE verificar que corrigió ese comentario.
- **D3 — APROBADA (jun 2026).** Primero adivinó "c" por desconocer `void` → se enseñó `void` ("no devuelve nada", console.log/handlers) + traducción de `T`/`U` a nombres descriptivos; con eso aplicó SOLO el criterio fotocopiadora del 29 y respondió "b" bien. Vocabulario corregido: dijo "constraint" donde era "parámetro de tipo" (en `imprimir<T>` no hay `extends`). La "firma mínima" costó 3 intentos (escribió una LLAMADA de mapear; luego CÓDIGO VIVO `imprimir(x: number): number {return x+1}` con export default de autocompletado) → se separó FIRMA (declara huecos) vs LLAMADA (los rellena) y se le dio la respuesta: `imprimir(x: unknown): void`; quedó apuntado el contraste `<T>` (recuerda, nadie lo usa = decorativo) vs `unknown` (acepta todo sin recordar = correcto aquí; en los plucks era DEFECTO, aquí es elección).
- **E1 — APROBADA Y CERRADA (13 jun 2026). PARCIAL-01 100% COMPLETO.** El "Por qué" (líneas 169–171) quedó con las DOS mitades: (1) "requiere number para comparar con `>`" + (2) "keyof por sí solo no basta porque no sabe que es un number" (lo resolvió tras aprender el drill 6 de exercise-01 / esPositivo). Pulido del coach: el matiz exacto es que keyof certifica que la etiqueta EXISTE pero no QUÉ guarda; Record<Clave,number> añade la garantía del contenido. Con esto el parcial diagnóstico queda enteramente cerrado. Detalle histórico de la firma: Su solución: `<Objeto extends Record<Clave, number>, Clave extends keyof Objeto>` — MEJOR que la receta del coach (que proponía `Clave extends string` por miedo a circularidad): TS acepta esta referencia mutua y su forma expresa los 2 requisitos literalmente. REGISTRAR la confesión hecha: el chequeo de circularidad de TS es más laxo de lo que el coach pintó. Se enseñó `Record<Llaves, Valor>` (fábrica de formas; puente: es la "llave calculada `[id]`" a nivel de tipos) y la FLECHA INVERTIDA (antes: Objeto manda, Llave del menú; ahora: Clave manda, Objeto debe tenerla con number — necesaria cuando el cuerpo hace matemática: `>`, `+`). Tras la firma dijo "se me hace demasiado complejo" y pidió ejercicios aparte → se montó `07-utility-types/exercise-01` (ver abajo). El "Por qué" de la línea 169 se responde con el drill 6 de ese archivo (el operador `>` exige números; keyof solo garantiza que la etiqueta existe).

#### Parcial de consolidación — `00-evaluacion/parcial-02` (✅ COMPLETO — 30 jul 2026)

Mismo formato que el 01 (un hueco por ítem, `SIN_RESPONDER` para tipos, letra para opción múltiple, `// completa aquí` para código, "Por qué" que revisa el coach; A1 resuelta como ejemplo). **12 ítems, `.tsx`.** Pedido por él con dos condiciones: **nada de la carpeta 10** y que cubra **las demás** (01→09; las 11-16 están vacías).

- **Cobertura:** A inferencia y lectura de tipos (`01`, `07`) · B objetos e inmutabilidad (`04`) · C funciones de orden superior (`02`, `03`) · D genéricos y utility types (`06`, `07`) · E uniones discriminadas (`05`, `08`) · F props de un componente (`09`).
- **Dificultad: un escalón por encima del 01, y apuntada a sus resbalones reincidentes documentados** (no a temas nuevos): **C1** es el quinto gemelo de filter+map+campo con el tipo de retorno a cargo suyo; **C2** (`unirRutas`) sale con la firma equivocada `(trozos: string[])`, que es EXACTAMENTE el bug abierto de `02-funciones/10` drill 3 — el test llama con argumentos sueltos, así que solo pasa con `...trozos`; **A2** exige juntar lectura de tipo de fuera hacia dentro + `noUncheckedIndexedAccess` (`((t: string) => boolean) | undefined`); **A3** apila `typeof` como operador de tipo + `Partial` + acceso indexado; **D2** es la trampa documentada de que `Omit` NO valida la clave mal escrita; **B1** obliga a copiar sin mutar; **E1** pide la guardia `never`.
- **⚠️ Reparto de corrección, igual que en el 01: hay "verde de mentira" de fábrica.** Con el examen en blanco, `pnpm test:run` da **9 rojos y 5 verdes**, y esos 5 verdes NO están bien: son A1 (el ejemplo) y los **ítems de TIPO** (A2, A3, D1, F1c), cuyas comprobaciones (`expectTypeOf`, `@ts-expect-error`) se borran en runtime. La nota real de esos sale de **`pnpm typecheck`**, que arranca con **31 errores**.
- **✅ CERRADO (30 jul 2026): 14/14 tests, typecheck 0, lint 0, los 11 ítems aprobados.** Los 3 que faltaban (D2, E1, F1) cayeron en una sola sesión, frente a la semana larga y los 4 archivos de refuerzo que costó el parcial-01. **Andamiaje retirado** al corregir: el `@typescript-eslint/no-unused-vars` de la cabecera (queda solo el `react-refresh`, que sigue haciendo falta) y el `type SIN_RESPONDER` comentado por él.
  - **D2 (la trampa de `Omit`) — letra suya, mitad del "Por qué" mía.** Acertó `"b"` **sondeando**, no adivinando: escribió el `type` a mano y miró cuál no se ponía en rojo. Antes de responder **cazó el `emial` mal escrito y paró a preguntar si era una errata mía** — el reflejo correcto, solo le faltó el paso siguiente ("¿y si TS tampoco se da cuenta?"). Cerró bien la mitad 🅱 (qué vale `X`: `Usuario` intacto, no se omite nada). **La mitad 🅰 la escribí yo** (`Pick<T, K extends keyof T>` vs `Omit<T, K extends keyof any>`: a `Pick` le exiges una llave real, a `Omit` solo un string) porque no llegó a comparar las firmas en `lib.es5.d.ts` tras pedírselo dos veces. Va marcada como tal dentro del archivo. Dos correcciones registradas: trajo una explicación del autocompletado que sonaba bien y era falsa ("Omit sólo la omite" — no omite NADA), y **reincidió en el resbalón de vocabulario "objeto" por "tipo" un mensaje después de corregírselo**.
  - **E1 (`describir`) — 3 `case` correctos a la primera, guardia fallida y arreglada sola.** Primer intento: `default: return "never"` — devolvió la PALABRA en vez del TIPO, y un `default` que retorna un valor normal anula el chequeo. **El test de E1 estaba VERDE con esa versión**: la rama `default` no se ejecuta nunca con solo 3 fases, así que ningún test de runtime puede cazarlo. Se le demostró con una sonda aparte (añadir una 4ª fase `"cancelado"` a un archivo suelto y pasar `tsc`): su versión, silencio; la correcta, `TS2322`. Lo arregló solo releyendo su propio `08-discriminated-unions/exercise-02.ts:113-116`, y la sonda repetida ya cazaba la fase nueva. **Contraste con el parcial-01: el B2 equivalente lo resolvió con `typeof figura` y obligó a construir un ejercicio de refuerzo entero.**
  - **F1 (`Tarjeta`) — correcto a la primera**, tipo + firma desestructurada + `&&` para el `<span>` condicional.
  - **⚠️ Corrección de dirección que hice mal yo:** al presentar E1 le dije "sin `default`", y es inexacto — el `default` **sí va**, es donde vive la guardia; lo que mata el chequeo es que ese `default` devuelva un valor normal en vez de contener `const _exhaustivo: never = p`.
  - **Ítems escritos por el coach, NO cuentan como suyos:** el "Por qué" de **A3** (el paso del `["edad"]` que extrae), la letra y el "Por qué" completo de **B2**, y el `...` de la firma de **C2**. Todo lo demás es suyo.
  - **🏆 C1 (`nombresBienPagados`) — QUINTO GEMELO del slip histórico filter+map+campo, y el primero a la PRIMERA.** Las tres piezas juntas (doble condición, `.nombre` extraído, `: string[]` anotado), sin pista, en examen. Y es literalmente la misma función que falló en `01/09` (`bienPagados`) **tras un pre-aviso explícito**. El músculo está hecho. Ver [[feedback_slip_map_noop_y_tipo_retorno]].
  - **✅ DOS AGUJEROS EN MI PROPIO TEST — SALDADOS (30 jul 2026).** El test de D1 pasaba en verde con dos versiones defectuosas: (a) con el cuerpo cambiado a `valor || reserva` (ninguno de los 4 casos usaba un valor *falsy* en la 1ª posición); (b) con la firma `<T>(valor: T, reserva: T): T`, que **devuelve `string | undefined`** en el caso `porDefecto(undefined, "vacío")` del propio enunciado. Parcheado con 3 casos *falsy* (`porDefecto(0, 99)` → `0`, `porDefecto("", "reserva")` → `""`, `porDefecto(false, true)` → `false`) y `expectTypeOf(porDefecto(undefined, 'vacío')).toEqualTypeOf<string>()`. **Verificado que ahora SÍ cazan**: con `||` el runtime devuelve `99`/`"reserva"` (rojo), y la firma floja da `TS2322 Type 'string | undefined' is not assignable to type 'string'` en sonda `tsc` aparte.
- **✅ Protocolo verificado (29 jul 2026):** respuestas escritas → `pnpm test:run` **14/14 verde** + `pnpm typecheck` exit 0 + `pnpm lint` 0 → huecos vaciados → **9/14 rojos** + 31 errores de typecheck, con `pnpm lint` todavía en **0** (el archivo lleva su bloque `eslint-disable` de andamiaje con `react-refresh` y `no-unused-vars`, y el porqué de cada uno escrito al lado; **se borra al corregir**). El import de `ReactNode` queda "sin usar" a propósito: es la pista del ítem F1.

#### Parcial 03 — `00-evaluacion/parcial-03` · CAZADOR DE BUGS (MONTADO 30 jul 2026, pendiente de resolver)

**Formato NUEVO, y es el punto del examen.** Los parciales 01 y 02 daban un hueco y él lo rellenaba. Este **no tiene huecos: las 11 funciones COMPILAN** (`pnpm typecheck` sale en 0 sobre el archivo tal cual) **y todas están mal.** Su trabajo es encontrar por qué.

- **Por qué este formato y no un parcial-02 bis.** Se comprobó al montarlo: entre `01`→`09` hay 90 ejercicios y **los dos parciales anteriores ya los examinaron**; la `10` tiene 1 solo ejercicio sin resolver y de la `11` a la `16` están vacías. **No hay temario nuevo que examinar.** Un tercer examen de rellenar huecos mediría lo mismo por tercera vez. Este mide otra habilidad: leer código que el compilador aprueba y no fiarse. Es la que le falló tres veces el mismo día (el test verde de E1, el `Omit` de D2, el autocompletado convincente).
- **Cómo corrige — mecanismo nuevo, reusable.** Cada ítem le pide **una PRUEBA, no una opinión**: rellena una constante `P3_NN_...` con la entrada que destapa el fallo, y el test la ejecuta contra la función rota comprobando que efectivamente miente, revienta o ensucia. No vale acertar de palabra. Más el `// Por qué:` que reviso yo. **Ventaja sobre el menú de letras (a/b/c) de los parciales anteriores: no hay nada que adivinar ni que le regale el diagnóstico.**
- **Los 11 bichos, todos fallos suyos documentados o aparecidos corrigiéndole** (ninguno es tema nuevo): 01 `!` que miente con `[]` *(ejemplo resuelto)* · 02 `||` en vez de `??` *(el agujero de mi propio test de D1)* · 03 `Omit` con la clave mal escrita *(D2 del parcial-02, ahora en código vivo)* · 04 `default` que tapa un `case` que falta *(E1 del parcial-02)* · 05 `.map(parseInt)` *(el callback recibe TRES argumentos)* · 06 `reduce` sin valor inicial *(su slip documentado 3×)* · 07 `.sort()` que muta el original · 08 predicado de tipo que MIENTE (`x is number` con `typeof x === "string"`) — TS no verifica el cuerpo de un predicado · 09 spread en orden invertido, el parche nunca gana · 10 `.includes` sobre objetos, compara identidad no contenido *(B2 del parcial-02)* · 11 React: `{cantidad && <span>}` pinta un `0` suelto *(se le enseñó al corregir F1)*.
- **✅ Protocolo verificado (30 jul 2026), en las dos direcciones:** starter → **10 rojos + 1 verde** (el verde es el ejemplo 01) con `pnpm typecheck` en **0** *(condición de diseño: el examen TIENE que compilar)* y `pnpm lint` en 0 → soluciones aplicadas → **11/11 verde**, typecheck 0, lint 0 → revertido al starter → 10 rojos otra vez. Copia resuelta de referencia guardada fuera del repo.
- ⚠️ **El aviso del "verde de mentira" va escrito en la cabecera**, porque aquí es más resbaladizo que nunca: varios tests pasan con el examen en blanco solo porque la constante tiene un valor del tipo correcto. La nota sale de que la prueba REVIENTE la función.
- **✅ CERRADO el 31 jul 2026 — 11/11**, `typecheck` 0, `lint` 0. Los tres parciales del proyecto quedan completos.
  - **02 (`||` vs `??`) — aprobado, pero costó 4 rondas y NINGUNA fue por el concepto.** Encontró el `0` pronto y explicó bien el falsy. Las 4 rondas se fueron en el **campo mecánico**: primero borró la constante `P3_02_ENTRADA` y la sustituyó por una línea de trazado (rompió el import del test entero, TS2724), luego la repuso **sin `export`**, y las correcciones de texto que se le pidieron no se aplicaron. **Patrón a vigilar, ver [[feedback_responde_porque_salta_campo]].** Cerró con la definición de `??` correcta y con las dos ramas ("¿es null o undefined? sí → derecha; no → izquierda"). Pendiente de que la pegue en el archivo, junto con `operando` y `falsy` en la línea 71.
  - **03 (`Omit` con la clave mal escrita) — aprobado, y por encima de lo pedido.** Añadió por su cuenta una inferencia que nadie le dio: *"si en un futuro el type incluye la propiedad `cuerop`, esta se va a omitir; por ahora sale el type completo sin omitir nada"*. Es decir, entendió que `Omit` no está roto — obedece sobre una llave que no existe, y **el fallo es que nadie valida la llave**. Esa es exactamente la mitad 🅰 que en el D2 del parcial-02 (esa misma mañana) tuvo que escribirle el coach. Corregido el vocabulario: "sale el type completo" → "el tipo resultante es idéntico a `Articulo`".
  - **04 (`default` que anula la exhaustividad) — aprobado.** Entrada correcta a la primera. Su "Por qué" se quedó en el SÍNTOMA (falta el case → entra el default) sin nombrar al culpable (el `default` es lo que apaga el aviso de compilación). Se le señaló como gemelo del E1 del parcial-02 con otra ropa: allí devolvía la palabra "never", aquí devuelve `"sin datos"`, que es peor porque suena verosímil.
  - **05 (`.map(parseInt)`) — CERRADO POR EL COACH (31 jul), a petición suya.** Venía de destapar el TERCER agujero de un test mío: respondió `["a", "b"]`, el test se puso VERDE y no valía — ese `NaN` no lo causa el bug, lo causa la `"a"`; corriendo su entrada contra las dos versiones (rota y correcta) el resultado era **idéntico**, y una entrada que se comporta igual con el código bueno y con el malo no prueba nada. **Mismo error de método que tenía mi test de D1 del parcial-02.** Test apretado (textos que sean números válidos por sí solos + más de uno). Se le enseñaron los dos conceptos que le faltaban, porque **ninguno lo había estudiado**: (a) **entregar una función no es llamarla** — `.map` la llama siempre con `(valor, índice, array)`, así que el índice aterriza en el hueco del `radix`; es literalmente el concepto de `10-eventos-formularios/exercise-01`, sin resolver. (b) **`radix` es la BASE**: válidas 2–36, el `0` significa "decide tú". **Escalera de 6 drills de `parseInt` en el chat: acertó 4/6.** Falló `parseInt("7", 0)` (dijo `NaN`, es `7`) y no supo `parseInt("2", 2)`. **MISCONCEPTION REINCIDENTE Y NOMBRADA: cree que la AUSENCIA de base produce `NaN`** — apareció 3 veces el mismo día ("como no hay radix sale NaN", "faltan argumentos", "base 0 → NaN"). Es al revés: **la ausencia es el caso cómodo; el `NaN` lo produce una base PRESENTE y mala.** Entrada elegida por el coach `["10","10","10"]` porque enseña los DOS modos de fallo: base 1 → `NaN`, base 2 → `"10"` leído en binario → **`2`, un número verosímil, peor que el `NaN`**.
  - **06 (`reduce` sin inicial) — aprobado, y el ítem más revelador de la sesión.** **Encontró el bug él solo** mirando una llamada de una línea (el slip que se le escapó 3 veces en `07-utility-types`). Pero acto seguido **ARREGLÓ la función**: añadió `, undefined`, violando la regla del examen ("no arregles las funciones, son las pruebas del delito") y **rompiendo el typecheck de 0 a 2 errores** — su arreglo reprodujo de paso el OTRO slip fichado de aquella sesión (falta `reduce<Producto | undefined>` explícito + portero del primer turno). **Reflejo de programador: encuentra el bug y la mano va sola a repararlo.** Revirtió rápido cuando se le señaló. La entrada `[]` sí costó: bajó de 3 a 2 productos sin llegar al vacío; se destrabó **ejecutando en node las cuatro listas (3/2/1/0) y viendo el `TypeError: Reduce of empty array with no initial value` real en pantalla** — con él, cero metáforas. **VOCAB WIN: usó "contrato" por su cuenta** ("sin el valor inicial no se cumple el contrato de devolver o un producto o un undefined"), correcto. **CAUSALIDAD INVENTADA CAZADA (lo más valioso del ítem):** escribió *"…no se cumple el contrato, **es por ello que** el compilador ni siquiera nos avisa"*. Falso: TS calla porque **no modela las excepciones**, no porque el contrato se incumpla. Pegó dos hechos ciertos con un "por lo tanto" que no existe, y sonaba convincente — que es exactamente el fallo que este parcial persigue.
  - **07 (`.sort()` que muta) y 08 (predicado que miente) — entradas SUYAS y correctas; los "Por qué" los redactó el coach al cierre**, a elección suya (ver diagnóstico abajo). En el 07 eligió dos productos con los precios invertidos respecto al orden de los ids —justo la lista que hacía falta— y **nombró solo el arreglo `[...ps]`**; en el 08 acertó `"Hola"` a la primera. Lo que no llegó a escribir en ninguno fue el "por qué TS no lo caza".
  - **🧭 DIAGNÓSTICO DE LA SESIÓN (31 jul) — leer antes de retomar.** Arrancó diciendo **"estoy frío"** y aun así **cazó los cuatro bichos**: la mitad de ENCONTRAR la aprueba limpia. Lo que se atascó fue **REDACTAR**: seis rondas de "revisa de nuevo" con movimiento casi nulo en los `// Por qué:`. **Y la causa apareció al final, y NO era desidia:** preguntó *"¿Se supone que te debo responder esas 3 preguntas aquí en el chat?"* — llevaba rondas creyendo que contestar por chat ya contaba. **⚠️ Matiza [[feedback_responde_porque_salta_campo]]: parte de lo que parecía saltarse el campo mecánico era no saber DÓNDE iba la respuesta. Decirlo explícito desde el primer ítem.**
  - **🧰 HERRAMIENTA NUEVA QUE FUNCIONÓ — la plantilla de 3 partes del "Por qué", reusable en todo el parcial:** **① MECANISMO** (qué hace la función, literalmente) · **② POR QUÉ ROMPE CON ESTA ENTRADA** (la cadena concreta hasta el fallo) · **③ POR QUÉ TYPESCRIPT NO LO CAZA** (qué ve el compilador y por qué le parece bien). **La ③ es el examen entero y es la que omitía sistemáticamente** en los 8 ítems. Escrita como modelo dentro del ítem 06 del archivo; las etiquetas son andamiaje, el contenido es lo obligatorio.
  - **Conceptos enseñados que quedan fichados:** TS **no modela excepciones** (no existe `throws` en un tipo) · `.sort()` **ordena en el sitio y devuelve el MISMO array** — en `ordenarPorPrecio` hay UNA lista con tres nombres, no dos; la única defensa del sistema de tipos es `readonly Producto[]`, que hace que TS **rechace** el `.sort()` · **TS no verifica el cuerpo de un predicado de tipo** · la familia "confía en mí" = **`as` + `!` + `x is T`**, las tres apagan al compilador (el ítem 01 es el `!`, el 08 es el predicado).
  - **Dos preguntas suyas muy buenas (su modo natural, le rinde):** (a) *"¿Alguna vez TypeScript actualizará esto para que no pase en desarrollo?"* → por qué no habrá `throws` (lanzar no es local y contaminaría toda la cadena de llamadas; en JS lanza casi todo; Java lo probó con excepciones comprobadas y se considera mala idea), qué **sí** hicieron (`unknown` en el `catch` desde TS 4.4 con `strict`), y la escalera de qué hacer en su lugar: **① devolver en vez de lanzar ② `Resultado<T>` con unión discriminada** (= su `08-discriminated-unions` aplicada a errores) **③ Zod en el borde ④ try/catch** como último recurso para código ajeno. (b) *"`esNumero` la considero una evaluación estúpida"* → concedido (está a lo bestia a propósito), y se le dio **la versión con corbata**: un `esUsuario` que comprueba 2 de 3 campos y ninguno de los tipos, parece código responsable y está igual de roto — **ese es el que escribirá de verdad en `13-fetch-tipado`**, y el motivo por el que allí la respuesta será Zod.
  - **09 (spread en orden invertido) — aprobado, y el mejor ítem suyo del parcial.** Entrada `{ tema: "oscuro" }` correcta, y **nombró el mecanismo Y el arreglo a la primera**: *"el orden del spread; si se quiere pisar la base debería ser `{...base, ...parche}`"* — su propia regla de `04-objetos/exercise-02` ("lo que se escribe después pisa") aplicada en sentido inverso, de leer un orden a deducir la consecuencia. **Lo que se corrigió fue una hipótesis DE MÁS, no de menos:** dio una segunda razón, *"AJUSTES_BASE es una const"*, que es falsa por tres motivos —con `let` el bug es idéntico (se ejecutó en node para aislarlo), `const` NO congela el contenido (solo impide reasignar la variable; `BASE.tema = "otro"` funciona), y `fusionar` nunca ESCRIBE en la base, solo la lee—. Queda anotado como "descartado" dentro del archivo para que no lo vuelva a dudar en frío. La ③ se la enseñó el coach con **su propia frase de `04-objetos`: "TypeScript vigila FORMAS"** — los dos órdenes producen la misma forma exacta, lo único que cambia son los valores, que TS no sigue.
  - **10 (`.includes` compara identidad) — aprobado por el método de la PREDICCIÓN, que funcionó muy bien y conviene reusar.** Llegó diciendo *"no tengo diferencias de opinión, me resulta difícil encontrar la fuga"*, y su lectura del valor de fábrica era **correcta** (un Cacao que no está en el catálogo → `false` es la respuesta buena, ahí no hay bug). En vez de explicárselo, se le pidió **una predicción sincera** sobre un caso concreto: un producto con los mismos valores que `CATALOGO[0]` pero tecleado aparte. Predijo **`true`** ("los valores coinciden"). Se ejecutó en node → **`false`**, y al lado `CATALOGO.includes(CATALOGO[0])` → `true`. La sorpresa hizo el trabajo que no habrían hecho tres párrafos. Conceptos que quedaron: `.includes` pregunta *"¿es el mismo objeto?"*, no *"¿dice lo mismo?"*; `{a:1} === {a:1}` es `false`; **y por qué la intuición falla justo aquí — con primitivos `.includes` funciona perfecto (`"Té" === "Té"` es `true`), así que lleva años usándolo sin que le falle, y el día que le pasa un objeto cambia de comportamiento sin avisar.** Analogía: dos ejemplares del mismo libro, mismo texto, distinto ejemplar. Arreglo: `lista.some(x => x.id === p.id)`.
  - **11 (React, el `0` suelto) — entrada suya (`0`), explicación del coach a petición suya.** El bug de React más común que existe. `cantidad && <span>` con `cantidad = 0` devuelve **el 0**, no `false` (el `&&` devuelve uno de sus operandos, mismo mecanismo que el `||` del ítem 02 con la condición al revés), y React **ignora `false`/`null`/`undefined` pero pinta los números como texto** → el `<span>` no se pinta y queda un `0` huérfano en pantalla. La ③: la expresión tiene tipo `0 | Element` y **las dos posibilidades son cosas que React sabe pintar**, así que no hay ningún tipo fuera de sitio. Arreglo: `cantidad > 0 && <span>…`.
  - **✍️ REESCRITURA FINAL DE LOS 11 "POR QUÉ" (a petición suya): "hazlo de la misma manera en que lo explico yo".** Se pasaron todos a **prosa llana en su registro**, sin las etiquetas `①②③` (eran andamiaje del coach, no contenido). **A los suyos —02, 03, 04— NO se les tocó una sola frase**: solo se les añadió debajo la parte que faltaba, el "TypeScript no lo caza porque…". Se quitaron también las etiquetas de autoría del archivo (las iba borrando él a mano según aparecían); el registro de quién razonó qué vive **aquí**, que es donde sirve.
  - **⚠️ INCIDENCIA TÉCNICA A RECORDAR (costó una reescritura entera):** durante toda la sesión VSCode tuvo `parcial-03.tsx` abierto con un búfer viejo y **fue pisando las ediciones del coach una por una**, hasta borrar la reescritura completa (el usuario dijo *"no veo los cambios"* y efectivamente no estaban: 0 apariciones del texto nuevo, tests de vuelta a 9/11). **Protocolo para la próxima: si el usuario tiene el archivo abierto, hacer UNA sola escritura completa en vez de N ediciones, y commitear inmediatamente después** — es lo que finalmente funcionó.
  - **▶ SIGUIENTE PASO: código real.** Con los tres parciales cerrados no queda nada que examinar aquí; el plan acordado sigue siendo migrar proyectos, Zod en una API propia y leer código ajeno (ver [[project_ruta_post_typescript]]). Pendientes sueltos del proyecto: `10-eventos-formularios` en curso (**01 cerrado 6/6; 02 montado el 2 ago 2026, pendiente de resolver**; el 01 era **literalmente** el concepto del ítem 05, entregar una función no es llamarla) y las carpetas 11→16 vacías.

**REFUERZO (13 jun 2026 · CERRADO 15 jun 2026 — exercise-02 COMPLETO 13/13): `07-utility-types/exercise-02` — el `reduce` campeón.** El usuario pidió un archivo aparte en escalera para entender el drill 8 de exercise-01 (`minimoPor`), que se le atascó en DOS frentes: la lógica del reduce-campeón (acumulador = "mejor objeto hasta ahora", inicial `undefined`, portero del primer turno, `reduce<T | undefined>`) y la firma genérica. 13 drills crecientes (a petición suya se añadió un CALENTAMIENTO de reduce arriba del bloque A porque "es el método que más se me olvida", y se renumeró todo 1→13): CALENTAMIENTO (1–6, reduce a secas — sumar/producto[neutro 1]/contarPares/concatenar/longitudTotal[string[]→number]/todosPositivos[neutro true], él escribe el cuerpo), A (7–8, campeón NÚMERO, tope `Infinity`/`-Infinity`), B (9–11, campeón OBJETO concreto sin genéricos — masBarato/masCaro/conMenosVidas, patrón undefined+guard), C (12–13, mismo cuerpo pero ahora genérico, él escribe el dúo; el 12 `menorPor` es el GEMELO de minimoPor, el 13 `mayorPor` = `maximoPor` del E1). **Drills 1–6 (CALENTAMIENTO reduce) APROBADOS** (sumar/producto/contarPares/concatenar/longitudTotal/todosPositivos; consolidó neutros, ternario+precedencia de `()`, cambio de tipo, y `&&` con neutro `true`). **Drill 7 (`menorNumero`) lo tuvo verde y luego lo ROMPIÓ** cruzando el patrón de CONTAR (`? 1 : 0` del drill 3) con el de CAMPEÓN, y dejó un bloque `{}` sin `return` (oficina sin entregar). Dijo "sigo sin entender la lógica del 7... techo en este tipo de ejercicios" → se reencuadró (no es techo: lo tenía resuelto; el carry-del-campeón es la forma MÁS difícil de reduce) y se le mostró el puente **bucle `for` → reduce** (acum = la variable que sobrevive; lo que DEVUELVES es el acum del siguiente turno). **CIERRE (15 jun 2026), drills 8–13:** 8 `mayorNumero` (espejo del 7) bien. 9 `masBarato` (campeón OBJETO concreto) las 4 piezas a la primera (`reduce<Producto|undefined>` + inicial `undefined` + portero `=== undefined` + `<`). **10/11 (`masCaro`/`conMenosVidas`): mismo fallo en ambos — OLVIDÓ EL INICIAL `, undefined`** → doble captura: TS2554 ("Expected 2 arguments, but got 1": la forma `reduce<U>` exige el inicial) + runtime `TypeError: Reduce of empty array with no initial value` en `[]`. Entendió que sin inicial el portero `if (acum === undefined)` NUNCA dispara (acum arranca = primer elemento) y el array vacío explota en vez de devolver undefined; lo corrigió leyendo las dos capas. 12/13 (`menorPor`/`mayorPor`) firma con el dúo de referencia mutua ya como GESTO. **PATRÓN A VIGILAR: olvidar el VALOR INICIAL del reduce campeón** — 3ª vez en la sesión (D5 de exercise-01 olvidó el `<Objeto|undefined>` explícito; 10 y 11 el `, undefined`). De las 4 piezas del patrón, el "inicial" es la que más se le escapa.

**`07-utility-types/exercise-03` — "el campeón, del bucle al reduce" — CERRADO (14 jun 2026).** A petición suya ("necesito más ejercicios para entender la lógica del drill 7"). 6 drills, mismo problema resuelto 2 veces: Bloque A (1–3, con `for`: mayorBucle/menorBucle/masLargaBucle), Bloque B (4–6, el MISMO con `reduce`: mayorReduce/menorReduce/masLargaReduce). Los 6 APROBADOS, 6/6 tests. **Cierre:** por fin articuló el razonamiento que había esquivado 4+ sesiones (por qué el inicial de un mínimo es `Infinity` y no `0`). Hitos del cierre: (a) consolidó control de flujo del `for`: el `return` final está FUERA del bucle (mismo nivel que el `let`), se ejecuta UNA sola vez al terminar todo el recorrido, no en cada vuelta ni atado al `if`; analogía PUERTA GIRATORIA (el for gira; el return está al otro lado, se cruza una vez). (b) Trazó el desastre del `0` como tope (`menorBucle([3,5,8])` con `menor=0` → `3<0` no, `5<0` no, `8<0` no → devuelve `0`, que ni está en el array). (c) **Confusión corregida (importante):** creía que `-Infinity` era "para cuando los números son negativos" → se aclaró que el tope depende de QUÉ buscas (máx→`-Infinity`, mín→`Infinity`), NO del signo de los datos (máximo de `[-5,-2,-8]` sigue usando `-Infinity`); y que `-Infinity` es el valor MÁS PEQUEÑO posible, no "el negativo más grande". (d) **Regla unificadora que articuló él:** "el tope inicial siempre es el valor que cualquier elemento real VENCE" = el peor campeón posible, puesto ahí para perder en la 1ª vuelta. Con esto deja de memorizar Infinity/-Infinity como dos datos sueltos. **Siguiente hilo: volver a `exercise-02` a rematar drills 9–13** (campeón OBJETO concreto 9–11 con `undefined`+guard, y genérico 12–13 con el dúo).

**`07-utility-types/exercise-01` — BLOQUE D + drill 8 — CERRADO (15 jun 2026). exercise-01 COMPLETO (14/14 tests, typecheck limpio).** Antes de rematar exercise-02 se añadió a exercise-01 un BLOQUE D (refuerzo del campeón-objeto GENÉRICO, partido en 3 tramos D1–D6 para separar lógica y firma): D·1 (D1–D2 `masPesado`/`masLigero`, tipos FIJOS → solo el cuerpo), D·2 (D3–D4 `maximoPorD`/`minimoPorD`, cuerpo dado → aprieta la FIRMA), D·3 (D5–D6 `maxPor`/`minPor`, de cero firma Y cuerpo). LOS 6 APROBADOS. Hitos: (a) **D3 destapó un fallo de mi propio enunciado**: prometía que "el dúo `<Objeto, Clave extends keyof Objeto>` y el rojo desaparece", pero quedaba vivo TS18047 ('mejor' is possibly 'null') — porque `Objeto` SIN constraint puede instanciarse como `null` y el guard `=== undefined` no lo excluye; lo apretó a `Objeto extends Record<Clave, number>` (referencia mutua, idéntica a la que descubrió en el E1). Matiz que se enseñó: el `>` NO protestó (a diferencia del `> 0` del E1) porque compara `Objeto[Clave]` contra SÍ MISMO (mismo tipo), no contra un number suelto. (b) **D5 olvidó el `reduce<Objeto|undefined>` explícito** → TS2769 "No overload matches"; entendió que el campeón necesita el genérico a mano cuando el acumulador CAMBIA de tipo respecto al inicial (en el calentamiento number→number TS lo adivinaba solo). (c) D6 capstone de memoria a la primera. **Drill 8 (`minimoPor`) CONFIRMADO APROBADO (15 jun 2026): cayó solo** tras BLOQUE D, firma `<Objeto extends Record<Clave, number>, Clave extends string>` — maneja ya las DOS variantes de la Clave (`extends string` aquí / `extends keyof Objeto` en el BLOQUE D) y las reconoce como las dos caras de la misma moneda. Con esto **07-utility-types exercise-01/-02/-03 quedan 100% completos**; el patrón `reduce` campeón (su antiguo "techo" del drill 7) está cerrado en sus 3 formas — número, objeto concreto, genérico — encadenando los tres archivos.

**[HISTÓRICO] Antes del refuerzo: `exercise-02` drill 1 (`sumar`).** Al terminar el 7, volver a `exercise-01` drill 8 (debería caer solo), y de ahí al "Por qué" del E1 (`parcial-01.ts:169`). Estado de exercise-01: drills 1–7 APROBADOS; **drill 8 APROBADO (15 jun 2026, ver bloque de arriba).**

**[HISTÓRICO] Retomar (12 jun 2026): `07-utility-types/exercise-01`, drill 1** — `04-objetos/exercise-04` quedó CERRADO (10/10) y vuelve el hilo de Record: escalera montada a petición suya porque el E1 del parcial "se le hizo complejo". 8 drills: A (1–3, fabricar tipos con Record, starters `unknown`), B (4, portero de clave FIJA `Record<"precio", number>` = puente con su bloque B de generics), C (5–8, clave VARIABLE con el dúo `<Clave extends string, Objeto extends Record<Clave, number>>`; el 6 `esPositivo` contiene la respuesta al "Por qué" del E1; el 8 `minimoPor` es el ESPEJO exacto del E1 con `<`). Estado inicial verificado: 8/8 tests runtime verdes, typecheck rojo solo por starters (TS2344 aliases + TS2578 expect-errors dormidos + TS2536/TS2339 firmas flojas). **Al terminar el drill 8: (a) escribir el "Por qué" de `parcial-01.ts:169` (con eso el parcial queda CERRADO), (b) releer la firma de `maximoPor` para confirmar que ya se lee sola.** Otros hilos abiertos: el "Por qué" del E1 (parcial línea ~170) ya tiene una respuesta breve del usuario ("requiere number para comparar con `>`") — va bien encaminada pero falta la otra mitad (por qué `keyof` solo NO basta); revisarla al llegar al drill 6 de Record. El comentario de D2 quedó VERIFICADO como corregido (12 jun 2026). Si los plucks aún se sienten flojos, bloque G opcional en 06-generics.

**`07-utility-types/exercise-04` — `Partial<T>` — COMPLETO (15 jun 2026, 5/5 tests, typecheck limpio).** Primer utility type con nombre. Drills: 1 `actualizarUsuario` + 2 `editarProducto` (patrón ACTUALIZAR = `Partial<T>` en el parámetro + merge inmutable `{ ...base, ...cambios }`); 3 partido en DOS que CONVIVEN por decisión del usuario — `crearAjustesParciales` (con `Partial`, el patrón real) y `crearAjustesCompletos` (contraste SIN `Partial`: el tipo entero obliga a pasar los 3 campos; el test lleva un `@ts-expect-error` sobre `crearAjustesCompletos({tema})` que DOCUMENTA el dolor que `Partial` evita); 4 `aplicarCambios` (lista de parches con `reduce`). **HALLAZGO IMPORTANTE: la nota del archivo estaba MAL** — decía que `aplicarCambios` no necesita `reduce<...>` explícito, pero SÍ lo necesita: como `lista` es `Partial<Usuario>[]` (elementos) y el acumulador debe ser `Usuario`, TS infiere el acum desde el array → lo toma como `Partial<Usuario>` y el retorno deja de ser `Usuario` (TS2322). Se corrigió a `lista.reduce<Usuario>(...)` y se reescribió la nota: vuelve la regla del campeón (acum de distinto tipo que los elementos → genérico a mano). **PATRÓN A VIGILAR (reincidió 3× hoy): arregla el CUERPO pero olvida el TIPO del parámetro** (drill 3 de -04 y drill 1 de -05). Check mental que se le dio: "¿el parámetro deja pasar lo que el test realmente le manda?".
- **`07-utility-types/exercise-05` — refuerzo de la LÓGICA del drill 5 (aplicar una lista de parches) — COMPLETO (19 jun 2026, 6/6 tests, typecheck limpio).** A petición suya ("el drill 5 no lo entiendo para nada"). Archivo aparte en escalera, mismo problema 3 veces: A (1 `aplicarDosParches`, merge a mano SIN bucle), B (2 `fusionarConFor`, 3 `fusionarCajasConFor`, con `for...of`), C (4 `fusionarConReduce`, 5 `fusionarCajasConReduce`, con `reduce<T>`), D (6 `aplicarParches`, capstone gemelo EXACTO del drill 5 de -04). **Drills 1–6 TODOS APROBADOS (1–2 el 15 jun; 3–6 cerrados el 19 jun 2026).** Conceptos trabajados a fondo en esta sesión: (a) el spread se ejecuta IZQ→DER y gana la copia de más a la DERECHA en colisión (analogía PIZARRA con casillas, traza con dos parches que tocan la misma `x`); (b) **`...base` NO es un "reset", es el CIMIENTO** — sin él pierdes toda propiedad que ningún parche mencione Y no compila (`{...Partial, ...Partial}` = `Partial`, no asignable al retorno completo); puente con `...ajustesPorDefecto` y con su analogía del formulario "editar perfil"; (c) el spread NO muta, crea objeto nuevo. **Punto de criterio dado (no bloqueó): en el drill 2 reasignó el PARÁMETRO `base` dentro del `for` (funciona e inmutable, pero `no-param-reassign` lo desaconseja) → lo idiomático es `let resultado = base`.** Nota positiva: en el drill 2 su comentario-resultado quedó CORRECTO y bien trazado (antes salían incompletos/a ojo — progreso). Pregunta de control pendiente del drill 5 de -04 (de dónde sale el `acum` con `nombre:'Ani'` en la 2ª vuelta y por qué sobrevive el `id`): RESPONDIDA en el cierre (ver abajo). **CIERRE (19 jun 2026), drills 3–6:** (a) **Drill 3 `fusionarCajasConFor` — el patrón clásico reincidió:** arregló el cuerpo pero dejó el tipo `Caja[]` (no `Partial<Caja>[]`). 1er intento "arregló" el rojo cambiando la LLAMADA a Cajas COMPLETAS en vez de aflojar el tipo → se le corrigió fuerte: **"el tipo se elige según lo que la función TIENE que aceptar; no se doblan los datos para encajar en el tipo, es al revés"** (su trabajo es aceptar parches incompletos). 2º intento puso `Partial<Caja[]>` (el `[]` DENTRO del `<>`) → se enseñó la diferencia de lectura: `Partial<Caja>[]` = "una LISTA de cajas parciales" vs `Partial<Caja[]>` = "una versión parcial de la LISTA entera" (afloja métodos/índices del array, no las props de Caja); **regla: el `[]` siempre va FUERA, dice "lista de lo que tengo a la izquierda"**. Lo corrigió SOLO leyendo la posición del `[]` (la habilidad que entrenamos). (b) **Drill 4 `fusionarConReduce`:** recordó SOLO el VALOR INICIAL (¡la pieza que más se le escapaba del reduce campeón!) pero olvidó el `<Punto>` explícito → runtime-verde / type-rojo (TS2322 `Partial<Punto>` no asignable a `Punto`): sin el genérico a mano TS deduce el acum del TIPO DEL ELEMENTO (parcial) y el retorno deja de ser completo. Reconectada la regla del campeón (acum de distinto tipo que los elementos → genérico explícito). Quitó además el `let resultado` que sobraba (cleanup aceptado). (c) **Drills 5 (`fusionarCajasConReduce`) y 6 (`aplicarParches`, capstone) a la primera** — gemelos con `Caja` y `Usuario`. **El capstone es el gemelo EXACTO del drill 5 de -04 que NO entendía: lo resolvió solo → objetivo del archivo cumplido.** (d) **Preguntas conceptuales suyas resueltas esta sesión (registrar, fueron buenas):** ① qué hace el `, base` final del reduce → es el VALOR INICIAL (2º argumento), el cimiento del turno 0; se mapeó pieza a pieza al `let resultado = base` del `for`; **conectó él solo con el `, 0` de los reduce numéricos** → quedó la regla unificada *"el 2º arg del reduce es SIEMPRE el valor inicial; QUÉ va ahí depende del trabajo: un **neutro** (0/1/""/true) si solo quieres un arranque seguro, o un **dato real** (`base`) si ese es tu punto de partida"* (señal de que el patrón reduce se le unifica, no memoriza casos sueltos). ② `=> (...)` vs `=> {...}`: se repasó VENTANILLA (expresión, entrega directa) vs OFICINA (cuerpo `{`, exige `return`) + los dos trabajos del `{` (objeto / cuerpo) y por qué el objeto literal se envuelve en `()` (el `(` dice "esto es un VALOR"); regla "mira lo PRIMERO tras la flecha". (e) **Explicó el drill 6 con sus palabras** — modelo sólido, con 3 correcciones: "datos"→**propiedades**; "hace cambios en el objeto"→**inmutable, devuelve objeto NUEVO** (el original sale intacto); y la gorda, **"pisa la base"→pisa el ACUMULADOR** (que en la 2ª vuelta NO es la base sino el RETORNO de la 1ª; reduce pasa lo que devuelve el callback como acum del turno siguiente) y el parche solo pisa **las propiedades que trae**. Con eso respondió las 2 preguntas de control: el acum de la vuelta 2 sale del retorno de la vuelta 1; el `id` sobrevive porque **`...acum` lo copia en cada vuelta y ningún parche lo menciona**. (f) **VOCAB win:** usó "retornará" correcto por su cuenta (la palabra que arrastraba mal, "imprime"). **Patrón de comentarios YA NO reincidió:** sus comentarios-resultado de los drills 3–6 salieron todos trazados y verdaderos (drill 4 con un caso propio creativo: 3 parches peleando por la misma `x`).
- **`07-utility-types/exercise-06` — refuerzo del drill 1 de exercise-05 (`aplicarDosParches`) — CREADO, EN CURSO (15 jun 2026).** Surgió porque al retomar el drill 3 de -05 el usuario frenó SOLO con buen criterio: "el Partial y el spread aún me cuesta entenderlo, no quiero avanzar a ciegas". Reencuadre dado (impostor): NO parte de cero con spread (04-objetos -02/-03/-04 aprobados + drills 1–2 de -05, todos spread). Diagnóstico clave que se le enseñó: mezcla DOS PISOS — 🔵 **spread = piso de los VALORES** (runtime: copia + gana el de la derecha; se ejecuta de verdad) vs 🟡 **`Partial` = piso de los TIPOS** (compilación: acepta objetos incompletos, NO inventa llaves; se borra en runtime, type erasure). Regla mental que quedó: "Partial decide QUÉ acepta la función (la portería); el spread, QUÉ haces dentro". A petición suya ("quiero más ejercicios ascendentes que tengan como fin entender el 1 de -05") se montó la escalera. Formato: tipos definidos junto a su bloque, starters MAL a propósito (tipo estricto + cuerpo que no hace el trabajo). **8 drills: A (1–3, spread A SOLAS con objetos completos: `copiar`[spread copia=objeto nuevo, `not.toBe`], `combinar`, `combinarTres`), B (4–5, `Partial` A SOLAS: `tomarParcial`[solo arregla el TIPO], `combinarParciales`[merge de 2 parciales SIGUE siendo parcial → semilla de por qué la base importa]), C (6–7, base completa + 1 parche: `aplicarUnParche`/`aplicarUnParcheCaja` → la base tapa los huecos, el "aha" del `...base`), D (8, `aplicarDosParchesBis` = gemelo EXACTO del drill 1 de -05).** Baseline verificado: 7 rojos a propósito + 1 verde (el 4 pasa en runtime; su único fallo es de tipo, visible solo en `pnpm typecheck`). **Estado: COMPLETO (19 jun 2026, 8/8 tests, typecheck limpio).** Drills 1–5 resueltos antes (BLOQUE A spread a solas + BLOQUE B Partial a solas); drills 6–8 cerrados el 19 jun. **Notas del cierre 6–8:** (a) **drill 6 `aplicarUnParche` — TIPO del parámetro CLAVADO de primeras** (`Partial<Punto>`, justo la pieza que se le escapaba 3+ veces → progreso real), pero en el CUERPO trajo maquinaria de más: copió el `.reduce` del capstone de -05 sobre un `parche` que aquí es UN solo objeto, no una lista (`Partial<Punto>` no tiene `.reduce` → TS2339 + runtime). Se separó: -05 capstone = LISTA de parches (`Partial<Usuario>[]` → reduce dobla la lista); -06 d6 = UN parche → caso atómico, spread mínimo `{ ...base, ...parche }` SIN reduce. Lo corrigió. (b) **Pregunta conceptual valiosa (registrar — hilo impostor / "para qué sirve en la vida real"):** preguntó si en un proyecto real hay que teclear a mano los objetos `{ ancho, alto, color }` o si "se reemplazan con lógica/HTML/CSS". Se separaron las CAPAS — la función = lógica/DATOS (objetos JS); HTML/CSS = capa VISUAL (no la reemplaza, colabora). De dónde salen los argumentos en runtime: estado (`useState`), formulario, o API — nunca hardcodeados. **El "aha" que cerró:** esta función ES `setState(prev => ({ ...prev, color }))` escrito en línea (prev=base, {color}=parche) → ya usaba el patrón en React sin saberlo. Ejemplo aterrizado dado (tarjeta + botón "pintar de azul": clic=HTML → `aplicarUnParche`=lógica → `style`=CSS). REUSAR este encuadre de capas en FASE 2. (c) **drills 7 (`aplicarUnParcheCaja`, mismo patrón con `Caja`) y 8 (`aplicarDosParchesBis`, base + 2 parches = gemelo EXACTO del drill 1 de -05) a la primera, tipo de parámetro incluido** — el patrón base+parche ya es gesto automático. Objetivo del archivo cumplido: el drill 1 de -05 que no entendía "para nada" quedó reconstruido desde los cimientos (A spread → B Partial → C base+1 → D base+2). Notas del cierre 4–5: (a) drill 4 `tomarParcial` costó — primero cambió el tipo de RETORNO (`: Punto`) en vez del PARÁMETRO y dijo "no entiendo"; se reenseñó con las DOS etiquetas de tipo de una función (parámetro = qué ENTRA / la puerta; retorno = qué SALE) y que `Partial<Punto>` es una puerta MÁS ANCHA: acepta incompletos Y completos (un Punto completo ES un Partial válido → relación subtipo, dibujo de círculos del bloque de literales). Pregunta suya valiosa: "¿y si meto uno completo da error?" → no, la puerta ancha los deja pasar igual. (b) drill 5 `combinarParciales`: reincidió el patrón fichado (arregló el CUERPO `{...p1,...p2}` pero dejó los parámetros en `Punto` → runtime verde / typecheck rojo en las llamadas del test); lo corrigió. Su retorno es `Partial<Punto>` a propósito (sin base, dos parciales siguen parciales) = semilla del BLOQUE C. -05 ya quedó COMPLETO (dependencia saldada).
- **`src/exercises/00-vocabulario/` CREADA (15 jun 2026) con README, drills PENDIENTES.** Idea suya: practicar NOMBRAR las piezas (no resolver lógica) porque al explicar por su cuenta cruza conceptos (clásico "se imprime" por "se retorna"). El README define el formato auto-corregible (respuesta = tipo literal de un menú cerrado, test verifica la palabra, "¿Por qué?" en comentario) y la tabla de pares confundidos. Montar los `exercise-NN.ts` cuando se retome ese hilo (ver `00-vocabulario/README.md` y memoria `project_05_diccionario_vocabulario_ts`).
- Próximos: terminar el PROYECTO "10 ejercicios por carpeta" (ver abajo), luego FASE 2 (React + TS). **exercise-06 COMPLETO (19 jun 2026).**

**🚧 PROYECTO EN CURSO (19 jun 2026): "10 ejercicios + 10 tests por carpeta".** Petición del usuario: que CADA carpeta de ejercicios en la que YA trabajamos (01–08, TS puro) tenga 10 `exercise-NN.ts` + sus 10 `exercise-NN.test.ts`. **ALCANCE acordado: SOLO carpetas 01–08** (las trabajadas); NO React (09–16, vacías), NO carpetas nuevas, NO `00-evaluacion`/`00-vocabulario`. **Formato obligatorio:** cada ejercicio nuevo = coach-style (sección EXPLICACIÓN con analogía + "por dentro" + caso real + OJO; luego EJERCICIO en escalera A/B/C; starters MAL a propósito → tests ROJOS por diseño; tipos definidos junto a su bloque; ❌ `any`/`as`; comando de test en la cabecera). **Cadencia: carpeta por carpeta** para mantener calidad (NO soltar todo de golpe). Cada `exercise-NN.test.ts` importa de su `-NN.ts` y usa `@ts-expect-error` que "voltean" (rojo TS2578 en starter → satisfecho al resolver).
  - **07-utility-types — CREADA HASTA 10 (19 jun 2026), VERIFICADA Y LISTA (20 jun 2026), PENDIENTE de resolver el usuario.** Añadidos `exercise-07` `Pick<T,K>`, `-08` `Omit<T,K>`, `-09` `Readonly<T>`, `-10` `Required<T>` (cada uno 5 drills A/B/C + su test). Baseline verificado: los 4 tests CORREN y dan **20/20 ROJOS por diseño** (starters sin resolver) → estructura OK. **✅ VERIFICADO (20 jun 2026): las SOLUCIONES previstas COMPILAN.** Protocolo ejecutado: resueltas las 4 en sitio (backup `.bak` de cada starter) → `pnpm typecheck` (`tsc -b --noEmit`) **EXIT 0 / verde** + `pnpm test:run` de los 4 **20/20 GREEN** (lógica OK, no solo tipos) → revertidas a starters desde el backup → árbol limpio. Confirmado que vuelan los puntos delicados: -07/-08/-10 los `@ts-expect-error` VOLTEAN al resolver; **-09 type-only** (Readonly se borra en runtime; su rojo es typecheck + checks "no muta"); **-10 capstone con `??`** (`borrador.titulo ?? defaults.titulo`) sin choque con `exactOptionalPropertyTypes`. Soluciones de referencia usadas: -07 `Pick<T,"a"|"b">` en retorno/parámetro + `.map` en el capstone; -08 `Omit<T,K>` con merge `{ id, ...datos }`; -09 `Readonly<T>` + spread `{ ...x }`; -10 `Required<T>` + capstone con `??` campo a campo. **La carpeta 07 se puede dar por buena: lista para que el usuario la resuelva.**
  - **01-tipos-basicos — CREADA HASTA 10 (20 jun 2026), VERIFICADA Y LISTA, PENDIENTE de resolver el usuario.** Añadidos 05–10 (5 drills A/B/C c/u + test): **05** `type` aliases (alias de objeto + alias de unión de literales como gancho del `@ts-expect-error`); **06** `unknown` vs `any` (starters con `any` que COMPILAN pero revientan/mienten en runtime → el peligro en carne propia; OJO apunta a 05-unions para el narrowing a fondo); **07** `typeof` operador de tipo (derivar `type X = typeof valor`; hueco clave = cambiar el tipo escrito a mano por `typeof`); **08** `as const` + `(typeof LISTA)[number]` (derivar una UNIÓN desde una lista; drill 1 el arreglo es añadir `as const`); **09** `interface` + `extends` (base/extendida, subtipo encaja donde se pide el supertipo); **10** capstone mini-modelo de dominio (junta as const→unión + interface/extends + filter/map). **✅ VERIFICADO (protocolo): soluciones → `pnpm typecheck` 0 errores en folder 01 + `pnpm test:run` 30/30 GREEN; degradadas a starters → 29/30 runtime ROJO + 40 errores typecheck (TS2578 `@ts-expect-error` sin voltear, TS6133 params/locals sin usar, TS6196 tipos sin usar, TS2353 excess en llamadas buenas) → todo VOLTEA verde al resolver.** Nota: apoyarse en `pnpm typecheck` además de `test:run` al corregir (varios drills tienen señal roja de TIPO vía `@ts-expect-error` que solo se ve en typecheck). **Corregido 20 jun (el usuario lo cazó): se vaciaron TODOS los cuerpos de 05–10 a stub puro (`""`/`0`/`[]`/`{}`/`false`), sin lógica ni intentos escritos, para que el usuario escriba la solución entera (no solo añada un tipo). Reverificado: 6 archivos rojos, 66 errores typecheck.**
  - **02-funciones — CREADA HASTA 10 (20 jun 2026), VERIFICADA Y LISTA, PENDIENTE de resolver el usuario.** Añadidos 02–10 (5 drills A/B/C c/u + test), subtemas que AÑADEN sobre el 01 (funciones-valor/callbacks/factories): **02** opcionales `?` + por defecto `=` (red: llamar con menos args = TS2554); **03** rest `...args` (red: pasar args sueltos a param array); **04** alias de tipo función (`type Operacion`, `Predicado`, `Transformador`); **05** `void` (callbacks de efecto + funciones que mutan un `registro`; tests observan el efecto sobre una lista); **06** callbacks a fondo (índice en el callback, cambio de tipo, `find`→`| undefined`, elegir entre callbacks); **07** closures con ESTADO mutable (contador/acumulador/alternador; `let` en la factory que persiste); **08** composición `g(f(x))` (2 y 3 funciones, cambio de tipo, aplicar 2 veces); **09** pipe (lista de funciones con `reduce`, point-free, sobre cada elemento); **10** capstone procesador de pasos (junta alias+rest+pipe+efecto+closure+objeto de closures con contador de usos). **✅ VERIFICADO: soluciones → 0 errores typecheck en folder 02 + 46/46 GREEN; starters → 45/46 runtime ROJO + 113 errores typecheck.** Nota: apoyarse en `pnpm typecheck` además de `test:run` (los `@ts-expect-error` y los tipos de callback solo se ven en typecheck). **Corregido 20 jun (el usuario lo cazó): se vaciaron TODOS los cuerpos de 02–10 a stub puro (`""`/`0`/`[]`/`{}`), sin lógica ni intentos escritos, para que el usuario escriba la solución entera (no solo añada un default/alias/tipo). Reverificado: 9 archivos rojos, 140 errores typecheck.**
  - **PENDIENTE CREAR (hueco para llegar a 10 c/u):**
    - `03-arrays`: ✅ **COMPLETA (28 jun 2026).** Creados 04–10 + tests, verificados (solución 35/35 verde → starters 35/35 rojo). Subtemas: 04 find/findIndex, 05 some/every, 06 sort inmutable+comparador, 07 slice+spread (inmutable), 08 flat/flatMap, 09 includes/indexOf/join, 10 capstone catálogo (filter+sort+map+reduce+join). Starters con cuerpo en blanco (`return undefined`/`-1`/`[]`/`false`/`''`/la entrada sin tocar).
    - `04-objetos`: ✅ **COMPLETA (28–29 jun 2026).** Creados 05–10 + tests, verificados (solución 30/30 verde → starters 30/30 rojo). Subtemas: 05 keys/values/entries, 06 Record contar+leer (guard `?? 0`, `in`, llave calculada), 07 transformar valores (entries→map→`fromEntries`, con tupla anotada `([k,v]): [string,number]`), 08 agrupar/groupBy + Set, 09 indexar `Record<number,Item>`+lookup+update inmutable, 10 capstone analítica de ventas (reduce-a-Record + Set + campeón + sort+map+join). Starters en blanco (`{}`/`[]`/`0`/`false`/`''`/`undefined`/entrada sin tocar).
    - `05-unions-narrowing`: ✅ **COMPLETA (29 jun 2026).** Creados 04–10 + tests, verificados (solución 35/35 verde → starters 35/35 rojo). Subtemas: 04 unión de literales→valor (switch exhaustivo sin default), 05 `typeof` narrowing (string|number) + predicado `x is number` en filter, 06 `Array.isArray` (T|T[]), 07 quitar null/undefined (`??` + filtro con predicado de tipo), 08 type guards propios (`x is Tipo`, reutilizados en filter), 09 narrowing con `in` (dos formas de objeto), 10 capstone valores de formulario (string|number|null). Starters en blanco (`''`/`0`/`false`/`[]`/`undefined`/entrada sin tocar).
    - `06-generics-basicos`: faltan **8** (03–10)
    - `08-discriminated-unions`: ✅ **COMPLETA (11 jul 2026).** Resueltos 03–10, 62/62 tests verdes + typecheck limpio. 10 (CAPSTONE máquina de estados de un pedido) cerrado: `cantidadItems`/`puedeCancelar`/`describirPedido` (switch 4-case + `never`) + transiciones `pagar`/`confirmar` que devuelven `Resultado<Pedido>` (patrón Result/Either, error devuelto no lanzado). Nota: los strings de error del test 10 se ajustaron a la redacción del usuario (`Sólo…`, decisión suya sobre display text).
  - **📊 ESTADO GLOBAL VERIFICADO (11 jul 2026, `vitest run src/exercises`): las 8 carpetas tienen los 10 ejercicios CREADOS (80 en total). Resueltos 73/80; PENDIENTES DE RESOLVER 7 ejercicios en 2 carpetas** — `04-objetos` (06–10) y `06-generics-basicos` (07–08). (Ojo: "COMPLETA" en las líneas de arriba significa CREADA con starters rojos por diseño, no resuelta; `04-objetos` está creada pero sus drills 06–10 aún sin resolver.) Suite: 522/555 verdes; los 33 rojos = starters de esos 7 ejercicios. Ya NO falta crear nada; solo resolver. Cerrar esas 3 carpetas = fin de TS puro → FASE 2 (React+TS, 09–16). Al crear cada carpeta: elegir subtemas que continúen el hilo del tema, mantener la escalera, y verificar SIEMPRE las dos cosas (tests corren rojos por diseño + soluciones compilan en verde). **REGLA DE STARTER (lección 20 jun, el usuario lo cazó): el CUERPO del starter va SIEMPRE en blanco/incorrecto (`return ""`, devuelve la entrada sin tocar, etc.), NUNCA la solución final — el usuario escribe la LÓGICA, no solo añade un tipo. Aunque el "arreglo" del drill sea solo de tipo (default param, `as const`, `?`, alias), igual deja el cuerpo en blanco y di "escribe el cuerpo" en la pista 👉. El cuerpo escrito = ejercicio regalado.**
  - **Sin commitear** (al cortar): 18 archivos nuevos de 02-funciones (02–10 + tests), 12 de 01-tipos-basicos (05–10 + tests), 8 de 07-utility-types (07–10 + tests), `exercise-06.ts` (drills 4–8) y este `README.md`. Nada commiteado: todo vive en el working tree.
  - **▶ RETOMAR EN LA PRÓXIMA SESIÓN (11 jul, `08` cerrada y committeada):** quedan **2 carpetas de TS puro** para entrar a FASE 2 React: `04-objetos` (06–10, 5 drills) y `06-generics-basicos` (07–08, 2 drills) = **7 ejercicios**. El usuario elige por cuál seguir. Recordatorios de estilo/limpieza aún abiertos del 09/10: 4 líneas comentadas en `exercise-09.ts` (~105–108, copia del tipo `Notificacion`) que el usuario puede borrar, y las líneas de trazado activas en `exercise-07.ts`/`exercise-10.ts` (p. ej. `pagar(...)`/`puedeCancelar(...)`) quedaron sin comentar — comentarlas cuando toque. **Slip a vigilar en FASE 2: el patrón `if(cond) return true/false` reaparece bajo presión aunque ya se drilló — pre-avisar. Y en comparaciones exactas de strings de test, la ortografía cuenta hasta la tilde/mayúscula (le pasó dos veces en el 10).** Ver [[feedback_slip_map_noop_y_tipo_retorno]], [[feedback_trace_lines_comentadas]].
- **Pendiente aparte (idea del usuario):** montar un drill PROPIO sobre el patrón real de estados `null`/`0`/`n` (texto de UI: "No disponible" vs "Agotado" vs cantidad) — surgió en F2 pero NO va encima de F2.
- **Pendiente de limpieza:** 5 referencias didácticas en `06-generics-basicos` (exercise-01 y -02) citan números planos viejos ("ejercicio 10/11/07"); actualizar a las rutas nuevas. Y borrar línea suelta `nombreTipo(...) // --- IGNORE ---` en `08-discriminated-unions/exercise-02.ts:73`.

---

### Archivo histórico — Prompt original del modo estricto

Este fue el contrato inicial. Se mantiene como referencia para entender por qué se cambió, no como guía vigente. Si en algún momento se quiere volver a este modo, basta con decirlo explícitamente.

<details>
<summary>Ver prompt original</summary>


Quiero que actúes como un entrenador técnico estricto especializado en TypeScript y React + TypeScript.

Tu objetivo NO es hacer el trabajo por mí.
Tu objetivo es obligarme a pensar, resolver problemas y desarrollar criterio técnico real.

#### CONTEXTO

Ya tengo buenas bases de JavaScript y React.
He trabajado bastante con React moderno y componentes.
Sin embargo, quiero aprender TypeScript de verdad, especialmente aplicado a React y entornos profesionales.

NO quiero un curso teórico.
NO quiero explicaciones eternas.
NO quiero copiar soluciones.

Quiero aprender resolviendo ejercicios progresivos y acumulativos.

#### ENTORNO DE TRABAJO

Todos los ejercicios deben hacerse dentro de un único proyecto React + TypeScript.

La estructura base debe ser algo similar a:

```
src/
  exercises/
    01-types/
    02-functions/
    03-arrays/
    etc...
```

Los ejercicios deben vivir dentro de carpetas organizadas por tema y dificultad.

NO quiero proyectos sueltos fuera del repositorio.
Todo debe mantenerse limpio, escalable y cercano a un entorno profesional.

#### METODOLOGÍA

Tu trabajo es actuar como un sistema de entrenamiento progresivo.

Debes:

- Darme UN ejercicio a la vez.
- NO avanzar al siguiente ejercicio hasta que el actual esté correctamente resuelto.
- Revisar mi solución críticamente.
- Detectar malas prácticas.
- Detectar tipados innecesarios.
- Detectar `any` innecesarios.
- Detectar complejidad innecesaria.
- Detectar errores conceptuales.
- Detectar oportunidades de simplificación.
- Explicar brevemente por qué algo está mal.
- Hacerme pensar antes de dar respuestas.

NO debes sobreexplicar.
NO debes escribir soluciones completas demasiado rápido.
NO debes rescatarme inmediatamente cuando me atasque.

#### FORMATO DE CADA EJERCICIO

Cada ejercicio debe incluir:

##### 1. Contexto o enunciado
Un párrafo breve explicando el problema.

##### 2. Objetivo técnico
Qué habilidad específica se busca practicar.

##### 3. Herramientas relevantes
Ejemplo: `map`, `filter`, `reduce`, ternarios, unions, generics, interfaces, narrowing, React props, `useState`, eventos tipados.

IMPORTANTE: NO expliques exactamente cómo usar esas herramientas. Solo menciona cuáles son relevantes.

##### 4. Restricciones
Ejemplo: no usar `any`, no usar type assertions innecesarias, no mutar arrays, evitar lógica duplicada, mantener funciones puras, no usar librerías externas.

##### 5. Estructura sugerida
Ejemplo: `src/exercises/03-arrays/exercise-01.ts` o `src/exercises/08-react-props/UserCard.tsx`.

##### 6. Criterios de validación
Qué debe cumplirse para considerar el ejercicio correcto.

#### SISTEMA DE AYUDA

Debes trabajar con niveles de ayuda progresivos. Si me bloqueo:

- **Nivel 1** — pista conceptual mínima.
- **Nivel 2** — orientación más concreta.
- **Nivel 3** — mostrar una parte pequeña de la solución.
- **Nivel 4** — solución completa explicada.

IMPORTANTE: NO subir automáticamente de nivel. Esperar a que yo lo pida.

#### REGLAS IMPORTANTES

- NO generar el siguiente ejercicio automáticamente.
- Esperar siempre mi solución.
- Priorizar razonamiento sobre velocidad.
- Obligarme a justificar decisiones técnicas cuando tenga sentido.
- Hacer ejercicios acumulativos reutilizando conceptos anteriores.
- Variar dificultad progresivamente.
- Introducir escenarios reales gradualmente.

#### PROGRESIÓN DE APRENDIZAJE

**FASE 1 — TypeScript puro (.ts)**
tipos básicos · inferencia · funciones · arrays · objetos · unions · narrowing · generics básicos · utility types · readonly · discriminated unions.

**FASE 2 — React + TypeScript (.tsx)**
props · children · eventos · formularios · `useState` · `useReducer` · custom hooks · fetch tipado · composición · context · refs · componentes reutilizables · tablas genéricas · patterns reales.

**FASE 3 — Escenarios reales**
mini features · refactors · debugging · APIs inconsistentes · arquitectura · tipado avanzado · separación de responsabilidades · performance básica · patrones reutilizables.

#### FORMA DE CORREGIR

Cuando revises mi solución:

1. Primero dime qué está bien.
2. Luego señala errores reales.
3. Luego señala mejoras importantes.
4. Luego dime si el ejercicio está aprobado o no.
5. SOLO si está aprobado puedes desbloquear el siguiente.

#### MUY IMPORTANTE

NO quiero progreso falso.

- Si mi solución funciona pero demuestra que no entendí el concepto: NO apruebes el ejercicio.
- Si estoy abusando de `any`: NO apruebes el ejercicio.
- Si estoy copiando patrones incorrectos: corrígeme.

Quiero desarrollar criterio profesional real.

#### COMPORTAMIENTO GENERAL

Sé directo. Sé técnico. Sé exigente. Prioriza claridad y razonamiento.

No me trates como principiante absoluto. Pero tampoco asumas conocimientos avanzados de TypeScript.

</details>
