# Dependencias — se reinstalan con npm install, no se suben al repo
node_modules/
**/node_modules/

# Archivos de compilación — se regeneran al hacer build, no necesitan versionarse
dist/
build/
dist-ssr/
**/dist/
**/build/

# Logs — archivos de registro generados por los package managers al fallar
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Archivos del sistema operativo — macOS genera .DS_Store, Windows genera Thumbs.db
.DS_Store
Thumbs.db

# Variables de entorno — contienen datos sensibles (API keys, contraseñas, etc.)
.env
.env.local
.env.development
.env.production
.env.test

# Configuración local del editor — versionamos solo lo compartible del workspace
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/*.code-snippets
.idea/

# Librería de iconos local — se mantiene en disco como recurso, no se versiona (infla el repo)
01-learning/03-css/00-assets/icons/

# Librería de fuentes local — mismo criterio que los iconos: recurso re-descargable, no se versiona (~464 MB)
01-learning/03-css/00-assets/fonts/

# (Las reglas de los assets del curso Three.js Journey vivían aquí. La carpeta
#  `04-javascript/05-libraries/3D/` se movió a su propio repo el 26 jul 2026,
#  así que estas reglas ya no apuntaban a nada y se retiraron.)

# Archivos generados por asistentes de IA — no aportan al código del proyecto
**/.agent/
**/.agents/
**/.windsurf/
**/skills-lock.json

# De `.claude/` se ignora el CONTENIDO (settings.local.json es config de esta máquina)…
**/.claude/*
# …salvo las skills PROPIAS de 05-typescript, que son material del proyecto: van
# versionadas y viajan con el repo, igual que su CLAUDE.md.
# La excepción es deliberadamente estrecha: las skills de OTROS proyectos son de
# terceros (frontend-design, responsive-design), o sea dependencias instaladas, y
# ésas siguen fuera de git.
!01-learning/05-typescript/.claude/
!01-learning/05-typescript/.claude/skills/
