# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Snapshot de datos generado en build (red de seguridad del fetch a la API).
# Se regenera en cada `pnpm build`; no tiene sentido versionarlo.
public/data-snapshot.json

# Env files (la URL es pública pero no se commitea el .env real)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
