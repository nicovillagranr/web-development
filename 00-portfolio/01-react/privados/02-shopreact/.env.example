# Copia este archivo a `.env` (o `.env.local`) antes de arrancar la app.
# `src/main.jsx` comprueba esta variable ANTES de montar React y lanza si falta,
# para no acabar con fetches apuntando a `undefined/products` y una pantalla vacía.
#
# Vite expone al cliente cualquier variable que empiece por VITE_.

VITE_API_URL=https://01-products-api.vercel.app
