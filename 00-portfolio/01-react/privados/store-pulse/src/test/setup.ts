// Añade los matchers de jest-dom (toBeInTheDocument, toHaveTextContent...) al expect
// de vitest. Se carga una vez por proceso de test desde vite.config.ts.
import "@testing-library/jest-dom/vitest";
